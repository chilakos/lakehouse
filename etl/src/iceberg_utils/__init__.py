"""Iceberg catalog and table management utilities.

Provides:
- catalog: Nessie REST catalog interaction (create namespace, create table, read/write)
- maintenance: Table maintenance procedures (compaction, snapshot expiration, orphan cleanup)
"""
