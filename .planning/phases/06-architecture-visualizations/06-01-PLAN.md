---
phase: 06-architecture-visualizations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/render_html.py
  - docs/architecture/data/services.yml
  - docs/architecture/data/environments.yml
  - docs/architecture/diagrams/marketecture.mmd
  - docs/architecture/diagrams/detailed-architecture.mmd
  - docs/templates/base_architecture.html
  - docs/architecture/marketecture.html
  - docs/architecture/detailed-architecture.html
  - etl/tests/test_html_render.py
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-08]

must_haves:
  truths:
    - "Marketecture HTML page displays stats banner with 1.5 PB, 300+, 40+, and 3 Query Engines"
    - "Marketecture shows 8 capability groups with value propositions and technology labels"
    - "Detailed architecture HTML shows 20+ services grouped by 8 infrastructure layers"
    - "Hovering over a service node reveals a CSS tooltip with version, protocol, healthcheck, depends_on"
    - "extract_services() returns ports, healthcheck, depends_on for all 25 docker-compose services"
    - "Init containers (minio-init, airflow-init) are excluded from the architecture diagram"
  artifacts:
    - path: "docs/render_html.py"
      provides: "extract_services(), render_mermaid_to_svg(), render_architecture(), _placeholder_svg()"
      exports: ["extract_services", "render_mermaid_to_svg", "render_architecture"]
    - path: "docs/architecture/data/services.yml"
      provides: "Service metadata overrides: descriptions, layer groupings, protocols, exclude list"
      contains: "layers:"
    - path: "docs/architecture/data/environments.yml"
      provides: "Dev/staging/prod environment comparison data"
      contains: "deployment:"
    - path: "docs/templates/base_architecture.html"
      provides: "Jinja2 template with stats banner, service grid, CSS tooltips, page_type conditionals"
      contains: ".service-node:hover .service-tooltip"
    - path: "docs/architecture/marketecture.html"
      provides: "Rendered marketecture page with stats banner and capability groups"
      contains: "1.5 PB"
    - path: "docs/architecture/detailed-architecture.html"
      provides: "Rendered detailed architecture with service nodes and CSS tooltips"
      contains: "service-node"
    - path: "docs/architecture/diagrams/marketecture.mmd"
      provides: "Mermaid flowchart with layer subgraphs and value tags"
    - path: "docs/architecture/diagrams/detailed-architecture.mmd"
      provides: "Mermaid overview with all services and inter-layer dependencies"
  key_links:
    - from: "docs/render_html.py"
      to: "docker-compose.yml"
      via: "yaml.safe_load in extract_services()"
      pattern: "yaml\\.safe_load.*compose"
    - from: "docs/render_html.py"
      to: "docs/architecture/data/services.yml"
      via: "Override merge in extract_services()"
      pattern: "services\\.yml"
    - from: "docs/templates/base_architecture.html"
      to: "docs/render_html.py"
      via: "Jinja2 template receives services_by_layer variable"
      pattern: "services_by_layer"
    - from: "docs/architecture/detailed-architecture.html"
      to: "CSS tooltip styles"
      via: ".service-node:hover .service-tooltip CSS rule"
      pattern: "service-node.*hover.*service-tooltip"
---

<objective>
Build the architecture rendering pipeline and deliver the two primary architecture HTML pages: marketecture (executive overview) and detailed architecture (engineer reference with CSS hover tooltips).

Purpose: Establish extract_services() metadata pipeline from docker-compose.yml, the base_architecture.html template with page_type conditionals, services.yml override system, and Mermaid pre-rendering with graceful mmdc fallback. This infrastructure enables all subsequent architecture pages in Plan 02.

Output: marketecture.html with stats banner and 8 capability groups; detailed-architecture.html with 20+ services grouped by 8 layers and CSS hover tooltips showing version/protocol/healthcheck/depends_on; 7 new tests.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architecture-visualizations/06-CONTEXT.md
@.planning/phases/06-architecture-visualizations/06-RESEARCH.md

<interfaces>
<!-- Phase 5 infrastructure this plan extends -->

From docs/render_html.py (Phase 5):
```python
def extract_versions(compose_path: Path | str | None = None) -> dict[str, dict[str, str]]:
    """Parse docker-compose.yml for service versions."""

def _create_jinja_env(template_dir: Path | None = None) -> Environment:
    """Create Jinja2 env with autoescape=False, macros directory."""

def render_swots(...) -> list[Path]:
    """Render SWOT HTML pages from YAML data files."""

def render_index(...) -> Path:
    """Render SWOT index page."""
```

From docs/templates/base_swot.html (Phase 5 pattern):
- Standalone HTML with embedded CSS (navy/gold palette)
- Version-stamped footer via extract_versions()
- CSS-only collapsible sections via details/summary
- Responsive design at 768px breakpoint
- Print-friendly @media rules

From docker-compose.yml (574 lines, 25 services):
- Authoritative source for: service names, images, versions, ports, healthcheck, depends_on
- YAML anchors (e.g., &airflow-env) resolved by PyYAML safe_load
- depends_on can be dict (with conditions) or list
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Architecture rendering infrastructure and data files</name>
  <files>docs/render_html.py, docs/architecture/data/services.yml, docs/architecture/data/environments.yml, docs/architecture/diagrams/marketecture.mmd, docs/architecture/diagrams/detailed-architecture.mmd, docs/templates/base_architecture.html, etl/tests/test_html_render.py</files>
  <behavior>
    - extract_services() returns dict with ports, healthcheck, depends_on for >= 20 services from docker-compose.yml
    - extract_services() with overrides merges layer, description, protocol from services.yml
    - extract_services() excludes init containers listed in services.yml exclude_from_diagrams
    - extract_services() handles depends_on as both dict (with conditions) and list formats
    - render_mermaid_to_svg() returns SVG string or raises RuntimeError when mmdc unavailable
    - _placeholder_svg() returns valid SVG with informational message
    - Marketecture HTML contains stats banner with "1.5 PB", "300+", "40+", "Query Engines"
    - Marketecture HTML contains 8+ capability group labels
    - Detailed architecture HTML contains 20+ service-node divs grouped by layer
    - Detailed architecture HTML contains .service-node:hover .service-tooltip CSS rule
  </behavior>
  <action>
    1. Create services.yml with layer definitions (storage, catalog, query, etl, semantic, governance, security, monitoring) including color codes and labels. Add per-service overrides for all 25 docker-compose services with descriptions, protocols, and primary_port. Include exclude_from_diagrams list for init containers.

    2. Create environments.yml with dev/staging/prod data: deployment method (Docker Compose / Terraform+Docker / Terraform+EKS), orchestration, storage, bucket prefix, replicas, workers, domain, IaC tool, notes. Source prod data from terraform.tfvars if available.

    3. Create marketecture.mmd Mermaid flowchart TD with subgraph groupings for 6+ layers (Sources, Ingestion, Lakehouse, Query, Semantic, Consumers) using classDef for navy/gold styling and value tags.

    4. Create detailed-architecture.mmd Mermaid flowchart with all 8 layer subgraphs showing service connections across layers.

    5. Add to render_html.py:
       - Constants: ARCH_DIAGRAM_DIR, ARCH_DATA_DIR, ARCH_OUTPUT_DIR
       - extract_services(compose_path, overrides_path) -> dict: Parse docker-compose.yml for image/version/ports/healthcheck/depends_on (handle both dict and list depends_on), merge with services.yml overrides, filter excluded services
       - render_mermaid_to_svg(mmd_path) -> str: subprocess call to npx mmdc with -t neutral -b transparent, tempfile for output, timeout 120s
       - _placeholder_svg(message) -> str: Graceful fallback SVG with styled rectangle and text
       - render_architecture(diagram_dir, data_dir, template_dir, output_dir, compose_path) -> list[Path]: Render HTML pages using page_type variable on base_architecture.html template

    6. Create base_architecture.html Jinja2 template with:
       - Embedded CSS: navy (#1a2332) / gold (#c8a961) palette, stats-banner grid, service-node cards, service-tooltip CSS-only hover visibility toggle, arch-layer swim-lanes with colored headers, responsive design, print rules forcing tooltips visible
       - page_type conditionals: "marketecture" shows stats banner + capability groups + Mermaid SVG; "detailed" shows services_by_layer grid with tooltip nodes
       - Version-stamped footer

    7. Write tests in etl/tests/test_html_render.py:
       - test_marketecture_stats_banner: verify "1.5 PB", "300+", "40+" in rendered HTML
       - test_marketecture_capability_groups: verify 8+ capability labels
       - test_detailed_arch_all_services: verify >= 20 service-node divs
       - test_extract_services_ports: verify ports returned for >= 20 services
       - test_extract_services_excludes_init: verify minio-init/airflow-init excluded
       - test_extract_services_layer_assignment: verify layer/description/protocol merged
       - test_css_hover_tooltips: verify .service-node:hover .service-tooltip CSS rule in HTML
  </action>
  <verify>
    <automated>python3 -m pytest etl/tests/test_html_render.py -x -q</automated>
  </verify>
  <done>extract_services() returns enriched metadata for all docker-compose services. render_mermaid_to_svg() works with graceful fallback. services.yml and environments.yml data files complete. base_architecture.html template renders both marketecture and detailed page types. All 7 new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Render marketecture and detailed architecture HTML pages</name>
  <files>docs/architecture/marketecture.html, docs/architecture/detailed-architecture.html</files>
  <action>
    Run render_architecture() to produce the two HTML files:

    1. marketecture.html: Stats banner (1.5 PB managed | 300+ data sources | 40+ engineers | 3 query engines), 8 capability group boxes (Sources, ETL/Ingestion, Iceberg Lakehouse, Query Engines, Semantic Layers, BI/AI Consumers, Governance, Security) with value propositions and technology labels, Mermaid marketecture diagram (placeholder SVG if mmdc unavailable), version-stamped footer.

    2. detailed-architecture.html: 8 layer swim-lanes (Storage, Catalog, Query, ETL/Orchestration, Semantic, Governance, Security, Monitoring) with colored headers. Each service as a card showing name + primary port. CSS hover tooltips showing version, protocol, healthcheck, depends_on. Scrollable reference view. Mermaid overview diagram. Version-stamped footer.

    Verify both files are standalone HTML with embedded CSS, no JavaScript, no external dependencies. Verify file sizes are reasonable (>10 KB each).
  </action>
  <verify>
    <automated>python3 -m pytest etl/tests/test_html_render.py -x -q</automated>
  </verify>
  <done>marketecture.html renders with stats banner showing 1.5 PB/300+/40+/3 query engines and 8 capability groups. detailed-architecture.html renders 20+ services grouped by 8 layers with CSS hover tooltips. Both files are standalone HTML with embedded CSS, no JS. All tests pass.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest etl/tests/test_html_render.py -x -q` -- all tests pass including 7 new ARCH tests
- `grep -c "service-node" docs/architecture/detailed-architecture.html` -- returns >= 20
- `grep -l "<script>" docs/architecture/*.html` -- returns nothing (no JavaScript)
- Both HTML files exist with size > 10 KB
</verification>

<success_criteria>
- extract_services() parses all 25 docker-compose services and merges services.yml overrides
- Marketecture page has stats banner with 1.5 PB, 300+, 40+, query engines and 8 capability groups
- Detailed architecture page shows 20+ services by layer with CSS hover tooltips
- Mermaid rendering has graceful placeholder SVG fallback when mmdc unavailable
- All HTML files standalone with embedded CSS, no JavaScript
- 7 new tests pass for ARCH-01, ARCH-02, ARCH-08
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-visualizations/06-01-SUMMARY.md`
</output>
