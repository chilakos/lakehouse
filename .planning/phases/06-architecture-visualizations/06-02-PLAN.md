---
phase: 06-architecture-visualizations
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - docs/architecture/diagrams/data-flow.mmd
  - docs/architecture/diagrams/service-dependency.mmd
  - docs/architecture/diagrams/security-layer.mmd
  - docs/architecture/diagrams/governance-stack.mmd
  - docs/templates/macros/environment_table.html
  - docs/templates/base_arch_index.html
  - docs/templates/base_architecture.html
  - docs/render_html.py
  - docs/architecture/data-flow.html
  - docs/architecture/service-dependency.html
  - docs/architecture/security-layer.html
  - docs/architecture/governance-stack.html
  - docs/architecture/index.html
  - etl/tests/test_html_render.py
autonomous: true
requirements: [ARCH-03, ARCH-04, ARCH-05, ARCH-06, ARCH-07]

must_haves:
  truths:
    - "Data flow diagram shows complete Bronze-Silver-Gold medallion path from source to consumer"
    - "Service dependency graph shows auto-generated depends_on relationships from docker-compose.yml"
    - "Security layer visualization shows Ranger RBAC flow with three access tiers and column/row level security"
    - "Governance stack shows OpenLineage-Marquez-Grafana lineage flow for BCBS 239 compliance"
    - "Environment table shows Development, Staging, and Production columns with deployment methods"
    - "Architecture index page links all 6 architecture pages with audience-tagged cards"
  artifacts:
    - path: "docs/architecture/data-flow.html"
      provides: "Medallion data flow with Bronze/Silver/Gold transformation stages"
      contains: "Bronze"
    - path: "docs/architecture/service-dependency.html"
      provides: "Dependency graph auto-generated from docker-compose depends_on"
      contains: "depends_on"
    - path: "docs/architecture/security-layer.html"
      provides: "Ranger RBAC architecture with access tiers, masking, row-level security"
      contains: "Ranger"
    - path: "docs/architecture/governance-stack.html"
      provides: "OpenLineage-Marquez-Grafana flow plus environment comparison table"
      contains: "BCBS 239"
    - path: "docs/architecture/index.html"
      provides: "Card index linking all 6 architecture pages with audience badges"
      contains: "Executives"
    - path: "docs/templates/macros/environment_table.html"
      provides: "Jinja2 macro rendering environments.yml data as comparison table"
      contains: "env-table"
    - path: "docs/templates/base_arch_index.html"
      provides: "Architecture index template with audience-tagged card grid"
      contains: "audience"
  key_links:
    - from: "docs/architecture/data-flow.html"
      to: "docs/architecture/diagrams/data-flow.mmd"
      via: "Mermaid pre-rendered to inline SVG"
      pattern: "<svg"
    - from: "docs/architecture/governance-stack.html"
      to: "docs/architecture/data/environments.yml"
      via: "environment_table macro renders env data"
      pattern: "Development.*Staging.*Production"
    - from: "docs/architecture/index.html"
      to: "docs/architecture/*.html"
      via: "Card links to each architecture page"
      pattern: "href.*\\.html"
    - from: "docs/render_html.py"
      to: "docs/templates/base_arch_index.html"
      via: "render_arch_index() function"
      pattern: "render_arch_index"
---

<objective>
Deliver the four specialized architecture diagram pages (data-flow, service-dependency, security-layer, governance-stack), the environment comparison table, and the architecture index page.

Purpose: Complete the architecture documentation set with cross-cutting concern visualizations (security and governance spans multiple services), the environment comparison table for deployment teams, and an index page that routes readers by audience role. These pages use the rendering infrastructure from Plan 01.

Output: data-flow.html with Bronze-Silver-Gold medallion path; service-dependency.html with docker-compose depends_on graph; security-layer.html with Ranger RBAC flow; governance-stack.html with OpenLineage-Marquez-Grafana flow and environment comparison table; index.html with 6 audience-tagged cards; 6 new tests.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architecture-visualizations/06-CONTEXT.md
@.planning/phases/06-architecture-visualizations/06-RESEARCH.md
@.planning/phases/06-architecture-visualizations/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- infrastructure this plan builds on -->

From docs/render_html.py (Plan 01):
```python
ARCH_DIAGRAM_DIR = PROJECT_ROOT / "docs" / "architecture" / "diagrams"
ARCH_DATA_DIR = PROJECT_ROOT / "docs" / "architecture" / "data"
ARCH_OUTPUT_DIR = PROJECT_ROOT / "docs" / "architecture"

def extract_services(compose_path=None, overrides_path=None) -> dict[str, dict]:
    """Parse docker-compose + services.yml overrides. Returns enriched service metadata."""

def render_mermaid_to_svg(mmd_path: Path) -> str:
    """Shell out to mmdc, return SVG string. Raises RuntimeError if mmdc fails."""

def _placeholder_svg(message: str = "Mermaid CLI required") -> str:
    """Graceful fallback SVG when mmdc unavailable."""

def render_architecture(diagram_dir=None, data_dir=None, template_dir=None,
                        output_dir=None, compose_path=None) -> list[Path]:
    """Render architecture HTML pages. Uses page_type on base_architecture.html."""
```

From docs/templates/base_architecture.html (Plan 01):
- page_type conditionals: "marketecture", "detailed" already implemented
- Embedded CSS with navy/gold palette, .service-node, .service-tooltip
- Needs extension for: "data-flow", "service-dependency", "security-layer", "governance-stack" page types

From docs/architecture/data/services.yml (Plan 01):
- layers: storage, catalog, query, etl, semantic, governance, security, monitoring
- Per-service descriptions, protocols, primary_port
- exclude_from_diagrams list

From docs/architecture/data/environments.yml (Plan 01):
- Three environments with deployment, orchestration, storage, bucket, replicas, workers, domain, IaC, notes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four specialized architecture pages with Mermaid diagrams and environment table</name>
  <files>docs/architecture/diagrams/data-flow.mmd, docs/architecture/diagrams/service-dependency.mmd, docs/architecture/diagrams/security-layer.mmd, docs/architecture/diagrams/governance-stack.mmd, docs/templates/macros/environment_table.html, docs/templates/base_architecture.html, docs/render_html.py, docs/architecture/data-flow.html, docs/architecture/service-dependency.html, docs/architecture/security-layer.html, docs/architecture/governance-stack.html, etl/tests/test_html_render.py</files>
  <behavior>
    - Data flow HTML contains "Bronze", "Silver", "Gold" medallion layer labels with transformation descriptions
    - Service dependency HTML shows depends_on relationships from docker-compose.yml with service names like nessie, trino
    - Security layer HTML contains "Ranger", "RBAC", role-based access control flow
    - Governance HTML contains "OpenLineage", "Marquez", "Grafana", "BCBS 239"
    - Governance HTML contains environment table with "Development", "Staging", "Production" columns
  </behavior>
  <action>
    1. Create Mermaid .mmd source files:
       - data-flow.mmd: flowchart TD showing Sources -> Bronze (Raw) -> Silver (Cleansed) -> Gold (Business-Ready) -> Cube Semantic -> BI/AI Consumers. Use classDef with bronze (#cd7f32), silver (#c0c0c0), gold (#c8a961) colors. Label edges with transformation stages (schema validation, deduplication, business rules, aggregation).
       - service-dependency.mmd: flowchart TD with subgraph per layer from services.yml. Auto-generate edges from docker-compose depends_on. Show 6 key dependency chains: Query (trino -> nessie -> minio), ETL (airflow -> trino, airflow -> nessie), Lineage (marquez -> marquez-db), Monitoring (grafana -> prometheus), Security (ranger-admin -> ranger-db), Metadata (openmetadata -> openmetadata-db).
       - security-layer.mmd: flowchart TD showing User Roles -> Trino -> Ranger Plugin -> Policy Check -> Allow/Deny. Include sections for column-level PII masking, row-level security, audit trail, three access tiers (data_readers, data_engineers, data_admin).
       - governance-stack.mmd: flowchart TD showing PySpark/Airflow -> OpenLineage events -> Marquez API -> Marquez DB. Observability: Marquez -> Grafana dashboards. Metadata: OpenMetadata catalog -> business glossary. BCBS 239 compliance flow.

    2. Create environment_table.html Jinja2 macro: Takes environments list, renders HTML table with "Aspect" label column and one column per environment. Rows: Deployment Method, Orchestration, Object Storage, Bucket Prefix, Service Replicas, Airflow Workers, Domain, IaC Tool, Notes. CSS: .env-table with navy header (#1a2332), alternating row stripes, responsive wrapper.

    3. Extend base_architecture.html with page_type conditionals for "data-flow", "service-dependency", "security-layer", "governance-stack". Each page type renders:
       - Mermaid SVG diagram (or placeholder)
       - Narrative section with .narrative CSS class (gold left border) explaining the diagram context
       - governance-stack additionally includes the environment_table macro call

    4. Extend render_architecture() in render_html.py to render 4 new pages. Each page uses base_architecture.html with appropriate page_type, title, subtitle, svg_diagram, and page-specific data (security services for security-layer, environment data for governance-stack).

    5. Write tests in etl/tests/test_html_render.py:
       - test_data_flow_medallion_path: verify "Bronze", "Silver", "Gold" in rendered data-flow.html
       - test_service_dependency_edges: verify depends_on references in service-dependency.html
       - test_security_ranger_services: verify "Ranger", "RBAC" in security-layer.html
       - test_governance_lineage_flow: verify "OpenLineage", "Marquez", "BCBS 239" in governance-stack.html
       - test_environment_table_columns: verify "Development", "Staging", "Production" in governance-stack.html
  </action>
  <verify>
    <automated>python3 -m pytest etl/tests/test_html_render.py -x -q</automated>
  </verify>
  <done>4 specialized architecture HTML pages rendered with Mermaid diagrams and narrative sections. Environment comparison table on governance-stack.html shows dev/staging/prod differences. All 5 new tests pass. All HTML files standalone with embedded CSS, no JavaScript.</done>
</task>

<task type="auto">
  <name>Task 2: Architecture index page with audience-tagged cards</name>
  <files>docs/templates/base_arch_index.html, docs/render_html.py, docs/architecture/index.html, etl/tests/test_html_render.py</files>
  <action>
    1. Create base_arch_index.html template with card grid layout. Each card has:
       - Title (page name)
       - Audience badge (color-coded: Executives=navy, Engineers=green, Security=red, Compliance=purple)
       - Description (1-2 sentence summary)
       - Link to the architecture page

    2. Add render_arch_index() function to render_html.py. Define arch_pages list with 6 entries:
       - Marketecture (Executives) - Executive overview of the lakehouse platform
       - Detailed Architecture (Engineers) - Complete service reference with ports and protocols
       - Data Flow (Engineers) - Bronze-Silver-Gold medallion data pipeline
       - Service Dependencies (Engineers) - Inter-service dependency graph
       - Security Layer (Security) - Ranger RBAC and access control
       - Governance Stack (Compliance) - Lineage, observability, and BCBS 239 compliance

    3. Render index.html at docs/architecture/index.html with version-stamped footer.

    4. Add test verifying index.html contains 6 href links to all architecture pages and audience badges (Executives, Engineers, Security, Compliance).
  </action>
  <verify>
    <automated>python3 -m pytest etl/tests/test_html_render.py -x -q</automated>
  </verify>
  <done>Architecture index page links all 6 architecture HTML files with audience-tagged cards. Index renders with correct card descriptions and role-based color badges. Test confirms all 6 links and 4 audience types present. All tests pass.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest etl/tests/test_html_render.py -x -q` -- all tests pass including 6 new tests
- `python3 -m pytest etl/tests/ -x -q` -- full suite passes
- All 7 architecture HTML files exist in docs/architecture/ (index + 6 pages)
- `grep -l "<script>" docs/architecture/*.html` returns nothing (no JavaScript)
- governance-stack.html contains environment comparison table with 3 environment columns
- index.html contains 6 card links with audience badges
</verification>

<success_criteria>
- Data flow page shows complete Bronze-Silver-Gold medallion path with transformation labels
- Service dependency page shows auto-extracted depends_on relationships from docker-compose.yml
- Security layer page documents Ranger RBAC with three access tiers, column masking, row-level security
- Governance page shows OpenLineage-Marquez-Grafana flow for BCBS 239 and environment comparison table
- Architecture index links all 6 pages with audience-tagged cards (Executives, Engineers, Security, Compliance)
- All HTML files are standalone with embedded CSS, no JavaScript, version-stamped footers
- 6 new tests pass for ARCH-03 through ARCH-07 plus index verification
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-visualizations/06-02-SUMMARY.md`
</output>
