---
phase: 08-data-catalog-and-glossary
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - docs/render_html.py
  - docs/templates/base_catalog.html
  - docs/catalog/data/metrics.yml
  - docs/catalog/data/regulatory.yml
  - docs/catalog/data/lineage.yml
  - docs/catalog/diagrams/trading-lineage.mmd
  - docs/catalog/diagrams/risk-lineage.mmd
  - docs/catalog/diagrams/lineage-overview.mmd
  - docs/catalog/diagrams/term-relationships.mmd
  - docs/catalog/metrics.html
  - docs/catalog/regulatory.html
  - docs/catalog/lineage.html
  - etl/tests/test_html_render.py
autonomous: true
requirements: [CAT-05, CAT-06, CAT-07, CAT-08]

must_haves:
  truths:
    - "Metric definitions page shows exact calculation logic (SQL) for all 8 Cube measures (total_notional, trade_count, avg_price, total_market_value, total_var_95, total_var_99, total_expected_shortfall, position_count)"
    - "Each metric shows human-readable formula by default with collapsible section revealing actual Cube SQL"
    - "Regulatory terms page has precise compliance definitions for BCBS 239, PII, VaR, and Expected Shortfall"
    - "BCBS 239 compliance tracing shows full audit trail from regulatory term to Gold table to Silver source to Bronze ingestion"
    - "Lineage page renders per-domain Mermaid SVG diagrams showing source-to-consumer flow (or placeholder SVGs)"
    - "Term relationship graph visualizes domain clusters with cross-domain connections between related terms"
  artifacts:
    - path: "docs/render_html.py"
      provides: "extract_cube_metrics() function extracting measures from Cube YAML files"
      exports: ["extract_cube_metrics"]
    - path: "docs/catalog/data/metrics.yml"
      provides: "Metrics page data with page_type: metrics"
      contains: "page_type"
    - path: "docs/catalog/data/regulatory.yml"
      provides: "Regulatory terms page data with BCBS 239 compliance tracing"
      contains: "bcbs"
    - path: "docs/catalog/data/lineage.yml"
      provides: "Lineage page data with diagram references"
      contains: "page_type"
    - path: "docs/catalog/diagrams/trading-lineage.mmd"
      provides: "Trading domain lineage Mermaid diagram"
      contains: "flowchart"
    - path: "docs/catalog/diagrams/risk-lineage.mmd"
      provides: "Risk domain lineage Mermaid diagram"
      contains: "flowchart"
    - path: "docs/catalog/diagrams/lineage-overview.mmd"
      provides: "Cross-domain overview lineage Mermaid diagram"
      contains: "flowchart"
    - path: "docs/catalog/diagrams/term-relationships.mmd"
      provides: "Term relationship graph Mermaid diagram with domain clusters"
      contains: "graph"
    - path: "docs/catalog/metrics.html"
      provides: "Rendered metrics page with Cube measure definitions and collapsible SQL"
      min_lines: 80
    - path: "docs/catalog/regulatory.html"
      provides: "Rendered regulatory terms page with BCBS 239 compliance tracing"
      min_lines: 80
    - path: "docs/catalog/lineage.html"
      provides: "Rendered lineage page with SVG diagrams (or placeholders)"
      min_lines: 80
    - path: "etl/tests/test_html_render.py"
      provides: "Tests for metrics, regulatory, lineage, and term relationships"
      contains: "test_catalog_metrics"
  key_links:
    - from: "docs/render_html.py"
      to: "semantic/model/cubes/*.yml"
      via: "extract_cube_metrics() parses Cube YAML measures"
      pattern: "yaml\\.safe_load"
    - from: "docs/render_html.py"
      to: "docs/catalog/diagrams/*.mmd"
      via: "render_mermaid_to_svg() renders lineage diagrams"
      pattern: "render_mermaid_to_svg"
    - from: "docs/catalog/data/regulatory.yml"
      to: "docs/catalog/data/glossary.yml"
      via: "Regulatory terms reference glossary terms and table mappings for compliance tracing"
      pattern: "gold\\."
---

<objective>
Add metric definitions from Cube YAML, regulatory terms with BCBS 239 compliance tracing, data lineage visualizations, and term relationship graph to complete the data catalog.

Purpose: Business users see exact metric calculation logic, compliance officers can trace regulated terms end-to-end, and data engineers see lineage flows per domain -- completing the 8 CAT requirements.

Output: 3 rendered HTML pages (metrics.html, regulatory.html, lineage.html), extract_cube_metrics() function, 3 YAML data files, 4 Mermaid diagrams, catalog rendering tests.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-catalog-and-glossary/08-CONTEXT.md
@.planning/phases/08-data-catalog-and-glossary/08-RESEARCH.md
@.planning/phases/08-data-catalog-and-glossary/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01. Executor should use these directly. -->

From docs/render_html.py (created in Plan 01):
```python
CATALOG_DATA_DIR = PROJECT_ROOT / "docs" / "catalog" / "data"
CATALOG_DIAGRAM_DIR = PROJECT_ROOT / "docs" / "catalog" / "diagrams"
CATALOG_OUTPUT_DIR = PROJECT_ROOT / "docs" / "catalog"

def extract_glossary_terms(glossary_path: Path | None = None) -> dict[str, list[dict]]:
    """Load glossary-seed.json and group terms by business domain."""

def extract_freshness_slas(tracker_path: Path | None = None) -> dict[str, dict]:
    """Extract DEFAULT_SLAS from freshness_tracker.py using AST parsing."""

def render_catalog_docs(data_dir, diagram_dir, template_dir, output_dir, compose_path) -> list[Path]:
    """Render all catalog documentation pages from YAML data + Jinja2 templates."""
```

From docs/templates/base_catalog.html (created in Plan 01):
- page_type branching: glossary, freshness, medallion, catalog-index
- Needs NEW page_type branches: metrics, regulatory, lineage
- Existing CSS: .term-card, .domain-section, .freshness-badge, .badge-green/yellow/red, .sla-card, .mapping-table
- Uses macros: collapsible.html, code_block.html

From semantic/model/cubes/trading_metrics.yml (3 measures):
```yaml
cubes:
  - name: trading_metrics
    sql_table: gold.trading_metrics
    measures:
      - name: total_notional
        sql: total_notional
        type: sum
        description: "Sum of notional values..."
        meta:
          glossary_term: notional_value
      - name: trade_count (sql: trade_count, type: sum, glossary_term: trade_count)
      - name: avg_price (sql: avg_price, type: avg, glossary_term: average_price)
```

From semantic/model/cubes/risk_exposure.yml (5 measures):
```yaml
cubes:
  - name: risk_exposure
    sql_table: gold.risk_exposure
    measures:
      - name: total_market_value (sql: total_market_value, type: sum, glossary_term: market_value)
      - name: total_var_95 (sql: total_var_95, type: sum, glossary_term: var_95)
      - name: total_var_99 (sql: total_var_99, type: sum, glossary_term: var_99)
      - name: total_expected_shortfall (sql: total_expected_shortfall, type: sum, glossary_term: expected_shortfall)
      - name: position_count (sql: position_count, type: sum, glossary_term: position_count)
```

From etl/src/governance/lineage_stubs.py (known lineage paths):
- Teradata sources: trades_history, positions_daily, counterparty_master (all from teradata.dw)
- Snowflake sources: risk_metrics, trading_summary (all from snowflake.analytics)

Lineage flow for diagrams:
- Trading: teradata.dw.trades_history -> bronze.raw_trades_history -> silver.trades_validated -> gold.trading_metrics -> Cube trading_metrics -> Tableau/Power BI/NL-to-SQL
- Risk: teradata.dw.positions_daily + snowflake.analytics.risk_metrics -> bronze -> gold.risk_exposure -> Cube risk_exposure -> Tableau/Power BI/NL-to-SQL

From infra/docker/openmetadata/glossary-seed.json (relatedTerms for term graph):
- Trade relates to: Position, Counterparty, Settlement
- Position relates to: Trade, Business Unit, Risk Metrics
- var_95 relates to: var_99, expected_shortfall, BCBS 239
- BCBS 239 relates to: SLA, Data Freshness, Bronze Layer
- (etc. -- all 17 terms have relatedTerms field)

From etl/src/governance/classification.py (PII context):
```python
class SensitivityLevel(str, Enum):
    PUBLIC = "PUBLIC"
    INTERNAL = "INTERNAL"
    CONFIDENTIAL = "CONFIDENTIAL"
    RESTRICTED = "RESTRICTED"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extract_cube_metrics(), new page_type branches, YAML data files, Mermaid diagrams, and tests</name>
  <files>docs/render_html.py, docs/templates/base_catalog.html, docs/catalog/data/metrics.yml, docs/catalog/data/regulatory.yml, docs/catalog/data/lineage.yml, docs/catalog/diagrams/trading-lineage.mmd, docs/catalog/diagrams/risk-lineage.mmd, docs/catalog/diagrams/lineage-overview.mmd, docs/catalog/diagrams/term-relationships.mmd, etl/tests/test_html_render.py</files>
  <behavior>
    - Test: extract_cube_metrics() parses both Cube YAML files and returns list of 8 metric dicts with keys: cube_name, sql_table, measure_name, measure_type, description, glossary_term, sql
    - Test: metrics page_type renders all 8 measures with human-readable description, Cube reference (cube_name, sql_table), and collapsible SQL formula
    - Test: regulatory page_type renders BCBS 239, PII, VaR, Expected Shortfall definitions with compliance framing and audit trail sections
    - Test: lineage page_type renders diagram sections with either SVG content or placeholder SVGs
    - Test: term relationship content is present on lineage page (domain clusters)
  </behavior>
  <action>
    1. Add `extract_cube_metrics()` to `docs/render_html.py`:
       ```python
       def extract_cube_metrics(cube_dir: Path | None = None) -> list[dict]:
       ```
       Parse all *.yml files in `semantic/model/cubes/` using yaml.safe_load(). For each cube, iterate measures and build list of dicts with: cube_name, sql_table, measure_name, measure_type, description, glossary_term (from meta.glossary_term, defaulting to ""), sql. Handle missing meta gracefully (defensive per Research Pitfall 5).

    2. Update `render_catalog_docs()` in docs/render_html.py to:
       - Call extract_cube_metrics() and pass result as `cube_metrics` context to template
       - Render .mmd files from diagram_dir using render_mermaid_to_svg() and pass SVG content in context (follow the render_architecture() Mermaid pattern from Phase 6)

    3. Add 3 new page_type branches to `docs/templates/base_catalog.html`:
       a. **metrics**: Render metric cards grouped by cube (trading_metrics, risk_exposure). Each metric shows:
          - Metric name as heading
          - Human-readable description (from Cube YAML description field)
          - Source table reference: "From: gold.trading_metrics"
          - Glossary term link (if available)
          - Collapsible "Calculation Detail" section (via collapsible macro) showing: measure type (sum/avg), SQL column reference
          Per CONTEXT.md: "show human-readable formula by default with collapsible section revealing actual Cube SQL for analysts"

       b. **regulatory**: Render regulatory term cards with compliance framing. Each term shows:
          - Term name (BCBS 239, PII, VaR, Expected Shortfall)
          - Paraphrased definition (not verbatim regulation quotes per CONTEXT.md)
          - Compliance context section explaining why it matters for the lakehouse
          - BCBS 239 compliance tracing section: full audit trail showing term -> Gold table -> Silver source -> Bronze ingestion -> Legacy source per CONTEXT.md locked decision
          - OpenMetadata reference link

       c. **lineage**: Render lineage diagrams section. For each domain diagram:
          - Domain heading with description
          - Embedded Mermaid SVG (or placeholder) using render_mermaid_to_svg()
          - Diagram labels use both: friendly label as primary, table name in smaller text (per CONTEXT.md)
          - Term relationship graph at bottom showing domain clusters with cross-domain connections (CAT-08)

    4. Create `docs/catalog/data/metrics.yml`:
       ```yaml
       title: "Metric Definitions"
       subtitle: "Calculation logic for key business metrics from the Cube semantic layer"
       page_type: "metrics"
       output_filename: "metrics.html"
       intro: "All metric calculations are defined in the Cube semantic layer and consumed by Tableau, Power BI, and NL-to-SQL. Values shown below are extracted from Cube YAML at render time."
       cubes_intro:
         trading_metrics: "Trading activity metrics aggregated by symbol and trading side."
         risk_exposure: "Risk exposure metrics per account, sector, and currency."
       ```
       NOTE: Actual metric definitions are NOT in the YAML -- they are injected at render time by extract_cube_metrics(). YAML provides page structure and intro text only.

    5. Create `docs/catalog/data/regulatory.yml`:
       ```yaml
       title: "Regulatory & Compliance Terms"
       subtitle: "Definitions and compliance tracing for regulated data concepts"
       page_type: "regulatory"
       output_filename: "regulatory.html"
       intro: "These definitions support BCBS 239 compliance..."
       terms:
         - name: "BCBS 239"
           slug: "bcbs-239"
           definition: "Basel Committee principles requiring financial institutions to have strong risk data aggregation capabilities and internal risk reporting practices."
           compliance_context: "The lakehouse supports BCBS 239 compliance through end-to-end data lineage tracking via OpenLineage/Marquez, automated data quality checks, and freshness SLA monitoring."
           audit_trail:
             - stage: "Regulatory Term"
               detail: "BCBS 239 -- Risk Data Aggregation and Reporting"
             - stage: "Gold Tables"
               detail: "gold.trading_metrics, gold.risk_exposure"
             - stage: "Silver Sources"
               detail: "silver.trades_validated, silver.positions_enriched"
             - stage: "Bronze Ingestion"
               detail: "bronze.raw_trades_history, bronze.raw_positions_daily"
             - stage: "Legacy Sources"
               detail: "Teradata DW (trades_history, positions_daily), Snowflake Analytics (risk_metrics)"
           openmetadata_path: "/glossary/bcbs-239"
         - name: "PII"
           slug: "pii"
           definition: "Personally Identifiable Information -- data that could identify a specific individual."
           compliance_context: "PII columns are automatically classified via regex rules (classification.py) and masked for non-privileged users via Ranger tag-based masking policies. Classification levels: PUBLIC, INTERNAL, CONFIDENTIAL, RESTRICTED."
           data_classification_levels:
             - level: "PUBLIC"
               description: "Market data, non-sensitive business identifiers"
             - level: "INTERNAL"
               description: "Internal business data, not externally shareable"
             - level: "CONFIDENTIAL"
               description: "Personal contact and compensation data"
             - level: "RESTRICTED"
               description: "SSN, tax IDs, account numbers, credit card numbers"
           openmetadata_path: "/glossary/pii"
         - name: "Value at Risk (VaR)"
           slug: "var"
           definition: "Maximum expected loss over a specified time horizon at a given confidence level."
           compliance_context: "VaR at 95% and 99% confidence levels are core BCBS 239 risk metrics..."
           metric_references:
             - measure: "total_var_95"
               cube: "risk_exposure"
               table: "gold.risk_exposure"
             - measure: "total_var_99"
               cube: "risk_exposure"
               table: "gold.risk_exposure"
           openmetadata_path: "/glossary/var"
         - name: "Expected Shortfall"
           slug: "expected-shortfall"
           definition: "Average loss in the tail beyond the VaR threshold, capturing the severity of worst-case scenarios."
           compliance_context: "ES is increasingly required by regulators as a more coherent risk measure than VaR..."
           metric_references:
             - measure: "total_expected_shortfall"
               cube: "risk_exposure"
               table: "gold.risk_exposure"
           openmetadata_path: "/glossary/expected-shortfall"
       ```

    6. Create `docs/catalog/data/lineage.yml`:
       ```yaml
       title: "Data Lineage"
       subtitle: "End-to-end data flow from legacy sources to business consumers"
       page_type: "lineage"
       output_filename: "lineage.html"
       intro: "These diagrams trace data from legacy source systems through the medallion layers to the Cube semantic layer and BI/AI consumers."
       diagrams:
         - name: "Trading Domain"
           description: "Trade data flows from Teradata DW through Bronze/Silver/Gold to trading metrics consumed by Tableau, Power BI, and NL-to-SQL."
           mermaid_file: "trading-lineage.mmd"
         - name: "Risk Domain"
           description: "Risk data combines Teradata positions and Snowflake risk metrics into Gold risk_exposure consumed by BI and AI."
           mermaid_file: "risk-lineage.mmd"
         - name: "Cross-Domain Overview"
           description: "Simplified view showing how Trading and Risk domains connect at the Gold layer and feed shared consumers."
           mermaid_file: "lineage-overview.mmd"
       term_relationships:
         title: "Term Relationship Graph"
         description: "Connections between glossary terms grouped by business domain."
         mermaid_file: "term-relationships.mmd"
       ```

    7. Create 4 Mermaid diagram files in `docs/catalog/diagrams/`:

       a. `trading-lineage.mmd` -- Trading domain lineage (flowchart LR):
          Source (Teradata DW: trades_history, counterparty_master) -> Bronze (raw_trades_history, raw_counterparty) -> Silver (trades_validated) -> Gold (trading_metrics) -> Cube (trading_metrics: total_notional, trade_count, avg_price) -> BI/AI (Tableau, Power BI, NL-to-SQL)
          Use diagram labels per CONTEXT.md: friendly label as primary ("Trading Metrics"), table name in smaller text via `<br/><small>` tags.

       b. `risk-lineage.mmd` -- Risk domain lineage (flowchart LR):
          Sources (Teradata DW: positions_daily + Snowflake Analytics: risk_metrics) -> Bronze (raw_positions_daily, raw_risk_metrics) -> Gold (risk_exposure) -> Cube (risk_exposure: total_market_value, total_var_95, total_var_99, total_expected_shortfall, position_count) -> BI/AI
          Keep focused: 3-5 horizontal layers, 2-4 nodes per layer (per Research Pitfall 4).

       c. `lineage-overview.mmd` -- Simplified cross-domain overview (flowchart LR):
          Teradata DW -> Bronze Layer -> Silver Layer -> Gold Layer (trading_metrics + risk_exposure) -> Cube Semantic Layer -> Consumers (Tableau, Power BI, NL-to-SQL)
          Snowflake Analytics joins at Bronze.
          Simplified: shows how domains connect, not individual tables.

       d. `term-relationships.mmd` -- Term relationship graph (graph TD):
          Subgraph clusters by domain: Trading (Trade, notional_value, trade_count, average_price), Risk (Position, market_value, var_95, var_99, expected_shortfall, position_count), Governance (PII, BCBS 239, SLA, Data Freshness, Business Unit), Infrastructure (Bronze Layer, Silver Layer, Gold Layer).
          Cross-domain connections using relatedTerms from glossary-seed.json:
          - Trade <-> Position (Trading-Risk)
          - BCBS 239 <-> var_95/var_99/expected_shortfall (Governance-Risk)
          - Data Freshness <-> Bronze/Silver/Gold (Governance-Infrastructure)
          Style domain subgraphs with domain colors.

    8. Add tests to `etl/tests/test_html_render.py`:
       - test_extract_cube_metrics: Verify returns list of 8 dicts with correct keys, all have glossary_term
       - test_catalog_metrics: Verify all 8 measure names rendered, collapsible SQL sections, cube source references
       - test_catalog_regulatory: Verify BCBS 239, PII, VaR, Expected Shortfall present, audit trail section for BCBS 239, data classification levels for PII
       - test_catalog_lineage: Verify 3 diagram sections (Trading, Risk, Overview), SVG content or placeholder, term relationship graph section
       - test_catalog_term_relationships: Verify domain clusters mentioned, cross-domain connections present

    9. Re-render the catalog index page (it already links to metrics.html, regulatory.html, lineage.html from Plan 01's catalog-index.yml).

    10. Run full test suite to confirm no regressions.

    IMPORTANT decisions per CONTEXT.md (locked):
    - Metric definitions show human-readable formula by default with collapsible section revealing actual Cube SQL
    - Regulatory terms paraphrase clearly with regulation name referenced but not quoted verbatim
    - BCBS 239 compliance tracing shows full audit trail: term -> Gold table -> Silver source -> Bronze ingestion
    - Lineage diagrams use Mermaid flowcharts with graceful placeholder fallback
    - Per-domain focused lineage diagrams (~2-3) plus simplified overview
    - Term relationship graph shows domain clusters with cross-domain connections
    - Diagram labels use both: friendly label as primary, table name in smaller text
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python -m pytest etl/tests/test_html_render.py -x -q --tb=short</automated>
  </verify>
  <done>3 HTML pages rendered (metrics.html, regulatory.html, lineage.html). Metrics page shows all 8 Cube measures with collapsible SQL. Regulatory page has BCBS 239, PII, VaR, ES definitions with compliance tracing. Lineage page has 3 domain diagrams + term relationship graph. All 4 Mermaid .mmd files created. extract_cube_metrics() returns 8 measures. All tests pass (existing + new). Phase 8 requirements CAT-01 through CAT-08 fully addressed.</done>
</task>

</tasks>

<verification>
1. `python -m pytest etl/tests/test_html_render.py -x -q --tb=short` -- all tests pass
2. `ls docs/catalog/metrics.html docs/catalog/regulatory.html docs/catalog/lineage.html` -- all 3 HTML files exist
3. `ls docs/catalog/diagrams/trading-lineage.mmd docs/catalog/diagrams/risk-lineage.mmd docs/catalog/diagrams/lineage-overview.mmd docs/catalog/diagrams/term-relationships.mmd` -- all 4 Mermaid files exist
4. `grep -c "measure_name\|Cube SQL\|Calculation" docs/catalog/metrics.html` -- metric content present
5. `grep "BCBS 239" docs/catalog/regulatory.html` -- regulatory terms present
6. `grep -c "svg\|placeholder" docs/catalog/lineage.html` -- diagrams rendered (SVG or placeholder)
7. Total catalog pages: `ls docs/catalog/*.html | wc -l` -- should be 7 (glossary, medallion, freshness-slas, index, metrics, regulatory, lineage)
</verification>

<success_criteria>
- 7 total HTML pages exist under docs/catalog/ (4 from Plan 01 + 3 from Plan 02)
- Metrics page shows all 8 Cube measures from trading_metrics.yml and risk_exposure.yml
- Each metric has human-readable description and collapsible Cube SQL formula
- Regulatory page has BCBS 239, PII, VaR, Expected Shortfall with compliance framing
- BCBS 239 shows full audit trail (term -> Gold -> Silver -> Bronze -> Legacy source)
- PII section references classification levels (PUBLIC through RESTRICTED)
- Lineage page has 3 domain diagrams (Trading, Risk, Overview) as SVG or placeholder
- Term relationship graph shows domain clusters with cross-domain connections
- extract_cube_metrics() correctly parses both Cube YAML files
- All Mermaid .mmd source files exist for future re-rendering
- All tests pass (full test_html_render.py suite)
- All 8 CAT requirements addressed (CAT-01 through CAT-08)
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-catalog-and-glossary/08-02-SUMMARY.md`
</output>
