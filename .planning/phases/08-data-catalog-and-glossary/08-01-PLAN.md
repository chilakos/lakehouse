---
phase: 08-data-catalog-and-glossary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/templates/base_catalog.html
  - docs/render_html.py
  - docs/catalog/data/glossary.yml
  - docs/catalog/data/medallion.yml
  - docs/catalog/data/freshness.yml
  - docs/catalog/data/catalog-index.yml
  - docs/catalog/glossary.html
  - docs/catalog/medallion.html
  - docs/catalog/freshness-slas.html
  - docs/catalog/index.html
  - etl/tests/test_html_render.py
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]

must_haves:
  truths:
    - "A business user can look up any of 17 glossary terms grouped by domain (Trading, Risk, Governance, Infrastructure) and find a plain-language definition"
    - "Each glossary term links to its physical table location in lakehouse.gold.* and shows an OpenMetadata reference path"
    - "A consolidated term-to-table mapping table provides a quick-reference view of all term-table relationships"
    - "The medallion layer explanation communicates Bronze/Silver/Gold concepts to non-technical readers with real table examples"
    - "Freshness SLA page shows traffic-light badges (GREEN/YELLOW/RED) with exact thresholds extracted from freshness_tracker.py at render time"
    - "A catalog index page links to all catalog pages with audience-tagged cards (Business Users, Compliance, Data Engineers)"
  artifacts:
    - path: "docs/templates/base_catalog.html"
      provides: "Jinja2 template for all catalog pages with page_type branching, traffic-light CSS, term card CSS, domain grouping CSS"
      min_lines: 200
    - path: "docs/render_html.py"
      provides: "render_catalog_docs(), extract_glossary_terms(), extract_freshness_slas() functions"
      exports: ["render_catalog_docs", "extract_glossary_terms", "extract_freshness_slas"]
    - path: "docs/catalog/data/glossary.yml"
      provides: "17 glossary terms from glossary-seed.json grouped by 4 business domains with term-to-table mapping"
      contains: "page_type"
    - path: "docs/catalog/data/freshness.yml"
      provides: "Freshness SLA page data with page_type: freshness"
      contains: "page_type"
    - path: "docs/catalog/glossary.html"
      provides: "Rendered glossary page with domain-grouped terms and inline mappings"
      min_lines: 100
    - path: "docs/catalog/freshness-slas.html"
      provides: "Rendered freshness SLA page with traffic-light badges"
      min_lines: 50
    - path: "docs/catalog/index.html"
      provides: "Catalog index page with audience-tagged cards"
      min_lines: 50
    - path: "etl/tests/test_html_render.py"
      provides: "Catalog rendering tests"
      contains: "test_catalog_glossary"
  key_links:
    - from: "docs/render_html.py"
      to: "docs/catalog/data/*.yml"
      via: "render_catalog_docs() loads YAML data files"
      pattern: "yaml\\.safe_load"
    - from: "docs/render_html.py"
      to: "etl/src/governance/freshness_tracker.py"
      via: "extract_freshness_slas() AST parses DEFAULT_SLAS"
      pattern: "ast\\.parse"
    - from: "docs/render_html.py"
      to: "infra/docker/openmetadata/glossary-seed.json"
      via: "extract_glossary_terms() loads and groups by domain"
      pattern: "glossary-seed\\.json"
    - from: "docs/templates/base_catalog.html"
      to: "docs/render_html.py"
      via: "Jinja2 template rendered by render_catalog_docs()"
      pattern: "base_catalog\\.html"
---

<objective>
Create the catalog documentation infrastructure and render the first 4 pages: business glossary with term-to-table mapping, medallion layer explanation, data freshness SLAs with traffic-light badges, and the catalog index page.

Purpose: Establish the base_catalog.html template, extraction functions (extract_glossary_terms, extract_freshness_slas), and render_catalog_docs() pipeline so Plan 02 can add metrics, regulatory, and lineage pages using the same infrastructure.

Output: 4 rendered HTML pages (glossary.html, medallion.html, freshness-slas.html, index.html), base_catalog.html template, 3 extraction functions in render_html.py, 4 YAML data files, catalog rendering tests.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-catalog-and-glossary/08-CONTEXT.md
@.planning/phases/08-data-catalog-and-glossary/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From docs/render_html.py (existing functions to follow as patterns):
```python
PROJECT_ROOT = Path(__file__).resolve().parent.parent
SWOT_DATA_DIR = PROJECT_ROOT / "docs" / "swot" / "data"

def _create_jinja_env(template_dir: Path | None = None) -> Environment:
    """Create Jinja2 environment with FileSystemLoader."""

def extract_versions(compose_path: Path | str | None = None) -> dict[str, dict[str, str]]:
    """Extract component versions from docker-compose.yml."""

def extract_services(compose_path: Path | str | None = None, services_yaml: Path | None = None) -> list[dict]:
    """Parse docker-compose.yml services merged with services.yml overrides."""

def extract_package_api(package_dir: Path) -> dict:
    """AST-based API extraction without runtime imports."""

def render_developer_docs(data_dir, diagram_dir, template_dir, output_dir, compose_path) -> list[Path]:
    """Render all developer doc pages from YAML data + Jinja2 templates."""

def render_mermaid_to_svg(mmd_path: Path) -> str:
    """Render Mermaid diagram to SVG with graceful mmdc fallback."""
```

From etl/src/governance/freshness_tracker.py (AST extraction target):
```python
DEFAULT_SLAS: dict[str, FreshnessSLA] = {
    "gold.*": FreshnessSLA(table_name="gold.*", expected_update_interval_hours=24.0, warning_threshold_hours=26.0, critical_threshold_hours=48.0),
    "silver.*": FreshnessSLA(table_name="silver.*", expected_update_interval_hours=12.0, warning_threshold_hours=14.0, critical_threshold_hours=24.0),
    "bronze.*": FreshnessSLA(table_name="bronze.*", expected_update_interval_hours=6.0, warning_threshold_hours=8.0, critical_threshold_hours=12.0),
}
```

From infra/docker/openmetadata/glossary-seed.json (17 terms):
- Trading domain: Trade, notional_value, trade_count, average_price
- Risk domain: Position, market_value, var_95, var_99, expected_shortfall, position_count
- Governance domain: PII, BCBS 239, SLA, Data Freshness, Business Unit
- Infrastructure domain: Bronze Layer, Silver Layer, Gold Layer

Tags for domain grouping:
- Trading: ["finance", "trading"]
- Risk: ["finance", "risk"] or ["risk"]
- Governance: ["privacy", "compliance", "governance", "sla", "quality", "monitoring"]
- Infrastructure: ["architecture", "medallion", "data-lake"]

Each term has: name, description, synonyms, relatedTerms, tags

From docs/templates/base_arch_index.html (card grid pattern):
- .card-grid with auto-fill minmax(280px, 1fr)
- .card with hover effects (box-shadow, border-color: #c8a961)
- .card-audience badge with audience-{type} CSS classes
- Navy header (#1a2332) with gold title (#c8a961)
- Footer with version stamps

From docs/templates/macros/collapsible.html (reuse for layered detail):
- CSS-only details/summary wrapper macro

From docs/templates/macros/code_block.html (reuse for SQL display):
- Code block rendering macro

Key CSS colors from project palette:
- Navy: #1a2332
- Gold: #c8a961
- Subtle text: #94a3b8, #475569
- Borders: #e2e8f0
- System font stack: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create base_catalog.html template, extraction functions, and render_catalog_docs() pipeline with tests</name>
  <files>docs/templates/base_catalog.html, docs/render_html.py, etl/tests/test_html_render.py</files>
  <behavior>
    - Test: extract_glossary_terms() loads glossary-seed.json and returns dict grouped by domain (Trading, Risk, Governance, Infrastructure) with at least 17 terms total
    - Test: extract_freshness_slas() AST-parses freshness_tracker.py and returns dict with keys "gold.*", "silver.*", "bronze.*" each having expected_hours, warning_hours, critical_hours numeric values
    - Test: render_catalog_docs() produces HTML files from YAML data files, each containing embedded CSS, navy header, version-stamped footer
    - Test: glossary page_type renders domain sections with term names, definitions, collapsible technical detail, table_mapping inline, and consolidated mapping table
    - Test: freshness page_type renders traffic-light badges (badge-green, badge-yellow, badge-red classes) with SLA threshold values
    - Test: medallion page_type renders Bronze/Silver/Gold narrative with real table examples
    - Test: catalog-index page_type renders audience-tagged cards with links to catalog pages
  </behavior>
  <action>
    1. Create `docs/templates/base_catalog.html` -- a new Jinja2 template with page_type branching for catalog pages. Reuse the project CSS palette (navy #1a2332, gold #c8a961, system font stack). Import macros: collapsible.html, code_block.html. Include page_type conditionals for: glossary, freshness, medallion, catalog-index. Add catalog-specific CSS:
       - .term-card with domain-colored left border, layered detail pattern (plain definition + collapsible technical detail via details/summary)
       - .domain-section with domain name header and domain color
       - .freshness-badge (inline-flex, rounded pill) with .badge-green (#dcfce7/#166534), .badge-yellow (#fef9c3/#854d0e), .badge-red (#fecaca/#991b1b)
       - .sla-card with threshold items in flex layout
       - .mapping-table for consolidated term-to-table mapping
       - .medallion-layer cards for Bronze/Silver/Gold
       - For catalog-index page_type: reuse card-grid layout from base_arch_index.html with audience tags (Business Users: green #059669, Compliance: purple #7c3aed, Data Engineers: blue #2563eb)
       - Responsive design at 768px breakpoint, print-friendly rules
       - OpenMetadata reference links styled as subtle info links

    2. Add to `docs/render_html.py`:
       a. Constants: CATALOG_DATA_DIR, CATALOG_DIAGRAM_DIR, CATALOG_OUTPUT_DIR
       b. `extract_glossary_terms(glossary_path: Path | None = None) -> dict[str, list[dict]]`: Load glossary-seed.json, group terms by business domain using tags. Domain mapping: Trading (tags: "trading", "finance" without "risk"), Risk (tags: "risk"), Governance (tags: "governance", "compliance", "privacy", "sla", "quality", "monitoring"), Infrastructure (tags: "architecture", "medallion", "data-lake"). Add slug field (kebab-case of name) to each term for OpenMetadata links. Handle terms that match multiple domains by using first match. Return dict with domain names as keys and list of term dicts as values.
       c. `extract_freshness_slas(tracker_path: Path | None = None) -> dict[str, dict]`: Parse freshness_tracker.py using ast.parse(). Walk AST to find DEFAULT_SLAS assignment. For each dict entry, extract the FreshnessSLA() call keyword arguments (expected_update_interval_hours, warning_threshold_hours, critical_threshold_hours) from ast.Constant nodes. Return dict mapping layer pattern (e.g., "gold.*") to {expected_hours, warning_hours, critical_hours}. Follow the extract_package_api() AST pattern from Phase 7.
       d. `render_catalog_docs(data_dir, diagram_dir, template_dir, output_dir, compose_path) -> list[Path]`: Follow the render_developer_docs() pattern. Load YAML data files from data_dir, render each through base_catalog.html template. Inject additional context at render time: glossary_terms from extract_glossary_terms(), freshness_slas from extract_freshness_slas(), cube_metrics from extract_cube_metrics() (stub empty list for now -- Plan 02 adds this), versions from extract_versions(). Write standalone HTML to output_dir. Return list of rendered file paths.

    3. Add tests to `etl/tests/test_html_render.py`:
       - test_extract_glossary_terms: Verify domain grouping, term count, slug field presence
       - test_extract_freshness_slas: Verify 3 layers returned with correct numeric thresholds
       - Catalog rendering fixture: Create sample YAML data files (glossary, freshness, medallion, catalog-index) in tmp_path, render via render_catalog_docs()
       - test_catalog_glossary: Verify domain section headers, term names, definitions present, collapsible technical detail, inline table_mapping, consolidated mapping table with CAT-02 requirement
       - test_catalog_medallion: Verify Bronze/Silver/Gold sections with real table examples
       - test_catalog_freshness_slas: Verify traffic-light badge CSS classes (badge-green, badge-yellow, badge-red), threshold values rendered
       - test_catalog_index: Verify audience-tagged cards (Business Users, Compliance, Data Engineers), links to catalog pages

    IMPORTANT decisions per CONTEXT.md (locked):
    - Layered detail: one-line plain-language definition first, then expandable "Technical Detail" section with SQL/table references via CSS-only collapsible sections
    - Terms grouped by business domain: Trading, Risk, Governance, Infrastructure
    - Term-to-table mapping shown BOTH ways: inline in each term definition AND consolidated mapping table
    - Freshness SLA display uses dashboard-style traffic-light badges with color per layer
    - SLA thresholds extracted from freshness_tracker.py at render time (same extract_services() pattern)
    - OpenMetadata references: each term shows "View in OpenMetadata: /glossary/term-name"
    - Medallion uses light technical approach with real table examples
    - Catalog index uses audience-tagged cards: "Business Users" (glossary, metrics, medallion), "Compliance" (regulatory, lineage), "Data Engineers" (freshness SLAs, term mapping)

    Key pitfalls to avoid:
    - Use `bullet_items` key name in YAML (not `items`) to avoid Jinja2 dict.items() collision
    - AST extraction: walk ast.Call nodes for FreshnessSLA() constructor, extract kw.value.value for float threshold values -- do NOT try ast.literal_eval on the full assignment
    - Normalize glossary term names to kebab-case slugs for OpenMetadata links
    - Set autoescape=False in Jinja2 env (consistent with Phase 5 decision)
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python -m pytest etl/tests/test_html_render.py -x -q --tb=short -k "catalog or extract_glossary or extract_freshness"</automated>
  </verify>
  <done>base_catalog.html template renders glossary/freshness/medallion/catalog-index page_types. extract_glossary_terms() returns 17 terms grouped by 4 domains. extract_freshness_slas() returns 3 layers with correct thresholds. All catalog tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create YAML data files and render glossary, medallion, freshness SLA, and catalog index HTML pages</name>
  <files>docs/catalog/data/glossary.yml, docs/catalog/data/medallion.yml, docs/catalog/data/freshness.yml, docs/catalog/data/catalog-index.yml, docs/catalog/glossary.html, docs/catalog/medallion.html, docs/catalog/freshness-slas.html, docs/catalog/index.html</files>
  <action>
    1. Create directory structure: `docs/catalog/data/`, `docs/catalog/diagrams/`

    2. Create `docs/catalog/data/glossary.yml` -- Transform glossary-seed.json content into domain-grouped YAML. Structure:
       ```yaml
       title: "Business Glossary"
       subtitle: "Plain-language definitions for lakehouse data terms"
       page_type: "glossary"
       output_filename: "glossary.html"
       domains:
         - name: "Trading"
           color: "#059669"
           terms:
             - name: "Trade"
               slug: "trade"
               definition: "A financial transaction..." (from glossary-seed.json description)
               technical_detail: "Raw trade data arrives in <code>bronze.raw_trades_history</code>..."
               synonyms: ["transaction", "deal", "execution"]
               related_terms: ["Position", "Counterparty", "Settlement"]
               table_mapping: "gold.trading_metrics"
               openmetadata_path: "/glossary/trade"
       # Include all 17 terms from glossary-seed.json, distributed across 4 domains
       # Term-to-table mapping for each term:
       #   Trading terms -> gold.trading_metrics
       #   Risk terms -> gold.risk_exposure
       #   Governance terms -> N/A (governance concepts, not data tables)
       #   Infrastructure terms -> N/A (architectural concepts)
       # For governance/infrastructure terms without direct table mapping, use table_mapping: null
       term_table_mapping:
         - term: "Trade"
           tables: ["bronze.raw_trades_history", "silver.trades_validated", "gold.trading_metrics"]
           cube_measure: "total_notional, trade_count, avg_price"
         # ... all terms with their full lineage paths
       ```
       Use the glossary-seed.json descriptions as-is for the definition field (they are already plain-language). Add technical_detail with table references and code tags for the collapsible section. Include OpenMetadata path as `/glossary/<slug>`.

    3. Create `docs/catalog/data/medallion.yml` -- Medallion layer explanation for non-technical users:
       ```yaml
       title: "Data Layers Explained"
       subtitle: "How data flows through Bronze, Silver, and Gold layers"
       page_type: "medallion"
       output_filename: "medallion.html"
       layers:
         - name: "Bronze Layer"
           subtitle: "Raw Data Ingestion"
           color: "#cd7f32"
           description: "Raw data as received from source systems..."
           example_tables: ["bronze.raw_trades_history", "bronze.raw_positions_daily", "bronze.raw_counterparty"]
           freshness_sla: "6 hours"
           what_happens: "Format conversion (CSV/JSON to Iceberg Parquet), metadata columns added (source_system, ingestion_ts, batch_id)"
         - name: "Silver Layer"
           subtitle: "Cleansed & Validated"
           color: "#C0C0C0"
           description: "..."
         - name: "Gold Layer"
           subtitle: "Business-Ready Analytics"
           color: "#c8a961"
           description: "..."
       ```
       Use light technical approach per CONTEXT.md: real table examples from the platform.

    4. Create `docs/catalog/data/freshness.yml` -- Freshness SLA documentation:
       ```yaml
       title: "Data Freshness SLAs"
       subtitle: "Update intervals and status thresholds by data layer"
       page_type: "freshness"
       output_filename: "freshness-slas.html"
       intro: "Data freshness is monitored using traffic-light SLA status..."
       status_definitions:
         - status: "GREEN"
           label: "On time"
           description: "Data is current -- within expected update interval"
           css_class: "badge-green"
         - status: "YELLOW"
           label: "Warning"
           description: "Data is stale -- past warning threshold but within critical threshold"
           css_class: "badge-yellow"
         - status: "RED"
           label: "Stale"
           description: "Data is critically stale -- past critical threshold, or never updated"
           css_class: "badge-red"
       monitoring_note: "Freshness is tracked in Grafana and OpenMetadata catalog. SLA breaches trigger automated alerts."
       ```
       NOTE: Actual SLA threshold values are NOT hard-coded in YAML -- they are injected at render time by extract_freshness_slas(). The YAML only provides page structure, status definitions, and intro text.

    5. Create `docs/catalog/data/catalog-index.yml` -- Catalog index page:
       ```yaml
       title: "Data Catalog"
       subtitle: "Business glossary, metrics, compliance, and data lineage documentation"
       page_type: "catalog-index"
       output_filename: "index.html"
       audience_groups:
         - name: "Business Users"
           audience_class: "business-users"
           pages:
             - title: "Business Glossary"
               filename: "glossary.html"
               description: "Plain-language definitions for all data terms, grouped by business domain"
             - title: "Data Layers Explained"
               filename: "medallion.html"
               description: "How data flows through Bronze, Silver, and Gold layers"
             - title: "Metric Definitions"
               filename: "metrics.html"
               description: "Calculation logic for key business metrics from the semantic layer"
         - name: "Compliance"
           audience_class: "compliance"
           pages:
             - title: "Regulatory Terms"
               filename: "regulatory.html"
               description: "BCBS 239, PII, VaR, Expected Shortfall definitions with compliance tracing"
             - title: "Data Lineage"
               filename: "lineage.html"
               description: "End-to-end data flow visualizations from source to consumer"
         - name: "Data Engineers"
           audience_class: "data-engineers"
           pages:
             - title: "Data Freshness SLAs"
               filename: "freshness-slas.html"
               description: "Update intervals and traffic-light status thresholds by data layer"
             - title: "Business Glossary"
               filename: "glossary.html"
               description: "Term-to-table mapping reference for all data terms"
       ```

    6. Run render_catalog_docs() to produce all 4 HTML pages. Verify each is standalone with embedded CSS, no external dependencies, version-stamped footer.

    7. Run full test suite to confirm no regressions: `python -m pytest etl/tests/test_html_render.py -q --tb=short`
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python -m pytest etl/tests/test_html_render.py -x -q --tb=short</automated>
  </verify>
  <done>4 HTML pages rendered under docs/catalog/ (glossary.html, medallion.html, freshness-slas.html, index.html). Each is standalone with embedded CSS, version-stamped footer, no external dependencies. Glossary page shows 17 terms grouped by 4 domains with inline table mapping and consolidated mapping table. Medallion page explains Bronze/Silver/Gold with real examples. Freshness page shows traffic-light badges with thresholds from freshness_tracker.py. Index page links all catalog pages with audience-tagged cards. All existing + new tests pass.</done>
</task>

</tasks>

<verification>
1. `python -m pytest etl/tests/test_html_render.py -x -q --tb=short` -- all tests pass including new catalog tests
2. `ls docs/catalog/glossary.html docs/catalog/medallion.html docs/catalog/freshness-slas.html docs/catalog/index.html` -- all 4 HTML files exist
3. `grep -l "badge-green\|badge-yellow\|badge-red" docs/catalog/freshness-slas.html` -- traffic-light badges present
4. `grep -c "domain-section" docs/catalog/glossary.html` -- at least 4 domain sections (Trading, Risk, Governance, Infrastructure)
5. `grep "mapping-table" docs/catalog/glossary.html` -- consolidated term-to-table mapping table present
6. `grep "OpenMetadata" docs/catalog/glossary.html` -- OpenMetadata references present
</verification>

<success_criteria>
- 4 rendered HTML pages exist under docs/catalog/
- Glossary page contains all 17 terms from glossary-seed.json grouped by 4 domains
- Each term has plain-language definition, collapsible technical detail, inline table mapping, and OpenMetadata link
- Consolidated term-to-table mapping table is present on glossary page
- Medallion page explains Bronze/Silver/Gold with real table examples
- Freshness SLA page shows traffic-light badges with thresholds extracted from freshness_tracker.py (not hard-coded)
- Catalog index page links to all catalog pages with audience-tagged cards
- All HTML files are standalone (embedded CSS, no JS, no external deps, file:// compatible)
- All tests pass (existing + new catalog tests)
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-catalog-and-glossary/08-01-SUMMARY.md`
</output>
