---
phase: 03-governance-security-hardening-and-platform
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [SEC-03, SEC-04, GOVN-02, GOVN-03, GOVN-04, GOVN-05, PLAT-01, PLAT-03]

must_haves:
  truths:
    - "All Phase 3 unit tests pass in a single run"
    - "Dashboard JSON files are valid and contain expected panels"
    - "Docker Compose validates with all new services defined"
    - "Governance module exports all required functions"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 3 governance, security, and platform deliverables end-to-end

Purpose: Confirm that all Phase 3 components (Ranger security, OpenMetadata catalog, compliance dashboards, audit trail) are implemented correctly and the full unit test suite passes before marking phase complete.
Output: Verification confirmation, phase ready for /gsd:verify-work
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-governance-security-hardening-and-platform/03-CONTEXT.md
@.planning/phases/03-governance-security-hardening-and-platform/03-01-SUMMARY.md
@.planning/phases/03-governance-security-hardening-and-platform/03-02-SUMMARY.md
@.planning/phases/03-governance-security-hardening-and-platform/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and validate all artifacts</name>
  <files></files>
  <action>
    Run comprehensive validation of all Phase 3 deliverables:

    1. Full unit test suite (must include Phase 3 governance tests):
       cd etl and run python -m pytest tests/unit -x -q --tb=short

    2. Validate Docker Compose with all new services:
       Parse docker-compose.yml and confirm ranger-admin, ranger-db, ranger-solr, ranger-zk, openmetadata-server, openmetadata-ingestion, elasticsearch, om-db are all defined.

    3. Validate all Grafana dashboards:
       Load pipeline_observability.json, bcbs239_compliance.json, data_freshness.json, audit_overview.json and confirm each has panels key with 3+ panels.

    4. Validate governance module exports:
       Import SensitivityLevel, classify_column, CLASSIFICATION_RULES from classification.
       Import create_masking_policy, create_row_filter_policy, create_tag_policy from ranger_policies.
       Import AuditRecord, AUDIT_SCHEMA, normalize_trino_audit from audit_schema.
       Import TrinoAuditExtractor, aggregate_audit_records from audit_aggregator.
       Import detect_anomalies, AnomalyType, AnomalyReport from anomaly_detector.
       Import FreshnessStatus, FreshnessSLA, check_table_freshness, get_freshness_badge from freshness_tracker.
       Import register_legacy_lineage_stub from lineage_stubs.

    5. Validate DAG files exist and are syntactically valid:
       dag_audit_aggregation.py, dag_anomaly_report.py, dag_freshness_check.py

    6. Validate Ranger config files exist:
       ranger-trino-security.xml, ranger-trino-audit.xml, bootstrap-policies.py, install.properties

    7. Validate Trino config updated to Ranger access control and event listener configured.

    8. Print summary of all Phase 3 artifacts with line counts.

    If any validation fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd etl && python -m pytest tests/unit -x -q --tb=short</automated>
  </verify>
  <done>Full unit test suite passes. All Docker Compose services valid. All dashboard JSON files valid. All governance module imports successful. All config files present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 3 deliverables</name>
  <files></files>
  <action>
    Present Phase 3 deliverables for human review:

    1. Apache Ranger Security (SEC-03, SEC-04, GOVN-03):
       - Ranger admin + Solr + ZooKeeper + DB in Docker Compose
       - Trino switched from file-based RBAC to Ranger access control
       - Tag-based data classification (PUBLIC/INTERNAL/CONFIDENTIAL/RESTRICTED)
       - Column masking policies: RESTRICTED to MASK_NULL for readers, CONFIDENTIAL to MASK_HASH for readers
       - Row-level filtering by business_unit for multi-BU access control
       - Bootstrap script seeds all policies for 3 roles
       - Trino HTTP event listener for audit capture

    2. OpenMetadata Data Catalog (PLAT-01, GOVN-04, PLAT-03):
       - OpenMetadata server + ingestion + Elasticsearch + DB in Docker Compose
       - Trino connector ingests bronze/silver/gold schema metadata
       - Business glossary seeded with 10 FSDM-based terms (Draft status for approval workflow demo)
       - Data freshness tracking with SLA-based traffic-light status

    3. Compliance Dashboards and Audit Trail (GOVN-02, GOVN-05):
       - BCBS 239 Compliance dashboard: accuracy (quality scores), completeness (lineage coverage), timeliness (SLA)
       - Data Freshness dashboard: per-table traffic-light SLA status
       - Audit Trail Overview dashboard: query volume, restricted access, masked columns, anomalies
       - Grafana Infinity plugin for Marquez lineage API queries
       - Cross-engine audit aggregation (Trino + Teradata + Snowflake) into common schema
       - Daily anomaly detection (bulk download, after-hours, restricted access, high-frequency)
       - 3 governance Airflow DAGs: audit aggregation, anomaly report, freshness check
       - Legacy lineage stubs for Teradata and Snowflake sources

    Verification steps for human:
    1. Review test output from Task 1 -- all unit tests should pass
    2. Review Docker Compose services count -- should include 8 new services (Ranger: 4, OpenMetadata: 4)
    3. Confirm Grafana dashboards exist with appropriate panels
    4. Confirm governance module structure has all 7 modules
    5. Confirm governance DAGs exist
    6. Type "approved" if satisfied, or describe any issues
  </action>
  <verify>Human approval received</verify>
  <done>Human confirms Phase 3 deliverables are correct and complete. Phase ready for /gsd:verify-work.</done>
</task>

</tasks>

<verification>
- Full test suite: `cd etl && python -m pytest tests/unit -x -q`
- Docker Compose: `python -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"`
- All phase requirements addressed via Plans 01-04
</verification>

<success_criteria>
- All unit tests pass (including new governance tests from Plans 01-03)
- All Docker Compose services valid (8 new services: 4 Ranger + 4 OpenMetadata)
- All 3 Grafana dashboards valid JSON with correct panels
- All governance module functions importable
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-governance-security-hardening-and-platform/03-04-SUMMARY.md`
</output>
