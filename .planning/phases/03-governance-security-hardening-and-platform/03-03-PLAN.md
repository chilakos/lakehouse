---
phase: 03-governance-security-hardening-and-platform
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - etl/src/governance/audit_schema.py
  - etl/src/governance/audit_aggregator.py
  - etl/src/governance/anomaly_detector.py
  - etl/dags/governance/dag_audit_aggregation.py
  - etl/dags/governance/dag_anomaly_report.py
  - etl/dags/governance/dag_freshness_check.py
  - etl/dags/governance/__init__.py
  - infra/docker/grafana/dashboards/bcbs239_compliance.json
  - infra/docker/grafana/dashboards/data_freshness.json
  - infra/docker/grafana/dashboards/audit_overview.json
  - infra/docker/grafana/provisioning/datasources.yml
  - infra/docker/grafana-reporter/grafana-reporter.env
  - etl/src/governance/audit_archiver.py
  - etl/tests/unit/test_audit_schema.py
  - etl/tests/unit/test_anomaly_detector.py
  - etl/tests/integration/test_compliance_lineage.py
  - etl/tests/integration/test_audit_pipeline.py
autonomous: true
requirements: [GOVN-02, GOVN-05]

must_haves:
  truths:
    - "A compliance officer can view end-to-end lineage for regulated datasets in Grafana with quality overlay showing accuracy/completeness/timeliness"
    - "Audit trail captures query access from Trino with user, tables, columns, and masked-vs-unmasked detail"
    - "Daily anomaly report flags suspicious access patterns (bulk downloads, after-hours access, unusual restricted table access)"
    - "Grafana compliance dashboards are auto-provisioned and show BCBS 239 metrics"
    - "PDF/HTML compliance reports are exportable via grafana-reporter for audit evidence archives"
    - "Audit records older than 90 days are archived to S3 Parquet for 7-year retention"
    - "Data freshness dashboard shows per-table SLA status with traffic-light indicators"
  artifacts:
    - path: "etl/src/governance/audit_schema.py"
      provides: "Common audit schema definition for cross-engine audit aggregation"
      exports: ["AuditRecord", "AUDIT_SCHEMA", "normalize_trino_audit", "normalize_teradata_audit", "normalize_snowflake_audit"]
    - path: "etl/src/governance/audit_aggregator.py"
      provides: "Cross-engine audit ETL logic"
      exports: ["TrinoAuditExtractor", "TeradataAuditExtractor", "SnowflakeAuditExtractor", "aggregate_audit_records"]
    - path: "etl/src/governance/anomaly_detector.py"
      provides: "Daily anomaly detection heuristics for audit data"
      exports: ["detect_anomalies", "AnomalyType", "AnomalyReport"]
    - path: "infra/docker/grafana/dashboards/bcbs239_compliance.json"
      provides: "BCBS 239 compliance dashboard with lineage and quality overlay"
      min_lines: 200
    - path: "infra/docker/grafana/dashboards/data_freshness.json"
      provides: "Data freshness SLA dashboard with traffic-light status per table"
      min_lines: 100
    - path: "infra/docker/grafana/dashboards/audit_overview.json"
      provides: "Audit trail overview dashboard"
      min_lines: 100
    - path: "etl/dags/governance/dag_audit_aggregation.py"
      provides: "Daily Airflow DAG for cross-engine audit ETL"
      min_lines: 50
  key_links:
    - from: "infra/docker/grafana/dashboards/bcbs239_compliance.json"
      to: "marquez:5000"
      via: "Grafana Infinity plugin queries Marquez lineage API"
      pattern: "yesoreyeram-infinity-datasource"
    - from: "etl/dags/governance/dag_audit_aggregation.py"
      to: "etl/src/governance/audit_aggregator.py"
      via: "DAG imports and runs audit ETL logic"
      pattern: "audit_aggregator"
    - from: "infra/docker/grafana/dashboards/data_freshness.json"
      to: "etl/src/governance/freshness_tracker.py"
      via: "Dashboard queries freshness data from PostgreSQL"
      pattern: "freshness"
---

<objective>
Build BCBS 239 compliance lineage dashboards, centralized cross-engine audit trail, anomaly detection, and data freshness monitoring

Purpose: Deliver regulatory compliance visibility (lineage with quality overlay for BCBS 239 accuracy/completeness/timeliness), centralized audit trail aggregating Trino/Teradata/Snowflake access logs into a common schema, daily anomaly detection for suspicious access patterns, and data freshness SLA dashboards.
Output: Audit schema and aggregation modules, anomaly detector, 3 Grafana dashboards, 3 governance DAGs, Grafana Infinity datasource for Marquez API, unit and integration tests
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-governance-security-hardening-and-platform/03-CONTEXT.md
@.planning/phases/03-governance-security-hardening-and-platform/03-RESEARCH.md
@.planning/phases/03-governance-security-hardening-and-platform/03-VALIDATION.md
@.planning/phases/03-governance-security-hardening-and-platform/03-01-SUMMARY.md
@.planning/phases/03-governance-security-hardening-and-platform/03-02-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 and Plan 02 that this plan depends on -->

From etl/src/governance/classification.py (Plan 01):
```python
from enum import Enum
class SensitivityLevel(Enum):
    PUBLIC = "PUBLIC"
    INTERNAL = "INTERNAL"
    CONFIDENTIAL = "CONFIDENTIAL"
    RESTRICTED = "RESTRICTED"

def classify_column(column_name: str) -> SensitivityLevel: ...
def classify_table_columns(columns: list[str]) -> dict[str, SensitivityLevel]: ...
```

From etl/src/governance/freshness_tracker.py (Plan 02):
```python
from enum import Enum
from dataclasses import dataclass

class FreshnessStatus(Enum):
    GREEN = "On time"
    YELLOW = "Warning"
    RED = "Stale"

@dataclass
class FreshnessSLA:
    table_name: str
    expected_update_interval_hours: float
    warning_threshold_hours: float
    critical_threshold_hours: float

def check_table_freshness(last_updated, sla) -> FreshnessStatus: ...
def get_freshness_badge(status) -> dict: ...
def get_all_freshness(table_slas, last_updated_map) -> list[dict]: ...
```

From etl/src/governance/lineage_stubs.py (Plan 02):
```python
def register_legacy_lineage_stub(marquez_url, namespace, source_name, dataset_name, description, fields=None, tags=None) -> dict: ...
```

From etl/src/lineage/config.py (Phase 2):
```python
OPENLINEAGE_NAMESPACE = "lakehouse"
_DEFAULT_OPENLINEAGE_URL = "http://marquez:5000"
```

From infra/docker/grafana/provisioning/datasources.yml (Phase 2):
```yaml
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
```

From infra/docker/grafana/dashboards/pipeline_observability.json (Phase 2):
Grafana dashboard JSON format reference -- 13 panels with Prometheus data source.

From docker-compose.yml (existing):
- PostgreSQL instances: postgres:5432, airflow-db:5433, marquez-db:5434, ranger-db:5435, om-db:5436
- Grafana: port 3001, provisioning from /etc/grafana/provisioning, dashboards from /var/lib/grafana/dashboards
- Marquez API: port 5000, lineage endpoint /api/v1-beta/lineage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit schema, aggregation module, and anomaly detection</name>
  <files>
    etl/src/governance/audit_schema.py,
    etl/src/governance/audit_aggregator.py,
    etl/src/governance/anomaly_detector.py,
    etl/dags/governance/__init__.py,
    etl/dags/governance/dag_audit_aggregation.py,
    etl/dags/governance/dag_anomaly_report.py,
    etl/dags/governance/dag_freshness_check.py,
    etl/tests/unit/test_audit_schema.py,
    etl/tests/unit/test_anomaly_detector.py,
    etl/tests/integration/test_audit_pipeline.py
  </files>
  <behavior>
    - Test: AuditRecord dataclass has all required fields (audit_id, timestamp, engine, user_name, query_id, query_text, tables_accessed, columns_accessed, rows_returned, bytes_scanned, masked_columns, access_granted, source_engine_audit_id)
    - Test: normalize_trino_audit converts Trino HTTP event listener payload to AuditRecord
    - Test: normalize_teradata_audit converts Teradata DBQL row to AuditRecord
    - Test: normalize_snowflake_audit converts Snowflake ACCESS_HISTORY row to AuditRecord
    - Test: AnomalyType enum has BULK_DOWNLOAD, AFTER_HOURS_ACCESS, UNUSUAL_RESTRICTED_ACCESS, HIGH_FREQUENCY_QUERY values
    - Test: detect_anomalies flags bulk download (>100k rows returned in single query)
    - Test: detect_anomalies flags after-hours access (queries between 22:00-06:00 local time)
    - Test: detect_anomalies flags unusual restricted table access (user not in expected group accessing restricted schema)
    - Test: AnomalyReport contains anomaly_type, severity, description, audit_records, detected_at
    - Integration test: audit aggregation DAG structure valid (skip if Airflow not running)
  </behavior>
  <action>
    1. Create etl/src/governance/audit_schema.py:
       - AuditRecord dataclass with fields per CONTEXT.md locked decision:
         - audit_id: str (UUID)
         - timestamp: datetime
         - engine: str ("trino" | "teradata" | "snowflake")
         - user_name: str
         - query_id: str
         - query_text: str
         - tables_accessed: list[dict] (each: {"schema": str, "table": str})
         - columns_accessed: list[dict] (each: {"schema": str, "table": str, "column": str})
         - rows_returned: int
         - bytes_scanned: int
         - masked_columns: list[dict] (columns where masking was applied)
         - access_granted: bool
         - source_engine_audit_id: str
       - AUDIT_SCHEMA: dict mapping field names to SQL types (for table creation DDL)
       - normalize_trino_audit(event_payload: dict) -> AuditRecord:
         - Parses Trino HTTP event listener JSON (queryId, query, user, queryCompletedEvent)
         - Extracts tables from queryCompletedEvent.metadata.tables
         - Extracts columns from queryCompletedEvent.columns
       - normalize_teradata_audit(dbql_row: dict) -> AuditRecord:
         - Parses Teradata DBC.QryLogV row (QueryID, UserName, QueryText, etc.)
         - Note: column-level access from DBC.DBQLObjTbl joined by QueryID
       - normalize_snowflake_audit(access_history_row: dict) -> AuditRecord:
         - Parses Snowflake ACCESS_HISTORY view columns
         - Note: column-level access from direct_objects_accessed JSONB
       - AuditRecord.to_dict() and AuditRecord.to_insert_values() for DB operations

    2. Create etl/src/governance/audit_aggregator.py:
       - TrinoAuditExtractor class:
         - __init__(audit_receiver_url): connects to audit receiver service
         - extract(since: datetime) -> list[AuditRecord]: fetches Trino audit events since timestamp
       - TeradataAuditExtractor class:
         - __init__(connection_params): Teradata DBQL connection (env var driven, graceful skip)
         - extract(since: datetime) -> list[AuditRecord]: queries DBC.QryLogV + DBC.DBQLObjTbl
       - SnowflakeAuditExtractor class:
         - __init__(connection_params): Snowflake connection (env var driven, graceful skip)
         - extract(since: datetime) -> list[AuditRecord]: queries SNOWFLAKE.ACCOUNT_USAGE.ACCESS_HISTORY
         - NOTE: ACCESS_HISTORY requires Enterprise edition (per pitfall #7). Fallback to QUERY_HISTORY.
       - aggregate_audit_records(records: list[AuditRecord], db_connection_string: str) -> int:
         - Inserts records into PostgreSQL audit table
         - Returns count of records inserted
         - Uses batch insert for efficiency

    3. Create etl/src/governance/anomaly_detector.py:
       - AnomalyType enum: BULK_DOWNLOAD, AFTER_HOURS_ACCESS, UNUSUAL_RESTRICTED_ACCESS, HIGH_FREQUENCY_QUERY
       - AnomalyReport dataclass: anomaly_type, severity ("low"|"medium"|"high"), description, audit_records (list[AuditRecord]), detected_at (datetime)
       - detect_anomalies(records: list[AuditRecord], business_hours: tuple = (6, 22)) -> list[AnomalyReport]:
         - BULK_DOWNLOAD: rows_returned > 100,000 in single query (severity: medium)
         - AFTER_HOURS_ACCESS: queries outside business_hours (severity: low, high if restricted tables)
         - UNUSUAL_RESTRICTED_ACCESS: user accessing sensitive_ns without being in data_admin group (severity: high)
         - HIGH_FREQUENCY_QUERY: same user > 1000 queries in 1 hour (severity: medium)
       - format_anomaly_report(anomalies: list[AnomalyReport]) -> str: markdown-formatted report
       - All heuristics configurable via parameters with sensible defaults

    4. Create etl/dags/governance/__init__.py: empty package init

    5. Create etl/dags/governance/dag_audit_aggregation.py:
       - Airflow DAG: governance_audit_aggregation
       - Schedule: daily at 02:00 UTC
       - default_args: retries=3, retry_delay=timedelta(minutes=5), retry_exponential_backoff=True
       - Tasks:
         - extract_trino_audit: PythonOperator calling TrinoAuditExtractor.extract()
         - extract_teradata_audit: PythonOperator (skip if TERADATA_HOST not set)
         - extract_snowflake_audit: PythonOperator (skip if SNOWFLAKE_ACCOUNT not set)
         - aggregate_records: PythonOperator calling aggregate_audit_records()
       - Dependencies: extract_* >> aggregate_records

    6. Create etl/dags/governance/dag_anomaly_report.py:
       - Airflow DAG: governance_anomaly_report
       - Schedule: daily at 06:00 UTC (after audit aggregation)
       - Tasks:
         - fetch_yesterday_audit: PythonOperator fetching audit records for previous day
         - run_anomaly_detection: PythonOperator calling detect_anomalies()
         - generate_report: PythonOperator calling format_anomaly_report(), saves to /tmp/anomaly_report_{date}.md
       - Dependencies: sequential

    7. Create etl/dags/governance/dag_freshness_check.py:
       - Airflow DAG: governance_freshness_check
       - Schedule: every 2 hours
       - Tasks:
         - check_freshness: PythonOperator calling get_all_freshness() from freshness_tracker module
         - update_metrics: PythonOperator that writes freshness status to a metrics table for Grafana
       - Dependencies: check_freshness >> update_metrics

    8. Create etl/tests/unit/test_audit_schema.py:
       - Test AuditRecord creation with all fields
       - Test to_dict() produces complete dictionary
       - Test normalize_trino_audit with sample Trino event payload
       - Test normalize_teradata_audit with sample DBQL row
       - Test normalize_snowflake_audit with sample ACCESS_HISTORY row
       - Test AUDIT_SCHEMA has all field definitions

    9. Create etl/tests/unit/test_anomaly_detector.py:
       - Test AnomalyType enum values
       - Test detect_anomalies with bulk download scenario (>100k rows)
       - Test detect_anomalies with after-hours scenario
       - Test detect_anomalies with restricted table access scenario
       - Test detect_anomalies with high-frequency scenario
       - Test detect_anomalies returns empty list for normal activity
       - Test format_anomaly_report produces markdown output

    10. Create etl/tests/integration/test_audit_pipeline.py:
        - @pytest.mark.integration marker
        - Skip if PostgreSQL audit table not available
        - Test: audit aggregation writes records to PostgreSQL
        - Test: records queryable by engine, user, date range
        - Test: DAG structure validation (imports, task count, dependencies)

    11. Create etl/tests/integration/test_compliance_lineage.py:
        - @pytest.mark.integration marker
        - Skip if Marquez not running
        - Test: Marquez lineage API returns graph for lakehouse datasets
        - Test: lineage graph has nodes (datasets + jobs) and edges
        - Test: legacy lineage stubs appear in lineage graph after registration
  </action>
  <verify>
    <automated>cd etl && python -m pytest tests/unit/test_audit_schema.py tests/unit/test_anomaly_detector.py -x -q</automated>
  </verify>
  <done>Audit schema with cross-engine normalization (Trino, Teradata, Snowflake). Audit aggregation module with engine-specific extractors. Anomaly detector with 4 heuristics (bulk download, after-hours, restricted access, high-frequency). 3 governance DAGs for audit, anomaly, and freshness. Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Grafana compliance dashboards and Marquez Infinity datasource</name>
  <files>
    infra/docker/grafana/dashboards/bcbs239_compliance.json,
    infra/docker/grafana/dashboards/data_freshness.json,
    infra/docker/grafana/dashboards/audit_overview.json,
    infra/docker/grafana/provisioning/datasources.yml
  </files>
  <action>
    1. Update infra/docker/grafana/provisioning/datasources.yml:
       - Keep existing Prometheus datasource (isDefault: true)
       - Add Marquez-API datasource:
         - name: Marquez-API
         - type: yesoreyeram-infinity-datasource
         - access: proxy
         - url: http://marquez:5000
         - jsonData: auth_method empty
       - Add PostgreSQL datasource for audit queries:
         - name: Audit-DB
         - type: postgres
         - url: marquez-db:5432 (reuse marquez-db for audit storage initially, or use a dedicated audit schema)
         - database: marquez
         - user: marquez
         - secureJsonData: password: marquez
         - jsonData: sslmode: disable, postgresVersion: 1500

    2. Create infra/docker/grafana/dashboards/bcbs239_compliance.json:
       Grafana dashboard JSON with these panels (follow existing pipeline_observability.json structure):
       - Title: "BCBS 239 Compliance - Regulatory Lineage"
       - Row 1: "Accuracy" section
         - Panel: "Data Quality Scores by Dataset" -- Table panel querying Prometheus for Soda quality metrics per dataset (null rate, uniqueness, range checks). Shows pass/fail/warn per quality check.
         - Panel: "Quality Score Trend" -- Time series showing quality score evolution over last 30 days
       - Row 2: "Completeness" section
         - Panel: "Lineage Coverage" -- Stat panel showing percentage of gold datasets with complete lineage chains (query Marquez API via Infinity: count datasets with upstream lineage / total gold datasets)
         - Panel: "Missing Lineage Sources" -- Table listing gold datasets without upstream lineage (Infinity query to Marquez API)
       - Row 3: "Timeliness" section
         - Panel: "Pipeline SLA Compliance" -- Gauge showing overall SLA compliance rate (from Prometheus airflow metrics)
         - Panel: "Late Pipeline Runs" -- Table listing pipelines that missed SLA in last 7 days
       - Row 4: "Lineage Explorer"
         - Panel: "Dataset Lineage Graph" -- Variable-driven panel: user selects a dataset from dropdown, Infinity queries Marquez /api/v1-beta/lineage?nodeId=dataset:lakehouse:{variable}, displays node table with upstream/downstream
       - Deep-link annotation: each dataset name links to OpenMetadata catalog page (http://localhost:8585/table/lakehouse-trino.iceberg.{schema}.{table}) per CONTEXT.md deep-link integration decision
       - Use uid "bcbs239-compliance" for cross-dashboard linking

    3. Create infra/docker/grafana/dashboards/data_freshness.json:
       Grafana dashboard JSON:
       - Title: "Data Freshness SLA Monitoring"
       - Panel: "Freshness Overview" -- Stat panel with overall green/yellow/red counts
       - Panel: "Table Freshness Status" -- Table panel showing each table's freshness status, last_updated, hours_since_update, SLA target. Color-coded: green/yellow/red rows.
       - Panel: "Freshness Timeline" -- Time series showing freshness trends (hours since update) for key tables over 7 days
       - Panel: "SLA Breaches" -- Table listing tables currently in YELLOW or RED status with breach duration
       - Data source: Audit-DB (PostgreSQL) querying freshness metrics table populated by dag_freshness_check
       - Deep-link: table names link to OpenMetadata catalog page

    4. Create infra/docker/grafana/dashboards/audit_overview.json:
       Grafana dashboard JSON:
       - Title: "Audit Trail Overview"
       - Panel: "Query Volume by Engine" -- Time series showing query counts per engine (Trino, Teradata, Snowflake) over time
       - Panel: "Top Users by Query Count" -- Bar chart showing top 10 users by query volume
       - Panel: "Restricted Data Access" -- Table showing accesses to sensitive_ns schema with user, timestamp, query
       - Panel: "Masked Column Access Log" -- Table showing queries where masking was applied (masked_columns not empty)
       - Panel: "Daily Anomaly Summary" -- Stat panel showing anomaly counts by type (from anomaly report output)
       - Panel: "Access Denied Events" -- Table showing access_granted=false events with user and query
       - Data source: Audit-DB (PostgreSQL)
       - Use uid "audit-overview" for cross-dashboard linking

    5. Install Grafana Infinity plugin:
       - Add GF_INSTALL_PLUGINS=yesoreyeram-infinity-datasource to Grafana container environment in docker-compose.yml
       - This enables the Marquez API data source for compliance dashboards

    6. Add grafana-reporter service to docker-compose.yml for PDF/HTML export:
       - Image: izakmarais/grafana-reporter:latest
       - Environment: GRAFANA_URL=http://grafana:3000
       - Create infra/docker/grafana-reporter/grafana-reporter.env with config
       - Expose on port 8686 for on-demand report generation
       - Add cron-like scheduled export (daily at 09:00 UTC) via Airflow DAG task in dag_anomaly_report.py:
         - New task: export_compliance_pdf that calls grafana-reporter API to export bcbs239_compliance dashboard to PDF
         - Output: /opt/airflow/reports/bcbs239_{date}.pdf for audit evidence archive

    7. Create etl/src/governance/audit_archiver.py:
       - archive_old_records(db_connection_string: str, s3_bucket: str, archive_after_days: int = 90) -> int:
         - Queries audit records older than archive_after_days from PostgreSQL
         - Writes to S3 as Parquet files partitioned by year/month
         - Deletes archived records from PostgreSQL
         - Returns count of archived records
       - S3 lifecycle policy documented in module docstring: configure S3-IA after 3 years, Glacier after 7 years
       - Add archive task to dag_audit_aggregation.py (runs after aggregate_records)
  </action>
  <verify>
    <automated>cd etl && python -m pytest tests/unit/test_dashboard_config.py -x -q 2>/dev/null; python -c "
import json, os
for f in ['bcbs239_compliance.json', 'data_freshness.json', 'audit_overview.json']:
    path = f'infra/docker/grafana/dashboards/{f}'
    d = json.load(open(path))
    assert 'panels' in d, f'{f} missing panels'
    assert len(d['panels']) >= 3, f'{f} has fewer than 3 panels'
    print(f'{f}: {len(d[\"panels\"])} panels - OK')
import yaml
ds = yaml.safe_load(open('infra/docker/grafana/provisioning/datasources.yml'))
names = [d['name'] for d in ds['datasources']]
assert 'Marquez-API' in names, 'Marquez-API datasource missing'
assert 'Audit-DB' in names, 'Audit-DB datasource missing'
print('Datasources: OK')
"</automated>
  </verify>
  <done>Three Grafana dashboards auto-provisioned: BCBS 239 compliance (lineage + quality overlay for accuracy/completeness/timeliness), data freshness SLA (traffic-light per table), audit trail overview (query volume, restricted access, anomalies). Grafana Infinity plugin enabled for Marquez API queries. Deep-links to OpenMetadata catalog.</done>
</task>

</tasks>

<verification>
- Unit tests: `cd etl && python -m pytest tests/unit/test_audit_schema.py tests/unit/test_anomaly_detector.py -x -q`
- Dashboard JSON validity: `python -c "import json; [json.load(open(f'infra/docker/grafana/dashboards/{f}')) for f in ['bcbs239_compliance.json','data_freshness.json','audit_overview.json']]"`
- Datasources config: `python -c "import yaml; d=yaml.safe_load(open('infra/docker/grafana/provisioning/datasources.yml')); print([s['name'] for s in d['datasources']])"`
- DAG files exist: `ls etl/dags/governance/dag_audit_aggregation.py etl/dags/governance/dag_anomaly_report.py etl/dags/governance/dag_freshness_check.py`
- Full unit suite: `cd etl && python -m pytest tests/unit -x -q`
</verification>

<success_criteria>
- Audit schema normalizes events from Trino, Teradata, and Snowflake into common format
- Anomaly detector flags 4 patterns: bulk download, after-hours, restricted access, high-frequency
- 3 governance DAGs: audit aggregation (daily), anomaly report (daily), freshness check (2-hourly)
- BCBS 239 dashboard shows accuracy (quality scores), completeness (lineage coverage), timeliness (SLA compliance)
- Data freshness dashboard shows per-table traffic-light status
- Audit overview dashboard shows query volume, restricted access, masked columns, anomalies
- Grafana Infinity plugin configured for Marquez API queries
- Deep-links between Grafana dashboards and OpenMetadata catalog
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-governance-security-hardening-and-platform/03-03-SUMMARY.md`
</output>
