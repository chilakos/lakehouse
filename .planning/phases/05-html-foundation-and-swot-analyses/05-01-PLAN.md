---
phase: 05-html-foundation-and-swot-analyses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/templates/base_swot.html
  - docs/templates/base_index.html
  - docs/templates/macros/swot_grid.html
  - docs/templates/macros/decision_matrix.html
  - docs/templates/macros/collapsible.html
  - docs/render_html.py
  - docs/swot/data/nessie-catalog.yml
  - docs/swot/nessie-catalog-swot.html
  - etl/tests/test_html_render.py
autonomous: true
requirements:
  - SWOT-01
  - SWOT-02
  - SWOT-09
  - SWOT-10
  - ARCH-09

must_haves:
  truths:
    - "Opening nessie-catalog-swot.html from the filesystem (file:// protocol) renders a professional document with navy/gold branding, embedded CSS, and readable typography"
    - "The Nessie SWOT HTML contains a 2x2 color-coded grid (green/yellow/blue/red), executive summary, decision matrix, and mitigations for every threat"
    - "Collapsible details/summary sections expand and collapse without JavaScript"
    - "The version-stamped footer shows generation date and platform component versions extracted from docker-compose.yml"
    - "All render tests pass, validating HTML structure, CSS embedding, responsive meta tags, and footer content"
  artifacts:
    - path: "docs/templates/base_swot.html"
      provides: "Jinja2 base template with embedded CSS, header, content block, and footer"
      contains: "{% block content %}"
    - path: "docs/render_html.py"
      provides: "Python render script that loads YAML data, extracts versions, and produces standalone HTML"
      exports: ["render_swots", "extract_versions"]
    - path: "docs/swot/data/nessie-catalog.yml"
      provides: "YAML data file for Nessie SWOT with all sections, evidence fields, and decision matrix"
      contains: "executive_summary"
    - path: "docs/swot/nessie-catalog-swot.html"
      provides: "Rendered standalone HTML for Nessie Iceberg Catalog SWOT analysis"
      contains: "<!DOCTYPE html>"
    - path: "etl/tests/test_html_render.py"
      provides: "Pytest test suite validating rendered HTML structure for all requirements"
      min_lines: 80
  key_links:
    - from: "docs/render_html.py"
      to: "docs/templates/base_swot.html"
      via: "Jinja2 FileSystemLoader"
      pattern: "FileSystemLoader.*templates"
    - from: "docs/render_html.py"
      to: "docker-compose.yml"
      via: "extract_versions function"
      pattern: "extract_versions|docker-compose"
    - from: "docs/templates/base_swot.html"
      to: "docs/templates/macros/"
      via: "Jinja2 import"
      pattern: "import.*macros"
    - from: "etl/tests/test_html_render.py"
      to: "docs/render_html.py"
      via: "import render functions"
      pattern: "from docs.render_html|render_swots"
---

<objective>
Build the shared CSS template infrastructure, Jinja2 render pipeline, version-stamped footer, and the first SWOT (Nessie Catalog) as a validated proof that the entire system works end-to-end.

Purpose: Establishes the reusable template foundation that all 6 SWOTs in Plan 02 and all downstream HTML deliverables (Phases 6-8) will inherit. The Nessie SWOT serves as the validation artifact -- if it renders correctly with all required elements, the infrastructure is proven.

Output: Working Jinja2 template system, render script, Nessie SWOT as standalone HTML, and test scaffold covering all 11 phase requirements.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-html-foundation-and-swot-analyses/05-CONTEXT.md
@.planning/phases/05-html-foundation-and-swot-analyses/05-RESEARCH.md
@docs/swot/nessie-catalog-swot.md
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Jinja2 base SWOT template, macros, CSS, and render script</name>
  <files>
    docs/templates/base_swot.html
    docs/templates/base_index.html
    docs/templates/macros/swot_grid.html
    docs/templates/macros/decision_matrix.html
    docs/templates/macros/collapsible.html
    docs/render_html.py
    etl/tests/test_html_render.py
  </files>
  <behavior>
    - Test: rendered HTML contains embedded style block (no external link tags)
    - Test: rendered HTML contains meta viewport tag for responsive design
    - Test: rendered HTML contains details and summary elements (collapsible sections)
    - Test: rendered HTML contains @media print rules with details-content expansion
    - Test: rendered HTML contains @media (max-width: 768px) responsive rules
    - Test: rendered HTML footer contains generation date string
    - Test: rendered HTML footer contains version strings extracted from docker-compose.yml (nessie, trino, cube at minimum)
    - Test: rendered HTML header has navy background (#1a2332) and gold accent (#c8a961) in CSS
    - Test: rendered HTML uses system font stack (contains "Segoe UI" in CSS)
    - Test: extract_versions() returns dict with expected service keys from docker-compose.yml
    - Test: render_swots() produces .html file from .yml data file
  </behavior>
  <action>
    Create the complete Jinja2 template infrastructure:

    1. `docs/templates/base_swot.html` -- Jinja2 base template containing:
       - Full embedded CSS in a `<style>` block (NO external stylesheet links). Use the CSS from 05-RESEARCH.md as the foundation.
       - Corporate navy (#1a2332) header with gold (#c8a961) accent for title. Plain document header: title, date, status badge only (McKinsey/Gartner report feel).
       - System font stack: `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif`
       - Status badges: `.status-decided` (green #22c55e) and `.status-undecided` (amber #f59e0b)
       - CSS-only collapsible `<details>/<summary>` styling with triangle rotation animation
       - `@media print` rules: expand all details via `details::details-content { display: block !important; }`, white background, 10pt font, 2cm page margins
       - `@media (max-width: 768px)` responsive rules: single-column SWOT grid, reduced padding, auto-overflow tables
       - Fluid typography: `font-size: clamp(0.95rem, 1.5vw, 1.1rem)`
       - Table styling: navy header, alternating row backgrounds, overflow-x wrapper
       - Version-stamped footer block: generation date, next review date, platform component versions in flex-wrap layout
       - Jinja2 blocks: `{% block styles %}`, `{% block header %}`, `{% block content %}`, `{% block footer %}`
       - Import macros for SWOT grid, decision matrix, and collapsible wrappers

    2. `docs/templates/macros/swot_grid.html` -- Macro rendering the 2x2 SWOT grid:
       - CSS Grid: `grid-template-columns: 1fr 1fr` (stacks to `1fr` on mobile)
       - Color-coded quadrants: green (Strengths), yellow (Weaknesses), blue (Opportunities), red (Threats)
       - Each quadrant lists items with ID, title, and brief description
       - Accepts strengths, weaknesses, opportunities, threats lists from YAML data

    3. `docs/templates/macros/decision_matrix.html` -- Macro rendering comparison tables:
       - Accepts criteria list and options dict
       - Renders as responsive table with overflow-x wrapper
       - Navy header row, alternating body rows

    4. `docs/templates/macros/collapsible.html` -- Macro wrapping content in `<details>/<summary>`:
       - Accepts title, content, and optional `open` boolean (default: false)
       - Executive Summary and Recommendation sections default to `open`

    5. `docs/templates/base_index.html` -- Jinja2 base template for index/dashboard pages:
       - Same CSS foundation as base_swot.html (via shared style block or Jinja2 include)
       - Card layout grid for listing documents with status badges
       - Inherits the same footer block with version stamps

    6. `docs/render_html.py` -- Python render script:
       - `extract_versions(compose_path="docker-compose.yml") -> dict`: Parse docker-compose.yml, extract image tags for key services (nessie, trino, cube-api, ranger-admin, marquez, openmetadata-server, postgres, elasticsearch, grafana, prometheus). Return dict mapping service name to version string.
       - `render_swots(data_dir, template_dir, output_dir) -> list[Path]`: Load each YAML file from `docs/swot/data/`, render via `base_swot.html`, write to `docs/swot/{name}-swot.html`. Return list of rendered file paths.
       - `render_index(swot_data_files, template_dir, output_dir) -> Path`: Render the cross-SWOT index page from all YAML data files.
       - `if __name__ == "__main__"`: CLI entry point that renders all SWOTs and index.
       - Make the module importable (not just CLI) so tests can call render functions directly.
       - Use `pathlib.Path` throughout. Use `PROJECT_ROOT = Path(__file__).resolve().parent.parent` for paths relative to repo root.

    7. `etl/tests/test_html_render.py` -- Pytest test scaffold:
       - Import render functions from `docs/render_html` (add `docs/` to sys.path if needed, or use relative import strategy).
       - Test `extract_versions()` returns dict with keys for nessie, trino, cube-api.
       - Test `render_swots()` produces valid HTML from a sample YAML data file.
       - Test rendered HTML contains `<style>` block (SWOT-01: embedded CSS).
       - Test rendered HTML does NOT contain `<link rel="stylesheet"` (no external CSS).
       - Test rendered HTML contains `<meta name="viewport"` (SWOT-10: responsive).
       - Test rendered HTML contains `<details` and `<summary` elements (SWOT-09: collapsible).
       - Test rendered HTML `@media print` section contains `details-content` (SWOT-09: print expansion).
       - Test rendered HTML contains `@media (max-width: 768px)` or similar tablet breakpoint (SWOT-10).
       - Test rendered HTML footer contains generation date (ARCH-09).
       - Test rendered HTML footer contains at least 3 version strings (ARCH-09).
       - Test rendered HTML CSS contains `#1a2332` (navy) and `#c8a961` (gold).
       - Test rendered HTML CSS contains system font stack substring.
       - Use a minimal fixture YAML data file for test rendering (create in test setup or as a conftest fixture).
       - Mark all tests with `@pytest.mark.unit`.

    IMPORTANT: Do NOT use JavaScript anywhere. All interactivity is CSS-only via `<details>/<summary>`.
    IMPORTANT: All CSS must be embedded in `<style>` tags. No `<link>` stylesheet references.
    IMPORTANT: The render script must be importable as a module (for tests) AND runnable as CLI.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python -m pytest etl/tests/test_html_render.py -x -v --timeout=30</automated>
  </verify>
  <done>
    Base SWOT template renders valid standalone HTML with embedded navy/gold CSS, collapsible sections, responsive tablet layout, print rules, and version-stamped footer. All template tests pass. Render script extracts versions from docker-compose.yml and produces HTML from YAML data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Nessie Catalog SWOT YAML data and render first HTML deliverable</name>
  <files>
    docs/swot/data/nessie-catalog.yml
    docs/swot/nessie-catalog-swot.html
  </files>
  <action>
    Convert the existing `docs/swot/nessie-catalog-swot.md` (176 lines) into a structured YAML data file and render it through the Jinja2 template system.

    1. `docs/swot/data/nessie-catalog.yml` -- Structured YAML with:
       - Metadata: title ("Iceberg Catalog SWOT Analysis"), subtitle ("Glue vs Nessie vs HMS vs Polaris"), status ("decided"), decision ("Nessie"), prepared_for ("Leadership Review"), date ("2026-03-13"), phase ("Phase 1 - Foundation and Feasibility Validation"), next_review ("2026-Q2")
       - `executive_summary`: Convert the existing executive summary text. Include the clear recommendation for Nessie.
       - `recommendation`: The recommendation paragraph with numbered rationale points.
       - `strengths`: List of 6 items (S1-S6) each with `id`, `title`, `description` (multi-line YAML string), and `evidence` field with specific data points.
         - S1: Open-Source with No License Cost (evidence: "Apache 2.0 license; $0 license cost vs commercial alternatives")
         - S2: REST Catalog Specification Compliance (evidence: "Implements Apache Iceberg REST catalog spec")
         - S3: Git-Like Branching for Schema Changes (evidence: "Zero-downtime migrations, reproducible queries, rollback")
         - S4: Multi-Engine Support (evidence: "Phase 1 validated: PySpark + Trino cross-engine reads/writes")
         - S5: PostgreSQL Backing Store (evidence: "ACID transactions, familiar ops model")
         - S6: Single Catalog Serving Multiple Storage Backends (evidence: "One Nessie instance serves S3 + MinIO")
       - `weaknesses`: List of 5 items (W1-W5) with same structure.
         - W1: Smaller Community Than Hive Metastore
         - W2: No Native High Availability Without External Orchestration
         - W3: Limited Vendor Support
         - W4: Teradata OTF Does Not Natively Support REST Catalog
         - W5: Learning Curve for Branching Semantics
       - `opportunities`: List of 5 items (O1-O5).
         - O1: REST Catalog Spec Becoming Industry Standard
         - O2: Zero-Downtime Schema Migrations via Branching
         - O3: Snowflake ICEBERG_REST Integration
         - O4: Growing Ecosystem Adoption
         - O5: Regulatory Compliance via Catalog Versioning
       - `threats`: List of 5 items (T1-T5), each with an additional `mitigation` field.
         - T1: Apache Polaris Gaining Momentum
         - T2: Vendor Lock-In Risk if Migrating to Managed Catalog
         - T3: REST Catalog Specification Still Evolving
         - T4: Nessie Project Governance Changes
         - T5: Scale Limitations Under Extreme Load
       - `decision_matrix`: dict with `criteria` list (License cost, REST catalog spec, Multi-engine support, Branching, Teradata OTF, Snowflake integration, Cloud-agnostic, HA complexity, Community size) and `options` dict mapping each option (Nessie, Polaris, AWS Glue, Hive Metastore) to their ratings.

       Preserve ALL content from the existing markdown. Enhance evidence fields with specific numbers where the markdown has them.

    2. Run `python docs/render_html.py` to generate `docs/swot/nessie-catalog-swot.html`.

    3. Verify the rendered HTML:
       - Open file size is reasonable (50-150 KB range for a full SWOT)
       - Contains all 6 strengths, 5 weaknesses, 5 opportunities, 5 threats
       - Decision matrix table renders with all 4 options and 9 criteria
       - Footer shows versions from docker-compose.yml
       - No broken Jinja2 template tags in output (no `{{` or `{%` in rendered HTML)

    4. Run the full test suite to confirm the Nessie SWOT passes all structural tests.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python docs/render_html.py && python -m pytest etl/tests/test_html_render.py -x -v --timeout=30 && python -c "html=open('docs/swot/nessie-catalog-swot.html').read(); assert '{{' not in html and '{%' not in html, 'Unrendered template tags'; assert html.count('swot-quadrant') == 4, 'Missing SWOT quadrants'; print('Nessie SWOT HTML validated')"</automated>
  </verify>
  <done>
    Nessie Catalog SWOT renders as polished standalone HTML matching the existing markdown content. All 21 SWOT items (S1-S6, W1-W5, O1-O5, T1-T5) present with evidence. Decision matrix shows 4 alternatives across 9 criteria. Footer shows live versions. All tests pass. The template infrastructure is proven end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `python docs/render_html.py` runs without errors and produces `docs/swot/nessie-catalog-swot.html`
2. `python -m pytest etl/tests/test_html_render.py -x -v --timeout=30` -- all tests pass
3. Opening `docs/swot/nessie-catalog-swot.html` in a browser from file:// protocol shows a professional document with navy/gold branding
4. No `<link rel="stylesheet">` or `<script>` tags in rendered HTML
5. `<details>` sections expand/collapse on click
6. Footer shows generation date and platform versions matching docker-compose.yml
</verification>

<success_criteria>
- Jinja2 base template renders standalone HTML with all CSS embedded
- Navy/gold corporate branding, system font stack, color-coded SWOT quadrants
- CSS-only collapsible sections using details/summary with print expansion
- Responsive design with tablet breakpoint (768px) and fluid typography
- Version-stamped footer auto-populated from docker-compose.yml
- Nessie Catalog SWOT fully rendered with all 21 items, decision matrix, and recommendation
- Render script is both CLI-runnable and importable for testing
- All pytest tests pass validating HTML structure for SWOT-01, SWOT-02, SWOT-09, SWOT-10, ARCH-09
</success_criteria>

<output>
After completion, create `.planning/phases/05-html-foundation-and-swot-analyses/05-01-SUMMARY.md`
</output>
