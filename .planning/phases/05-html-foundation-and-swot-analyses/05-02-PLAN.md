---
phase: 05-html-foundation-and-swot-analyses
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - docs/swot/data/snowflake-strategy.yml
  - docs/swot/data/data-model-strategy.yml
  - docs/swot/data/datastage-migration.yml
  - docs/swot/data/bi-semantic-layer.yml
  - docs/swot/data/ai-semantic-layer.yml
  - docs/swot/snowflake-strategy-swot.html
  - docs/swot/data-model-strategy-swot.html
  - docs/swot/datastage-migration-swot.html
  - docs/swot/bi-semantic-layer-swot.html
  - docs/swot/ai-semantic-layer-swot.html
  - docs/swot/index.html
autonomous: true
requirements:
  - SWOT-03
  - SWOT-04
  - SWOT-05
  - SWOT-06
  - SWOT-07
  - SWOT-08

must_haves:
  truths:
    - "The Snowflake Strategy SWOT presents 3 options (Retire/Keep as Iceberg compute/Maintain) with a clear recommendation backed by evidence, enabling leadership to make a decision"
    - "The Data Model Strategy SWOT presents 3 options (Keep FSDM/Evolve/New medallion) with backward compatibility analysis and a clear recommendation, enabling leadership to make a decision"
    - "The 3 decided SWOTs (DataStage, BI Semantic, AI Semantic) show full competitive analysis of each rejected alternative with evidence from the codebase"
    - "Every SWOT HTML has a 2x2 grid, executive summary, decision matrix, and mitigations for every threat"
    - "The cross-SWOT index page shows all 6 analyses with Decided (green) and Undecided (amber) status badges and links to each standalone file"
    - "All 7 HTML files (6 SWOTs + index) open correctly from file:// protocol with consistent branding"
  artifacts:
    - path: "docs/swot/data/snowflake-strategy.yml"
      provides: "Snowflake Strategy SWOT content with 3 strategic options, TCO analysis, and recommendation"
      contains: "executive_summary"
    - path: "docs/swot/data/data-model-strategy.yml"
      provides: "Data Model Strategy SWOT content with FSDM analysis, medallion evolution, and recommendation"
      contains: "executive_summary"
    - path: "docs/swot/data/datastage-migration.yml"
      provides: "DataStage Migration SWOT content with evidence from ETL framework"
      contains: "executive_summary"
    - path: "docs/swot/data/bi-semantic-layer.yml"
      provides: "BI Semantic Layer SWOT content with Cube evidence"
      contains: "executive_summary"
    - path: "docs/swot/data/ai-semantic-layer.yml"
      provides: "AI Semantic Layer SWOT content with NL-to-SQL evidence"
      contains: "executive_summary"
    - path: "docs/swot/index.html"
      provides: "Cross-SWOT index page with dashboard card layout and status badges"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "docs/swot/index.html"
      to: "docs/swot/*-swot.html"
      via: "relative href links"
      pattern: "href=.*-swot\\.html"
    - from: "docs/swot/data/datastage-migration.yml"
      to: "docs/etl-patterns.md"
      via: "evidence citations"
      pattern: "etl-patterns|BasePipeline"
    - from: "docs/swot/data/bi-semantic-layer.yml"
      to: "semantic/model/cubes/"
      via: "evidence citations"
      pattern: "Cube.*YAML|trading_metrics|risk_exposure"
    - from: "docs/swot/data/ai-semantic-layer.yml"
      to: "etl/src/semantic/nl_to_sql.py"
      via: "evidence citations"
      pattern: "NLToSQLEngine|Claude.*Bedrock"
---

<objective>
Create all 5 remaining SWOT YAML data files and render all 6 SWOTs plus the cross-SWOT index page as polished standalone HTML deliverables.

Purpose: Delivers the executive-grade strategic analysis documents that leadership needs to validate decided strategies and make pending decisions on Snowflake and Data Model. The 2 undecided SWOTs (Snowflake Strategy, Data Model Strategy) are the highest-value deliverables -- they unblock active leadership decisions.

Output: 5 new SWOT YAML data files, 5 new SWOT HTML pages, 1 cross-SWOT index page (7 total HTML files when including Nessie from Plan 01).
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-html-foundation-and-swot-analyses/05-CONTEXT.md
@.planning/phases/05-html-foundation-and-swot-analyses/05-RESEARCH.md
@.planning/phases/05-html-foundation-and-swot-analyses/05-01-SUMMARY.md
@docs/swot/nessie-catalog-swot.md
@docs/swot/data/nessie-catalog.yml
@docs/etl-patterns.md
@docs/adr/001-teradata-otf-nessie-feasibility.md
@docker-compose.yml
@semantic/model/cubes/trading_metrics.yml
@semantic/model/cubes/risk_exposure.yml
@etl/src/semantic/nl_to_sql.py
@etl/src/semantic/metric_context.py

<interfaces>
<!-- From Plan 05-01: Template and render infrastructure the executor will use -->

docs/render_html.py exports:
  - extract_versions(compose_path: str = "docker-compose.yml") -> dict
  - render_swots(data_dir, template_dir, output_dir) -> list[Path]
  - render_index(swot_data_files, template_dir, output_dir) -> Path

docs/swot/data/ YAML schema (established by nessie-catalog.yml):
  - title: str
  - subtitle: str
  - status: "decided" | "undecided"
  - decision: str | null
  - prepared_for: str
  - date: str
  - phase: str
  - next_review: str
  - executive_summary: str (multi-line)
  - recommendation: str (multi-line)
  - strengths: list[{id, title, description, evidence}]
  - weaknesses: list[{id, title, description, evidence}]
  - opportunities: list[{id, title, description, evidence}]
  - threats: list[{id, title, description, evidence, mitigation}]
  - decision_matrix: {criteria: list[str], options: dict[str, list[str]]}

docs/templates/base_swot.html:
  - Jinja2 base template with blocks: styles, header, content, footer
  - Macros available: swot_grid, decision_matrix, collapsible

docs/templates/base_index.html:
  - Jinja2 base template for index/dashboard pages
  - Card layout with status badges
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create undecided SWOT YAML data files (Snowflake Strategy and Data Model Strategy)</name>
  <files>
    docs/swot/data/snowflake-strategy.yml
    docs/swot/data/data-model-strategy.yml
    docs/swot/snowflake-strategy-swot.html
    docs/swot/data-model-strategy-swot.html
  </files>
  <action>
    Create the 2 highest-priority SWOT analyses -- the undecided ones that unblock leadership decisions. Both must present balanced analysis WITH a clear recommendation backed by evidence.

    **SWOT-03: Snowflake Strategy** (`docs/swot/data/snowflake-strategy.yml`):
    - title: "Snowflake Strategy SWOT Analysis"
    - subtitle: "Retire vs Keep as Iceberg Compute vs Maintain Current State"
    - status: "undecided"
    - decision: null
    - phase: "Lakehouse Architecture Transformation"
    - Three strategic options to evaluate:
      1. **Retire Snowflake** -- Route all analytics through Trino/Teradata; eliminate Snowflake entirely
      2. **Keep as Iceberg Compute-Only** -- Snowflake reads Iceberg tables via ICEBERG_REST catalog (Nessie); no data copies; compute-only role
      3. **Maintain Current State** -- Continue Snowflake with data copies from Teradata/Cloudera
    - Strengths (4-5 items): e.g., Snowflake ICEBERG_REST support enables zero-copy access (evidence: "Snowflake supports ICEBERG_REST catalog type connecting to Nessie; validated in Snowflake documentation"), existing team Snowflake skills, strong BI tooling ecosystem, consumption-based pricing flexibility for variable workloads
    - Weaknesses (4-5 items): e.g., consumption pricing is expensive at scale (evidence: "Snowflake credits cost $2-4/credit on-demand, $1.50-2.50 with annual commitments; compute is ~80% of bill"), data copy maintenance overhead in current state, vendor lock-in on proprietary features, duplicate governance layer needed
    - Opportunities (3-4 items): e.g., Trino-based lakehouse achieving up to 55% lower TCO over 3 years (evidence: "Starburst benchmark data -- note vendor bias; actual savings depend on workload profile"), consolidation to single governance/lineage stack, ICEBERG_REST as a bridge during transition
    - Threats (3-4 items with mitigations): e.g., Snowflake contract lock-in (mitigation: "Negotiate compute-only contract terms; ICEBERG_REST enables gradual migration without cliff"), loss of Snowflake-specific features (Snowpipe, Time Travel for Snowflake-native tables), team retraining costs
    - Decision matrix: Compare all 3 options across criteria (Annual cost, Data duplication, Governance complexity, Team disruption, BI tool compatibility, Migration risk, Long-term flexibility)
    - Recommendation: "We recommend Option 2: Keep Snowflake as Iceberg Compute-Only. This preserves existing team skills and BI tool connections while eliminating data copies and reducing costs. The ICEBERG_REST integration with Nessie enables zero-copy access to lakehouse data. Leadership should verify: (a) current Snowflake contract allows compute-only usage, (b) team BI workloads can tolerate Iceberg table query performance through Snowflake."

    **SWOT-05: Data Model Strategy** (`docs/swot/data/data-model-strategy.yml`):
    - title: "Data Model Strategy SWOT Analysis"
    - subtitle: "Keep FSDM vs Evolve Incrementally vs New Medallion-Native Model"
    - status: "undecided"
    - decision: null
    - phase: "Lakehouse Architecture Transformation"
    - Three strategic options to evaluate:
      1. **Keep FSDM As-Is** -- Maintain Teradata FSDM entity naming and structure in the lakehouse
      2. **Evolve FSDM Incrementally** -- Preserve FSDM entity names in Bronze/Silver layers; introduce medallion conventions in Gold layer; backward-compatible evolution
      3. **New Medallion-Native Model** -- Break from FSDM entirely; redesign around Bronze/Silver/Gold with domain-driven naming
    - Strengths (4-5 items): e.g., existing FSDM provides standardized financial services vocabulary (evidence: "FSDM covers Party, Account, Instrument, Transaction, Risk, Compliance domains -- industry-standard for banking"), Gold tables already use medallion naming (evidence: "gold.trading_metrics, gold.risk_exposure follow medallion conventions, not strict FSDM entity naming -- de facto evolution already happening"), 40+ engineers already know FSDM conventions
    - Weaknesses (4-5 items): e.g., FSDM is partially followed (evidence: "PROJECT.md notes 'partially aligned to FSDM' -- indicates drift"), FSDM is Teradata-proprietary model (risk if Teradata role diminishes), strict FSDM doesn't naturally express medallion quality tiers, mapping FSDM entities to Bronze/Silver/Gold requires additional metadata layer
    - Opportunities (3-4 items): e.g., incremental evolution preserves investment while modernizing (evidence: "Gold layer already evolved organically -- formalizing this approach has low disruption"), medallion layer enables clear data quality contracts per tier, business glossary can bridge FSDM terms to new physical names
    - Threats (3-4 items with mitigations): e.g., breaking change risk for 40+ engineer team (mitigation: "Incremental evolution preserves all existing names; new naming is additive, not replacing"), regulatory reporting queries tied to FSDM column names (mitigation: "Create views maintaining FSDM-compatible names for regulatory queries"), loss of FSDM vendor support if Teradata role reduces
    - Decision matrix: Compare all 3 options across criteria (Migration effort, Backward compatibility, Team disruption, Regulatory compliance, Long-term flexibility, Data quality enablement, Vendor independence)
    - Recommendation: "We recommend Option 2: Evolve FSDM Incrementally. The codebase already shows organic evolution (Gold tables use medallion naming while Bronze/Silver retain FSDM conventions). Formalizing this pattern preserves the 40+ engineer team's existing knowledge, maintains backward compatibility for regulatory reporting, and enables data quality contracts per medallion tier. Leadership should verify: (a) regulatory reporting queries can use compatibility views during transition, (b) business glossary mapping effort is acceptable."

    After creating both YAML files, render them:
    ```bash
    python docs/render_html.py
    ```

    Verify each rendered HTML:
    - Contains all SWOT items with evidence fields populated
    - Status badge shows "Undecided" (amber)
    - Decision matrix renders with all 3 options and all criteria
    - Recommendation section is explicit and actionable (not "it depends")
    - No unrendered Jinja2 tags in output
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python docs/render_html.py && python -c "
for f in ['docs/swot/snowflake-strategy-swot.html', 'docs/swot/data-model-strategy-swot.html']:
    html = open(f).read()
    assert '{{' not in html and '{%' not in html, f'{f}: unrendered template tags'
    assert 'undecided' in html.lower() or 'Undecided' in html, f'{f}: missing undecided status'
    assert html.count('swot-quadrant') == 4, f'{f}: missing SWOT quadrants'
    assert 'recommend' in html.lower(), f'{f}: missing recommendation'
    print(f'{f}: OK')
print('Both undecided SWOTs validated')
"</automated>
  </verify>
  <done>
    Snowflake Strategy and Data Model Strategy SWOTs render as polished standalone HTML with balanced 3-option analysis, evidence-backed items, decision matrices, and explicit recommendations that enable leadership decisions. Status badges show Undecided (amber).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create decided SWOT YAML data files (DataStage, BI Semantic, AI Semantic)</name>
  <files>
    docs/swot/data/datastage-migration.yml
    docs/swot/data/bi-semantic-layer.yml
    docs/swot/data/ai-semantic-layer.yml
    docs/swot/datastage-migration-swot.html
    docs/swot/bi-semantic-layer-swot.html
    docs/swot/ai-semantic-layer-swot.html
  </files>
  <action>
    Create the 3 decided SWOT analyses. Each must show full competitive analysis of rejected alternatives (not just the chosen option) to demonstrate rigor to leadership.

    **SWOT-04: DataStage Migration** (`docs/swot/data/datastage-migration.yml`):
    - title: "DataStage Migration SWOT Analysis"
    - subtitle: "Big-Bang vs Phased Python vs Parallel-Run"
    - status: "decided"
    - decision: "Phased Python Migration"
    - phase: "Phase 2 - ETL Migration and Data Pipeline"
    - Reference `docs/etl-patterns.md` (565 lines) for evidence. Reference the BasePipeline framework in `etl/src/pipelines/`.
    - Strengths (4-5 items): e.g., BasePipeline framework is proven (evidence: "480 tests passing; pilot migration of 5-10 DataStage jobs complete"), Python is testable/CI-friendly (evidence: "GitHub Actions CI with ruff linting, pytest, terraform validate"), Airflow orchestration operational (evidence: "DAG dependency management, watermark-based incremental loading"), phased approach reduces risk (evidence: "5-10 pilot jobs validated before committing to full migration")
    - Weaknesses (3-4 items): e.g., full migration of 300+ jobs will take months, Python performance vs DataStage for some complex transformations, team retraining from DataStage to Python/PySpark
    - Opportunities (3-4 items): e.g., Iceberg-native ETL enables medallion data quality (evidence: "Bronze/Silver/Gold with schema validation, quality checks per layer"), COBOL copybook parsing validated (evidence: "ETL-03: Mainframe source connectivity validated"), open-source stack eliminates license costs
    - Threats (3-4 items with mitigations): e.g., DataStage-specific transformations with no direct Python equivalent (mitigation: "Complexity classification in job inventory identifies high-risk jobs for early attention"), mainframe connectivity edge cases (mitigation: "Phase 2 validated core mainframe patterns; edge cases handled incrementally")
    - Decision matrix: Compare Big-Bang vs Phased Python vs Parallel-Run across criteria (Risk, Timeline, Cost, Team disruption, Rollback capability, Quality assurance)

    **SWOT-06: BI Semantic Layer** (`docs/swot/data/bi-semantic-layer.yml`):
    - title: "BI Semantic Layer SWOT Analysis"
    - subtitle: "Direct Access vs dbt Metrics vs AtScale vs Cube"
    - status: "decided"
    - decision: "Cube v0.36.0"
    - phase: "Phase 4 - Semantic Layers and Consumer Migration"
    - Reference `semantic/model/cubes/` for evidence (trading_metrics.yml, risk_exposure.yml).
    - Strengths (4-5 items): e.g., Cube provides unified metric definitions (evidence: "trading_metrics and risk_exposure cubes define measures with exact calculation logic"), PostgreSQL wire protocol enables any BI tool (evidence: "SQL API on port 15432 -- Tableau and Power BI connected via standard PostgreSQL driver"), pre-aggregation for performance (evidence: "Cube pre-aggregation layer caches frequent queries"), open-source with commercial support available
    - Weaknesses (3-4 items): e.g., additional infrastructure component to maintain, Cube-specific YAML learning curve, pre-aggregation configuration requires tuning
    - Opportunities (3-4 items): e.g., glossary term linkage enables business-friendly metric discovery (evidence: "Cube YAML definitions map to business glossary terms"), caching reduces Trino query load, centralized metric governance prevents BI tool definition drift
    - Threats (3-4 items with mitigations): e.g., Cube project roadmap changes (mitigation: "PostgreSQL wire protocol means BI tools connect via standard driver; switching semantic layer doesn't require BI reconfiguration")
    - Decision matrix: Compare Direct Access vs dbt Metrics vs AtScale vs Cube across criteria (BI tool compatibility, Metric governance, Performance, Cost, Complexity, Multi-engine support)

    **SWOT-07: AI Semantic Layer** (`docs/swot/data/ai-semantic-layer.yml`):
    - title: "AI Semantic Layer SWOT Analysis"
    - subtitle: "Build NL-to-SQL vs Buy Commercial Solution"
    - status: "decided"
    - decision: "Build-own with Claude on Bedrock"
    - phase: "Phase 4 - Semantic Layers and Consumer Migration"
    - Reference `etl/src/semantic/nl_to_sql.py` and related modules for evidence.
    - Strengths (4-5 items): e.g., NLToSQLEngine integrates with Cube YAML for context (evidence: "metric_context.py loads Cube definitions as LLM prompt context; SQL generation uses actual measure/dimension names"), Claude Sonnet on Bedrock provides enterprise-grade LLM (evidence: "AWS Bedrock deployment -- data stays in VPC, no external API calls"), golden dataset evaluation framework (evidence: "golden_datasets/ directory with per-domain test cases for accuracy benchmarking"), full control over prompt engineering and domain-specific tuning
    - Weaknesses (3-4 items): e.g., ongoing prompt maintenance as schema evolves, LLM accuracy variability for complex joins, no commercial support contract
    - Opportunities (3-4 items): e.g., domain-specific fine-tuning possible, few-shot examples per domain improve accuracy over time, Claude model upgrades improve quality without code changes
    - Threats (3-4 items with mitigations): e.g., Bedrock pricing changes (mitigation: "Anthropic API available as fallback; prompt format is model-agnostic"), LLM hallucination risk for financial queries (mitigation: "Golden dataset validation gates; SQL validation against schema before execution")
    - Decision matrix: Compare Build-own vs major commercial alternatives (ThoughtSpot, Databricks AI/BI, Snowflake Cortex) across criteria (Cost, Customization, Accuracy, Data residency, Integration with existing stack, Vendor lock-in)

    After creating all 3 YAML files, render them:
    ```bash
    python docs/render_html.py
    ```

    Verify each rendered HTML:
    - Contains all SWOT items with evidence fields populated
    - Status badge shows "Decided" (green)
    - Decision matrix renders with all alternatives
    - Evidence cites specific codebase artifacts or validated results
    - No unrendered Jinja2 tags in output
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python docs/render_html.py && python -c "
for f in ['docs/swot/datastage-migration-swot.html', 'docs/swot/bi-semantic-layer-swot.html', 'docs/swot/ai-semantic-layer-swot.html']:
    html = open(f).read()
    assert '{{' not in html and '{%' not in html, f'{f}: unrendered template tags'
    assert 'decided' in html.lower() or 'Decided' in html, f'{f}: missing decided status'
    assert html.count('swot-quadrant') == 4, f'{f}: missing SWOT quadrants'
    assert 'recommend' in html.lower(), f'{f}: missing recommendation'
    print(f'{f}: OK')
print('All 3 decided SWOTs validated')
"</automated>
  </verify>
  <done>
    DataStage Migration, BI Semantic Layer, and AI Semantic Layer SWOTs render as polished standalone HTML with full competitive analysis of rejected alternatives, evidence from codebase artifacts, decision matrices, and recommendations. Status badges show Decided (green).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create cross-SWOT index page and run full phase validation</name>
  <files>
    docs/swot/index.html
  </files>
  <action>
    Render the cross-SWOT index page and validate the complete phase output.

    1. Ensure the `render_index()` function in `docs/render_html.py` correctly builds the index page using `base_index.html` template with:
       - Dashboard-style card layout: CSS Grid, 2 columns on desktop, 1 column on tablet
       - Each card shows: SWOT title, subtitle (alternatives compared), status badge (Decided green / Undecided amber), brief description (1-2 sentences from executive_summary), and link to the standalone HTML file
       - Visual grouping: Undecided SWOTs appear first (highest priority for leadership) with a "Pending Decision" section header, followed by Decided SWOTs with a "Completed Analyses" section header
       - Overall summary paragraph at top: "6 strategic analyses covering the lakehouse architecture transformation. 4 decisions confirmed, 2 pending leadership review."
       - Same version-stamped footer as all other pages

    2. Run `python docs/render_html.py` to render ALL pages (6 SWOTs + 1 index = 7 HTML files).

    3. Full validation:
       - Index page has 6 cards with correct status badges (2 undecided/amber, 4 decided/green)
       - Each card links to the correct standalone SWOT HTML file (relative paths, e.g., `href="snowflake-strategy-swot.html"`)
       - All 7 HTML files are self-contained (no external CSS links, no JavaScript, no external font links)
       - All 7 HTML files have version-stamped footers with generation date and component versions
       - All collapsible sections work (contain `<details>` and `<summary>` tags)
       - All files have responsive meta viewport tag
       - Run the full test suite to confirm all tests pass

    4. If the `render_index` function needs adjustments to the `base_index.html` template (e.g., card layout, grouping logic), update the template as needed. The index template should read all YAML data files to build the card list.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python docs/render_html.py && python -m pytest etl/tests/test_html_render.py -x -v --timeout=30 && python -c "
import os
# Check all 7 HTML files exist
expected = ['nessie-catalog-swot.html', 'snowflake-strategy-swot.html', 'data-model-strategy-swot.html', 'datastage-migration-swot.html', 'bi-semantic-layer-swot.html', 'ai-semantic-layer-swot.html', 'index.html']
for f in expected:
    path = f'docs/swot/{f}'
    assert os.path.exists(path), f'Missing: {path}'
    html = open(path).read()
    assert '{{' not in html and '{%' not in html, f'{path}: unrendered template tags'
    assert '<script' not in html.lower(), f'{path}: contains JavaScript'
    assert '<link rel=\"stylesheet\"' not in html, f'{path}: contains external CSS link'
    assert '<style>' in html, f'{path}: missing embedded CSS'
    print(f'{path}: OK ({len(html)} bytes)')
# Check index has all 6 links
idx = open('docs/swot/index.html').read()
for swot in expected[:-1]:
    assert swot in idx, f'Index missing link to {swot}'
print(f'Index links: all 6 present')
print('PHASE 5 COMPLETE: 7 HTML files validated')
"</automated>
  </verify>
  <done>
    Cross-SWOT index page renders with dashboard card layout showing all 6 SWOTs with correct status badges (2 amber/undecided, 4 green/decided) and links to standalone files. All 7 HTML files are self-contained, have version-stamped footers, responsive design, and collapsible sections. Full test suite passes. Phase 5 deliverables are complete.
  </done>
</task>

</tasks>

<verification>
1. `python docs/render_html.py` renders all 7 HTML files without errors
2. `python -m pytest etl/tests/test_html_render.py -x -v --timeout=30` -- all tests pass
3. All 7 HTML files open correctly from file:// protocol with consistent navy/gold branding
4. No `<script>`, `<link rel="stylesheet">`, or external font references in any rendered HTML
5. Index page shows 6 cards: 2 Undecided (amber badge, listed first) + 4 Decided (green badge)
6. Each SWOT HTML has: 2x2 grid, executive summary, detailed S/W/O/T sections with evidence, decision matrix, mitigations for every threat, and a recommendation
7. The 2 undecided SWOTs have explicit actionable recommendations (not "it depends")
8. All footers show generation date and platform versions matching docker-compose.yml
</verification>

<success_criteria>
- 5 new SWOT YAML data files created with comprehensive content and evidence fields
- 5 new SWOT HTML files rendered as polished standalone documents
- Snowflake Strategy SWOT presents 3 options with clear recommendation for "Keep as Iceberg Compute-Only"
- Data Model Strategy SWOT presents 3 options with clear recommendation for "Evolve FSDM Incrementally"
- 3 decided SWOTs show full competitive analysis of rejected alternatives with codebase evidence
- Cross-SWOT index page has dashboard card layout with correct status badges and links
- All 7 HTML files (6 SWOTs + index) are self-contained, responsive, print-friendly, with version footers
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-html-foundation-and-swot-analyses/05-02-SUMMARY.md`
</output>
