---
phase: 04-semantic-layers-consumer-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - etl/src/semantic/prompt_builder.py
  - etl/src/semantic/nl_to_sql.py
  - etl/src/semantic/evaluation.py
  - etl/src/semantic/golden_datasets/trading_questions.json
  - etl/src/semantic/golden_datasets/risk_questions.json
  - etl/tests/unit/test_nl_to_sql.py
  - etl/tests/unit/test_prompt_builder.py
  - etl/tests/unit/test_evaluation.py
autonomous: true
requirements:
  - AISEM-01
  - AISEM-02
  - AISEM-03

must_haves:
  truths:
    - "NL-to-SQL prompt builder injects Cube YAML metric definitions as context so business terms resolve to correct SQL"
    - "NL-to-SQL engine generates valid Trino SQL from natural language questions on trading and risk domains"
    - "Golden evaluation datasets exist for both pilot domains with simple and complex question classifications"
    - "Evaluation framework measures execution accuracy by comparing generated SQL results against golden SQL results"
    - "Accuracy metrics are computed separately for simple and complex queries to validate tiered thresholds"
  artifacts:
    - path: "etl/src/semantic/prompt_builder.py"
      provides: "LLM prompt construction with Cube YAML metric context injection"
      contains: "def build_prompt"
    - path: "etl/src/semantic/nl_to_sql.py"
      provides: "NL-to-SQL engine using Claude via Bedrock"
      contains: "def generate_sql"
    - path: "etl/src/semantic/evaluation.py"
      provides: "Accuracy evaluation framework with golden dataset comparison"
      contains: "def evaluate_accuracy"
    - path: "etl/src/semantic/golden_datasets/trading_questions.json"
      provides: "Golden Q&A pairs for trading domain"
      min_lines: 20
    - path: "etl/src/semantic/golden_datasets/risk_questions.json"
      provides: "Golden Q&A pairs for risk exposure domain"
      min_lines: 20
  key_links:
    - from: "etl/src/semantic/prompt_builder.py"
      to: "etl/src/semantic/metric_context.py"
      via: "imports build_metric_context for prompt construction"
      pattern: "from.*metric_context.*import"
    - from: "etl/src/semantic/nl_to_sql.py"
      to: "etl/src/semantic/prompt_builder.py"
      via: "uses build_prompt to construct LLM messages"
      pattern: "from.*prompt_builder.*import.*build_prompt"
    - from: "etl/src/semantic/nl_to_sql.py"
      to: "boto3 bedrock-runtime"
      via: "Bedrock invoke_model for Claude API calls"
      pattern: "bedrock-runtime|invoke_model"
    - from: "etl/src/semantic/evaluation.py"
      to: "etl/src/semantic/golden_datasets/"
      via: "loads golden Q&A JSON for accuracy measurement"
      pattern: "load_golden_dataset"
---

<objective>
Build the NL-to-SQL service that reads Cube YAML metric definitions (from Plan 01) as semantic context, generates Trino SQL via Claude on Bedrock, and evaluates accuracy against golden datasets for both pilot domains.

Purpose: Delivers NL-to-SQL capability (AISEM-01) that provably uses the same metric definitions as the BI semantic layer (AISEM-02), with a measurement framework for the tiered accuracy thresholds (AISEM-03: 90% simple, 70% complex).

Output: NL-to-SQL engine (prompt builder + Bedrock client + evaluation framework), golden evaluation datasets for trading + risk exposure, and comprehensive unit tests with mocked LLM.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-semantic-layers-consumer-migration/04-CONTEXT.md
@.planning/phases/04-semantic-layers-consumer-migration/04-RESEARCH.md
@.planning/phases/04-semantic-layers-consumer-migration/04-01-SUMMARY.md

@etl/src/semantic/metric_context.py
@etl/src/semantic/benchmark.py
@semantic/model/cubes/trading_metrics.yml
@semantic/model/cubes/risk_exposure.yml
@etl/pyproject.toml

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From etl/src/semantic/metric_context.py (created in Plan 01):
```python
def load_cube_definitions(model_dir: str) -> list[dict[str, Any]]
def build_metric_context(definitions: list[dict[str, Any]]) -> str
```

From semantic/model/cubes/trading_metrics.yml (created in Plan 01):
```yaml
cubes:
  - name: trading_metrics
    sql_table: gold.trading_metrics
    measures:
      - name: total_notional (sum)
      - name: trade_count (sum)
      - name: avg_price (avg)
    dimensions:
      - name: symbol (string)
      - name: side (string)
```

From semantic/model/cubes/risk_exposure.yml (created in Plan 01):
```yaml
cubes:
  - name: risk_exposure
    sql_table: gold.risk_exposure
    measures:
      - name: total_market_value (sum)
      - name: total_var_95 (sum)
      - name: total_var_99 (sum)
      - name: total_expected_shortfall (sum)
      - name: position_count (sum)
    dimensions:
      - name: account_id (string)
      - name: sector (string)
      - name: currency (string)
```

From etl/src/iceberg_utils/trino.py (existing):
```python
def get_trino_connection(host=None, port=None, user="trino", catalog="iceberg", schema="default") -> Connection
def execute_query(conn: Connection, sql: str) -> list[tuple]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NL-to-SQL prompt builder and engine with Bedrock client</name>
  <files>
    etl/src/semantic/prompt_builder.py,
    etl/src/semantic/nl_to_sql.py,
    etl/tests/unit/test_prompt_builder.py,
    etl/tests/unit/test_nl_to_sql.py
  </files>
  <behavior>
    - test_system_prompt_contains_metric_context: SYSTEM_PROMPT template has {metric_context} placeholder
    - test_system_prompt_contains_few_shot: SYSTEM_PROMPT template has {few_shot_examples} placeholder
    - test_system_prompt_contains_decimal_rule: system prompt mentions DECIMAL types and no floating point
    - test_system_prompt_contains_schema_qualification: system prompt requires gold.table_name qualification
    - test_build_prompt_returns_messages: build_prompt returns list of dicts with "role" and "content" keys
    - test_build_prompt_system_message_has_context: first message role is "system" and content contains the metric_context arg
    - test_build_prompt_user_message_has_question: second message role is "user" and content contains the question
    - test_build_few_shot_examples: build_few_shot_examples returns formatted Q&A pairs for a domain
    - test_generate_sql_calls_bedrock: generate_sql invokes bedrock-runtime client with correct model_id (mocked)
    - test_generate_sql_returns_sql_string: generate_sql returns a SQL string from the mocked response
    - test_generate_sql_strips_markdown_fences: if LLM returns ```sql...```, the fences are stripped
    - test_generate_sql_uses_metric_context: generate_sql calls build_metric_context with the model_dir
    - test_nl_to_sql_engine_init: NLToSQLEngine initializes with model_dir and optional bedrock config
    - test_nl_to_sql_engine_ask: engine.ask(question) returns SQL string
    - test_nl_to_sql_engine_ask_with_domain: engine.ask(question, domain="trading") uses domain-specific few-shot examples
  </behavior>
  <action>
    **Tests first (RED):** Create `etl/tests/unit/test_prompt_builder.py` and `etl/tests/unit/test_nl_to_sql.py`. All LLM calls are mocked with `unittest.mock.patch` (mock boto3.client). The tests validate prompt structure, context injection, and SQL extraction logic without any live API calls. Run tests -- they must fail.

    **Implementation (GREEN):**

    1. **Prompt builder** -- Create `etl/src/semantic/prompt_builder.py` following the research pattern:
       - SYSTEM_PROMPT template with `{metric_context}` and `{few_shot_examples}` placeholders
       - System prompt MUST include: (a) "Use ONLY the tables, columns, and metrics defined below", (b) "Financial values use DECIMAL types - never use floating point", (c) "Always qualify table names with schema: gold.table_name", (d) "For aggregations, use the pre-defined metric formulas exactly as specified"
       - USER_PROMPT template with `{question}` placeholder and "Return ONLY the SQL query, no explanation"
       - `build_prompt(question, metric_context, few_shot_examples) -> list[dict[str, str]]`: Returns system + user message list
       - `build_few_shot_examples(domain: str) -> str`: Returns 3-5 domain-specific Q&A pairs (trading or risk_exposure). Hard-coded examples per domain that demonstrate the expected SQL patterns.
       - Uses `from __future__ import annotations`

    2. **NL-to-SQL engine** -- Create `etl/src/semantic/nl_to_sql.py`:
       - `class NLToSQLEngine`: Initialized with model_dir (path to semantic/model), region_name (default "us-east-1"), model_id (default "anthropic.claude-sonnet-4-20250514-v1:0")
       - `generate_sql(question, domain=None) -> str`: (a) Load Cube definitions via metric_context.load_cube_definitions, (b) Build context via metric_context.build_metric_context, (c) Build few-shot examples via prompt_builder.build_few_shot_examples(domain), (d) Build prompt via prompt_builder.build_prompt, (e) Call boto3 bedrock-runtime invoke_model, (f) Parse response, strip markdown fences (```sql...```), return clean SQL
       - `ask(question, domain=None) -> str`: Convenience wrapper for generate_sql
       - Error handling: Wrap Bedrock calls in try/except, raise `NLToSQLError` on failure
       - `class NLToSQLError(Exception)`: Custom exception for NL-to-SQL failures
       - Uses TYPE_CHECKING pattern for boto3 imports

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_prompt_builder.py tests/unit/test_nl_to_sql.py -x -q</automated>
  </verify>
  <done>
    prompt_builder.py constructs LLM prompts with Cube YAML metric context and domain-specific few-shot examples. nl_to_sql.py provides NLToSQLEngine that generates Trino SQL via Claude on Bedrock. All LLM interactions are mockable. Tests pass with mocked Bedrock client.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden evaluation datasets and accuracy evaluation framework</name>
  <files>
    etl/src/semantic/evaluation.py,
    etl/src/semantic/golden_datasets/trading_questions.json,
    etl/src/semantic/golden_datasets/risk_questions.json,
    etl/tests/unit/test_evaluation.py
  </files>
  <behavior>
    - test_load_golden_dataset_returns_list: load_golden_dataset returns list of dicts
    - test_golden_dataset_entry_has_required_fields: each entry has question, sql, complexity fields
    - test_golden_dataset_complexity_values: complexity is "simple" or "complex"
    - test_trading_dataset_has_simple_and_complex: trading_questions.json has both simple and complex entries
    - test_risk_dataset_has_simple_and_complex: risk_questions.json has both simple and complex entries
    - test_trading_dataset_min_size: trading_questions.json has at least 15 entries
    - test_risk_dataset_min_size: risk_questions.json has at least 15 entries
    - test_golden_sql_references_correct_tables: trading questions reference gold.trading_metrics, risk questions reference gold.risk_exposure
    - test_eval_result_dataclass: EvalResult has question, golden_sql, generated_sql, match, complexity, error fields
    - test_evaluate_accuracy_all_correct: 100% accuracy when all results match
    - test_evaluate_accuracy_some_wrong: partial accuracy computed correctly
    - test_evaluate_accuracy_filter_simple: complexity_filter="simple" only counts simple entries
    - test_evaluate_accuracy_filter_complex: complexity_filter="complex" only counts complex entries
    - test_evaluate_accuracy_empty_results: returns 0.0 accuracy for empty list
    - test_run_evaluation_mocked: run_evaluation generates SQL for each golden question and compares (mocked LLM and DB)
    - test_evaluation_report_structure: generate_evaluation_report returns dict with overall, simple, complex accuracy breakdowns
  </behavior>
  <action>
    **Tests first (RED):** Create `etl/tests/unit/test_evaluation.py` testing golden dataset loading, accuracy calculation, and evaluation report generation. Mock the Trino connection and NL-to-SQL engine. Run tests -- they must fail.

    **Implementation (GREEN):**

    1. **Golden datasets** -- Create `etl/src/semantic/golden_datasets/trading_questions.json`:
       - At least 15 question-SQL pairs for the trading domain
       - Mix of simple (single-table lookups, WHERE filters) and complex (aggregation, GROUP BY, HAVING, subqueries)
       - Simple examples: "What is the total notional for AAPL?", "How many BUY trades are there?", "What is the average price for JPM?"
       - Complex examples: "What symbol has the highest total notional?", "Which symbols have more than 100 trades?", "Compare BUY vs SELL total notional for all symbols"
       - All SQL uses `gold.trading_metrics` table with correct column names
       - Each entry: `{"question": "...", "sql": "...", "complexity": "simple"|"complex"}`

    2. **Risk exposure golden dataset** -- Create `etl/src/semantic/golden_datasets/risk_questions.json`:
       - At least 15 question-SQL pairs for the risk exposure domain
       - Simple: "What is the total market value for account ACCT-1234?", "What is the VaR 95 for the Technology sector?"
       - Complex: "Which sector has the highest expected shortfall?", "Compare VaR 95 vs VaR 99 by currency", "What accounts have more than 10 positions?"
       - All SQL uses `gold.risk_exposure` table

    3. **Evaluation framework** -- Create `etl/src/semantic/evaluation.py` following the research pattern:
       - `EvalResult` dataclass: question, golden_sql, generated_sql, golden_results (list[tuple]), generated_results (list[tuple]), match (bool), complexity (str), error (str|None)
       - `load_golden_dataset(path: str) -> list[dict]`: Load JSON file
       - `evaluate_accuracy(results: list[EvalResult], complexity_filter: str|None = None) -> dict`: Compute total, correct, accuracy_pct, filtered by complexity if specified
       - `run_evaluation(engine: NLToSQLEngine, conn: Connection, dataset_path: str) -> list[EvalResult]`: For each golden question, (a) generate SQL via engine.ask(), (b) execute both golden SQL and generated SQL against Trino conn, (c) compare result sets (order-independent), (d) record EvalResult
       - `generate_evaluation_report(results: list[EvalResult]) -> dict`: Returns dict with overall accuracy, simple accuracy, complex accuracy, per-question details, and pass/fail against thresholds (90% simple, 70% complex)
       - Result comparison: sort both result sets, compare as sets. If generated SQL errors, match=False and error field populated.

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_evaluation.py -x -q</automated>
  </verify>
  <done>
    Golden datasets exist for both pilot domains (15+ entries each, mix of simple/complex). Evaluation framework loads datasets, runs evaluation with mocked LLM/DB, computes tiered accuracy metrics. generate_evaluation_report returns overall/simple/complex accuracy breakdowns with pass/fail against thresholds.
  </done>
</task>

</tasks>

<verification>
Run all new unit tests from both plans:
```bash
cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_prompt_builder.py tests/unit/test_nl_to_sql.py tests/unit/test_evaluation.py -x -q
```

Run full unit test suite to confirm no regressions:
```bash
cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit -x -q
```

Validate golden datasets are well-formed JSON:
```bash
cd /home/azureuser/lakehouse && python3 -c "import json; [json.load(open(f)) for f in ['etl/src/semantic/golden_datasets/trading_questions.json', 'etl/src/semantic/golden_datasets/risk_questions.json']]; print('Golden datasets valid')"
```
</verification>

<success_criteria>
- NL-to-SQL prompt builder injects Cube YAML metric context into LLM prompts (AISEM-02)
- NL-to-SQL engine generates SQL via Claude on Bedrock with proper error handling (AISEM-01)
- Golden datasets have 15+ entries each for trading and risk exposure with simple/complex mix
- Evaluation framework computes tiered accuracy (90% simple / 70% complex thresholds) (AISEM-03)
- All unit tests pass with mocked LLM and DB connections
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-semantic-layers-consumer-migration/04-02-SUMMARY.md`
</output>
