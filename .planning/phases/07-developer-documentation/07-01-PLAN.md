---
phase: 07-developer-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/templates/base_developer.html
  - docs/templates/macros/code_block.html
  - docs/templates/macros/checklist.html
  - docs/render_html.py
  - docs/developer/data/onboarding.yml
  - docs/developer/data/repo-structure.yml
  - docs/developer/data/first-pipeline.yml
  - docs/developer/data/day1-checklist.yml
  - docs/developer/onboarding.html
  - docs/developer/repo-structure.html
  - docs/developer/first-pipeline.html
  - docs/developer/day1-checklist.html
  - etl/tests/test_html_render.py
autonomous: true
requirements: [DEV-01, DEV-02, DEV-03, DEV-09]

must_haves:
  truths:
    - "render_developer_docs() renders all YAML data files in docs/developer/data/ to standalone HTML"
    - "base_developer.html template supports page_type variants (guide, reference, faq, checklist, visualization)"
    - "Onboarding guide lists prerequisites, Docker Compose launch steps, and service verification commands"
    - "Repo structure walkthrough describes every top-level directory and key files"
    - "First pipeline tutorial builds a synthetic hello-world CSV-to-Bronze pipeline step by step"
    - "Day 1 checklist renders as a single printed page with compact checkbox layout via @media print CSS"
  artifacts:
    - path: "docs/templates/base_developer.html"
      provides: "Jinja2 template with page_type conditional blocks, navy/gold CSS, responsive, print rules"
      min_lines: 100
    - path: "docs/render_html.py"
      provides: "render_developer_docs() function extending the existing render pipeline"
      contains: "render_developer_docs"
    - path: "docs/developer/data/onboarding.yml"
      provides: "DEV-01 structured content"
    - path: "docs/developer/data/day1-checklist.yml"
      provides: "DEV-09 checklist items"
    - path: "etl/tests/test_html_render.py"
      provides: "Tests for developer docs rendering"
      contains: "test_developer"
  key_links:
    - from: "docs/render_html.py"
      to: "docs/developer/data/*.yml"
      via: "render_developer_docs() iterates YAML files"
      pattern: "render_developer_docs"
    - from: "docs/templates/base_developer.html"
      to: "docs/templates/macros/collapsible.html"
      via: "Jinja2 macro import"
      pattern: "collapsible"
    - from: "etl/tests/test_html_render.py"
      to: "docs/render_html.py"
      via: "import render_developer_docs"
      pattern: "render_developer_docs"
---

<objective>
Create the developer documentation rendering infrastructure (template, macros, render function) and produce the first batch of developer-facing HTML pages: onboarding guide (DEV-01), repository structure walkthrough (DEV-02), first pipeline tutorial (DEV-03), and Day 1 checklist (DEV-09).

Purpose: Establishes the base template and render function that all 12 developer docs will use, plus delivers the "New Engineers" content batch that a new developer needs on their first day.

Output: `base_developer.html` template, `render_developer_docs()` in render_html.py, 2 new macros, 4 YAML data files, 4 rendered HTML pages, and tests.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-developer-documentation/07-CONTEXT.md
@.planning/phases/07-developer-documentation/07-RESEARCH.md
@.planning/phases/05-html-foundation-and-swot-analyses/05-01-SUMMARY.md
@.planning/phases/06-architecture-visualizations/06-01-SUMMARY.md

<interfaces>
<!-- Key functions and templates the executor needs from existing codebase -->

From docs/render_html.py (established render pipeline):
```python
def extract_versions(compose_path: Path | str | None = None) -> dict[str, dict[str, str]]:
    """Extract component versions from docker-compose.yml image tags."""

def _create_jinja_env(template_dir: Path | None = None) -> Environment:
    """Create Jinja2 Environment with FileSystemLoader and autoescape=False."""

def extract_services(compose_path: Path | str | None = None, overrides_path: Path | str | None = None) -> dict:
    """Extract service metadata from docker-compose.yml merged with services.yml overrides."""

def render_mermaid_to_svg(mmd_path: Path) -> str:
    """Render a Mermaid .mmd file to SVG string via mmdc CLI."""

def _placeholder_svg(message: str = "Mermaid CLI required") -> str:
    """Generate a placeholder SVG when mmdc is unavailable."""
```

From docs/templates/base_arch_index.html (card grid pattern to reuse for developer index):
- Card grid: `.card-grid` with `grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))`
- Audience badges: `.audience-executives`, `.audience-engineers`, `.audience-security`, `.audience-compliance`
- To add for developer docs: `.audience-new-engineers`, `.audience-all-engineers`, `.audience-contributors`

From docs/templates/macros/collapsible.html (reuse for FAQ and long code examples):
```html
{% macro collapsible(summary_text) %}
<details><summary>{{ summary_text }}</summary>{{ caller() }}</details>
{% endmacro %}
```

Existing constants pattern in render_html.py:
```python
PROJECT_ROOT = Path(__file__).parent.parent
TEMPLATE_DIR = PROJECT_ROOT / "docs" / "templates"
SWOT_DATA_DIR = PROJECT_ROOT / "docs" / "swot" / "data"
ARCH_DATA_DIR = PROJECT_ROOT / "docs" / "architecture" / "data"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create base_developer.html template, macros, render_developer_docs() function, and test scaffold</name>
  <files>
    docs/templates/base_developer.html,
    docs/templates/macros/code_block.html,
    docs/templates/macros/checklist.html,
    docs/render_html.py,
    etl/tests/test_html_render.py
  </files>
  <behavior>
    - Test: render_developer_docs() with a minimal YAML data file (page_type: guide) produces valid HTML with DOCTYPE, navy branding (#1a2332), and version footer
    - Test: render_developer_docs() with page_type: checklist produces HTML with checkbox items and @media print CSS rules (font-size 8-9pt, compact spacing)
    - Test: render_developer_docs() with page_type: reference produces HTML with table structures
    - Test: render_developer_docs() with page_type: faq produces HTML with details/summary collapsible entries
    - Test: render_developer_docs() skips empty/None YAML files without error
    - Test: code_block macro produces pre/code HTML with syntax class
  </behavior>
  <action>
    1. Create `docs/templates/base_developer.html` -- a single flexible Jinja2 template with conditional content blocks driven by `page_type` field from YAML data:
       - `guide`: sequential sections with headings, prose content, optional code blocks (DEV-01, DEV-02, DEV-03, DEV-05, DEV-06)
       - `reference`: tables and structured data with section headings (DEV-04, DEV-07, DEV-10, DEV-12)
       - `faq`: Symptom-Fix-Why collapsible entries using details/summary (DEV-08)
       - `checklist`: compact checkbox layout with @media print CSS for single-page A4 output (DEV-09)
       - `visualization`: Mermaid SVG with surrounding narrative context (DEV-11)
       - Include navy (#1a2332) / gold (#c8a961) CSS matching existing base_swot.html branding
       - Embed all CSS inline (standalone HTML, no external deps)
       - Include responsive design at 768px breakpoint
       - Include @media print rules (white bg, black text, no border-radius)
       - Import collapsible macro from macros/collapsible.html
       - Include version-stamped footer block (same as base_swot.html)
       - For checklist page_type: compact @media print with 8pt font, tight margins, checkbox styling

    2. Create `docs/templates/macros/code_block.html` -- macro for rendering code examples with language class:
       ```
       {% macro code_block(code, language="python") %}
       <pre><code class="language-{{ language }}">{{ code }}</code></pre>
       {% endmacro %}
       ```

    3. Create `docs/templates/macros/checklist.html` -- macro for checkbox items:
       ```
       {% macro checklist_item(text, verify="") %}
       <label class="checklist-item"><input type="checkbox"> {{ text }}{% if verify %} <span class="verify">({{ verify }})</span>{% endif %}</label>
       {% endmacro %}
       ```

    4. Add `render_developer_docs()` function to `docs/render_html.py` following the exact pattern from render_architecture() (see RESEARCH Pattern 1):
       - Add constants: `DEV_DATA_DIR = PROJECT_ROOT / "docs" / "developer" / "data"`, `DEV_DIAGRAM_DIR = PROJECT_ROOT / "docs" / "developer" / "diagrams"`, `DEV_OUTPUT_DIR = PROJECT_ROOT / "docs" / "developer"`
       - Function signature: `render_developer_docs(data_dir, diagram_dir, template_dir, output_dir, compose_path) -> list[Path]`
       - Create output_dir if not exists
       - Load Jinja2 env, extract versions, extract services (reuse extract_services() for service data)
       - Render Mermaid diagrams from diagram_dir/*.mmd with graceful fallback via render_mermaid_to_svg()
       - Iterate sorted YAML files in data_dir, render each through base_developer.html template
       - Pass versions, services, svg_diagrams, generation_date to template context
       - Use output_filename from YAML if present, otherwise data_file.stem + ".html"
       - Skip None YAML files (empty files)

    5. Add tests to `etl/tests/test_html_render.py` (append new test functions after existing ones):
       - `test_developer_docs_render_guide` -- guide page_type produces valid HTML
       - `test_developer_docs_render_checklist` -- checklist page_type has checkbox and print CSS
       - `test_developer_docs_render_reference` -- reference page_type has table structures
       - `test_developer_docs_render_faq` -- faq page_type has details/summary elements
       - `test_developer_docs_render_skip_empty` -- empty YAML file does not cause error
       - All tests use `@pytest.mark.unit` marker and follow existing test patterns (tmp_path for data/output dirs)
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/test_html_render.py -x --tb=short -m unit -k "developer"</automated>
  </verify>
  <done>
    base_developer.html template renders all 5 page_type variants correctly. render_developer_docs() function works end-to-end with YAML data files. All new tests pass alongside existing 26 tests. Two macros (code_block, checklist) created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create YAML data files and render HTML for onboarding, repo structure, first pipeline, and Day 1 checklist</name>
  <files>
    docs/developer/data/onboarding.yml,
    docs/developer/data/repo-structure.yml,
    docs/developer/data/first-pipeline.yml,
    docs/developer/data/day1-checklist.yml,
    docs/developer/onboarding.html,
    docs/developer/repo-structure.html,
    docs/developer/first-pipeline.html,
    docs/developer/day1-checklist.html,
    etl/tests/test_html_render.py
  </files>
  <action>
    1. Create `docs/developer/data/onboarding.yml` (DEV-01) with page_type: guide:
       - title: "Getting Started"
       - Sections: Prerequisites (Python 3.11+, Docker, Docker Compose, Git -- list only, no install instructions per user decision), Clone and Setup (git clone, cd, docker-compose up -d), Verify Services (list key services with health check commands from docker-compose.yml: `curl -s http://localhost:8081/health` for Airflow, `docker exec trino trino --execute "SELECT 1"` for Trino, etc.), First Commands (run pytest, check Airflow DAGs)
       - Tone: concise, code-first, assumes competent Python engineers per user decision
       - All commands must be copy-paste-ready

    2. Create `docs/developer/data/repo-structure.yml` (DEV-02) with page_type: guide:
       - title: "Repository Structure"
       - Sections: Top-level overview (directory tree with descriptions for etl/, docs/, infra/, semantic/, ci/, dbt/, .github/), ETL package deep-dive (src/ subdirectories: pipelines/, config/, governance/, quality/, semantic/, iceberg_utils/, lineage/, inventory/), Key files (docker-compose.yml, pyproject.toml, .pre-commit-config.yaml, render_html.py)
       - Extract actual directory structure from the repo

    3. Create `docs/developer/data/first-pipeline.yml` (DEV-03) with page_type: guide:
       - title: "Write Your First Pipeline"
       - Build a synthetic "hello world" CSV-to-Bronze pipeline step by step per user decision:
         Step 1: Create pipeline class extending BasePipeline (show full import: `from src.pipelines.base import BasePipeline, PipelineConfig, MedallionLayer`)
         Step 2: Define schema with StructType (from pyspark.sql.types)
         Step 3: Implement extract() to read CSV
         Step 4: Implement transform() with basic cleaning
         Step 5: Add quality checks
         Step 6: Create Airflow DAG
         Step 7: Run tests (`python -m pytest tests/ -k "hello_world" -x`)
       - Reference 1-2 existing concrete pipelines (TradesBronzePipeline, TradesSilverPipeline) as "what you'll build toward"
       - All code examples show full import paths per user decision

    4. Create `docs/developer/data/day1-checklist.yml` (DEV-09) with page_type: checklist:
       - title: "Day 1 Checklist"
       - Terse checkbox items with verification commands per user decision. Three sections:
         Setup: "Clone repo", "Docker Compose up (verify: docker ps | wc -l shows 25+)", "Services healthy (verify: curl Airflow, Trino query)", "Run full test suite (verify: pytest shows 500+ passing)"
         First Pipeline: "Read BasePipeline source", "Create hello_world pipeline", "Run pipeline test", "Check Bronze output in Trino"
         First PR: "Create feature branch", "Run pre-commit hooks", "Push and create PR", "CI checks pass"
       - Must fit on a single printed A4/Letter page -- use compact styling, no prose, just terse items with verify commands
       - Link to detailed pages (onboarding.html, first-pipeline.html, contributor.html) for more info

    5. Render all 4 HTML pages by running render_developer_docs() (or by invoking the render pipeline).

    6. Add 4 specific tests to `etl/tests/test_html_render.py`:
       - `test_developer_onboarding` -- DEV-01: HTML contains "docker-compose", service verification commands, prerequisites list
       - `test_developer_repo_structure` -- DEV-02: HTML contains directory names (etl/, docs/, infra/), key file descriptions
       - `test_developer_first_pipeline` -- DEV-03: HTML contains "BasePipeline", "hello" (synthetic example), step-by-step code blocks
       - `test_developer_day1_checklist` -- DEV-09: HTML contains checkbox inputs, @media print CSS, links to other pages
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/test_html_render.py -x --tb=short -m unit -k "developer"</automated>
  </verify>
  <done>
    4 YAML data files created with accurate, copy-paste-ready content. 4 HTML pages rendered as standalone files in docs/developer/. Day 1 checklist fits on one printed page. All code examples use full import paths. Tests verify content presence in rendered HTML. All tests pass (existing + new).
  </done>
</task>

</tasks>

<verification>
1. `cd /home/azureuser/lakehouse/etl && python -m pytest tests/test_html_render.py -x --tb=short -m unit` -- all tests pass (existing 26 + new developer doc tests)
2. `ls docs/developer/*.html` -- shows onboarding.html, repo-structure.html, first-pipeline.html, day1-checklist.html
3. `grep -l "<!DOCTYPE html>" docs/developer/*.html | wc -l` -- shows 4
4. `grep -c "#1a2332" docs/developer/onboarding.html` -- confirms navy branding present
5. `grep "BasePipeline" docs/developer/first-pipeline.html` -- confirms tutorial content
</verification>

<success_criteria>
- base_developer.html template renders 5 page_type variants correctly
- render_developer_docs() function follows established render_architecture() pattern
- 4 developer HTML pages rendered as standalone files with embedded CSS
- Onboarding guide has copy-paste-ready Docker Compose and verification commands
- First pipeline tutorial walks through synthetic hello-world CSV-to-Bronze example
- Day 1 checklist is compact enough for single-page print
- All code examples show full import paths (from src.pipelines.base import ...)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-developer-documentation/07-01-SUMMARY.md`
</output>
