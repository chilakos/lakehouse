---
phase: 07-developer-documentation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - docs/developer/data/etl-patterns.yml
  - docs/developer/data/testing.yml
  - docs/developer/data/cicd.yml
  - docs/developer/data/service-urls.yml
  - docs/developer/data/troubleshooting.yml
  - docs/developer/diagrams/cicd-flow.mmd
  - docs/developer/etl-patterns.html
  - docs/developer/testing.html
  - docs/developer/cicd.html
  - docs/developer/service-urls.html
  - docs/developer/troubleshooting.html
  - etl/tests/test_html_render.py
autonomous: true
requirements: [DEV-04, DEV-05, DEV-06, DEV-07, DEV-08]

must_haves:
  truths:
    - "ETL patterns reference contains all 8 sections from etl-patterns.md converted to HTML"
    - "Testing guide covers unit tests, integration tests, 4 pytest markers, CI gate behavior, and includes pytest output snippets"
    - "CI/CD workflow page has a Mermaid SVG (or placeholder) showing PR-to-production promotion path"
    - "Service URL reference table lists all platform services with ports extracted from docker-compose.yml"
    - "Troubleshooting FAQ uses Symptom-Fix-Why format with collapsible entries"
  artifacts:
    - path: "docs/developer/etl-patterns.html"
      provides: "DEV-04 ETL patterns as standalone HTML"
      contains: "medallion"
    - path: "docs/developer/testing.html"
      provides: "DEV-05 testing guide"
      contains: "pytest"
    - path: "docs/developer/cicd.html"
      provides: "DEV-06 CI/CD workflow"
      contains: "staging"
    - path: "docs/developer/service-urls.html"
      provides: "DEV-07 service URL reference"
      contains: "localhost"
    - path: "docs/developer/troubleshooting.html"
      provides: "DEV-08 troubleshooting FAQ"
      contains: "symptom"
    - path: "docs/developer/diagrams/cicd-flow.mmd"
      provides: "Mermaid CI/CD promotion flowchart"
  key_links:
    - from: "docs/developer/data/service-urls.yml"
      to: "docs/render_html.py"
      via: "extract_services() provides dynamic service data at render time"
      pattern: "extract_services"
    - from: "docs/developer/data/etl-patterns.yml"
      to: "docs/etl-patterns.md"
      via: "Content converted from existing markdown"
      pattern: "medallion"
    - from: "docs/developer/data/cicd.yml"
      to: ".github/workflows/"
      via: "Content derived from CI workflow definitions"
      pattern: "ci.yml"
---

<objective>
Create the "All Engineers" documentation batch: ETL patterns reference (DEV-04), testing guide (DEV-05), CI/CD workflow (DEV-06), service URL reference (DEV-07), and troubleshooting FAQ (DEV-08). These are the day-to-day reference pages every engineer on the 40-person team will use.

Purpose: Converts existing knowledge scattered across markdown files, docker-compose.yml, CI workflows, and tribal knowledge into structured, searchable HTML reference pages.

Output: 5 YAML data files, 1 Mermaid diagram, 5 rendered HTML pages, and tests.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-developer-documentation/07-CONTEXT.md
@.planning/phases/07-developer-documentation/07-RESEARCH.md
@.planning/phases/07-developer-documentation/07-01-SUMMARY.md

<interfaces>
<!-- Key assets from the codebase to reference -->

From docs/render_html.py (available after Plan 07-01):
```python
def render_developer_docs(
    data_dir: Path | None = None,
    diagram_dir: Path | None = None,
    template_dir: Path | None = None,
    output_dir: Path | None = None,
    compose_path: Path | str | None = None,
) -> list[Path]:
    """Render all developer documentation pages from YAML data + Jinja2 templates."""
```

From docs/etl-patterns.md (564 lines, 8 sections to convert for DEV-04):
- Section 1: Medallion Architecture Overview
- Section 2: Creating a New Pipeline (step-by-step)
- Section 3: Data Quality Integration
- Section 4: Airflow DAG Patterns
- Section 5: Incremental Loading
- Section 6: Mainframe Source Handling
- Section 7: Testing Patterns
- Section 8: Quick Reference (import paths, CLI commands)

From .github/workflows/ (for DEV-06):
- ci.yml: 4 jobs (python-lint, python-test, cube-yaml-validate, terraform-validate/fmt)
- deploy-dev.yml, deploy-staging.yml, deploy-prod.yml: terraform workspace deployments
- Promotion: feature branch -> PR to dev -> merge -> PR to staging -> merge -> PR to main -> prod

From etl/pyproject.toml (for DEV-05, DEV-12):
- pytest markers: unit, integration, slow, snowflake
- ruff config: line-length, select rules

From docker-compose.yml + docs/architecture/data/services.yml (for DEV-07):
- extract_services() already parses and merges this data
- 25+ services with ports, health checks, descriptions

From RESEARCH Pitfall recommendations (for DEV-08):
- 10 specific troubleshooting entries identified (Docker memory, Nessie health, Spark JARs, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML data files for ETL patterns, testing guide, CI/CD workflow, service URLs, and troubleshooting FAQ</name>
  <files>
    docs/developer/data/etl-patterns.yml,
    docs/developer/data/testing.yml,
    docs/developer/data/cicd.yml,
    docs/developer/data/service-urls.yml,
    docs/developer/data/troubleshooting.yml,
    docs/developer/diagrams/cicd-flow.mmd
  </files>
  <action>
    1. Create `docs/developer/data/etl-patterns.yml` (DEV-04) with page_type: reference:
       - title: "ETL Patterns Reference"
       - Convert all 8 sections from `docs/etl-patterns.md` (564 lines) to structured YAML per user decision ("convert as-is, minimal rewriting needed")
       - Preserve code blocks as raw content strings in YAML (use YAML literal block scalars `|` for multi-line code)
       - Preserve tables as structured data (list of dicts)
       - Sections: Medallion Architecture, Creating a New Pipeline, Data Quality Integration, Airflow DAG Patterns, Incremental Loading, Mainframe Source Handling, Testing Patterns, Quick Reference
       - Read docs/etl-patterns.md to extract accurate content

    2. Create `docs/developer/data/testing.yml` (DEV-05) with page_type: guide:
       - title: "Testing Guide"
       - Sections:
         a) Test Organization: directory structure (etl/tests/), conftest.py fixtures (spark_session, mock_s3, nessie_catalog, soda_connection, sample_trades_df)
         b) Pytest Markers: table of 4 markers (unit, integration, slow, snowflake) with descriptions and usage
         c) Running Tests: commands for unit only (`-m unit`), integration (`-m integration`), full suite, single file, verbose
         d) CI Gate Behavior: what runs on PR (lint + unit), what runs on merge (full suite), how to handle failures
         e) Writing New Tests: test file naming, marker decoration, fixture usage, assertion patterns
         f) Example Output: include formatted pytest output snippets per user decision showing passing and failing examples (use pre-formatted strings in YAML)
       - Read etl/pyproject.toml and etl/tests/conftest.py for accurate marker and fixture names

    3. Create `docs/developer/data/cicd.yml` (DEV-06) with page_type: guide:
       - title: "CI/CD Workflow"
       - Sections:
         a) Overview: PR-to-production promotion path narrative
         b) CI Pipeline (ci.yml): 4 jobs with what each checks (python-lint: ruff check+format, python-test: pytest -m unit, cube-yaml-validate, terraform-validate+fmt)
         c) Environment Promotion: dev -> staging -> prod with what happens at each gate
         d) Deploy Workflows: deploy-dev.yml (push to dev branch), deploy-staging.yml (push to staging), deploy-prod.yml (push to main + smoke tests)
         e) Branch Naming: feature/TICKET-description convention, CI parses branch prefix for environment routing
         f) CI/CD Flow Diagram: references cicd-flow.mmd Mermaid diagram
       - Read .github/workflows/ci.yml and deploy-*.yml for accurate job names and triggers

    4. Create `docs/developer/diagrams/cicd-flow.mmd` (DEV-06) -- Mermaid flowchart per user decision:
       ```
       flowchart TD
         A[feature/TICKET-desc branch] --> B{PR to dev}
         B --> C[CI: lint + unit tests + validate]
         C -->|Pass| D[Merge to dev]
         D --> E[Deploy to dev environment]
         E --> F{PR to staging}
         F --> G[CI gates]
         G -->|Pass| H[Merge to staging]
         H --> I[Deploy to staging + integration tests]
         I --> J{PR to main}
         J --> K[CI gates]
         K -->|Pass| L[Merge to main]
         L --> M[Deploy to production + smoke tests]
       ```

    5. Create `docs/developer/data/service-urls.yml` (DEV-07) with page_type: reference:
       - title: "Service URL Reference"
       - Do NOT hardcode service ports -- the template should render from `services` context variable (populated by extract_services() at render time per user decision and RESEARCH Pitfall 3)
       - YAML data file contains: title, subtitle, column_headers (Service, URL, Protocol, Description, Default Credentials, Common Actions), and developer-specific annotations:
         For key services add annotations dict with default_credentials and common_actions:
         - Airflow: admin/admin, "View DAG runs, trigger pipeline"
         - Trino: any user, "Run SQL queries, check catalogs"
         - MinIO: minioadmin/minioadmin, "Browse buckets, check data files"
         - Grafana: admin/admin, "View dashboards, check alerts"
         - Nessie: no auth, "Browse catalog, check table metadata"
         - OpenMetadata: admin/admin, "View lineage, browse glossary"
       - The template renders dynamic service data (ports, healthchecks) from extract_services() and static annotations from the YAML

    6. Create `docs/developer/data/troubleshooting.yml` (DEV-08) with page_type: faq:
       - title: "Troubleshooting FAQ"
       - Use Symptom-Fix-Why format per user decision, 10 entries from RESEARCH recommendations:
         1. Spark executor OOM killed during large Bronze ingest (increase Docker memory to 8GB+)
         2. Nessie health check fails on startup (wait 30s for PostgreSQL, restart nessie)
         3. Spark JAR conflict: NoSuchMethodError in Iceberg (docker compose down -v && up -d)
         4. Airflow webserver won't start (airflow-init must complete first; check docker compose logs airflow-init)
         5. Ranger admin UI inaccessible (ZK -> Solr -> DB -> Admin startup chain; restart in order)
         6. MinIO bucket not found (minio-init container must run first; docker compose up minio-init)
         7. Trino queries fail after catalog config change (restart trino workers: docker compose restart trino)
         8. Cube pre-aggregation build timeout (check Trino connectivity; verify CUBE_DB_HOST env var)
         9. OpenMetadata ingestion hangs (elasticsearch indexing lag; check ES health)
         10. pytest markers not recognized (add markers to pyproject.toml [tool.pytest.ini_options] markers list)
       - Categories: "Docker and Services", "Query Engine", "ETL Pipeline", "Testing"
  </action>
  <verify>
    <automated>ls -la /home/azureuser/lakehouse/docs/developer/data/etl-patterns.yml /home/azureuser/lakehouse/docs/developer/data/testing.yml /home/azureuser/lakehouse/docs/developer/data/cicd.yml /home/azureuser/lakehouse/docs/developer/data/service-urls.yml /home/azureuser/lakehouse/docs/developer/data/troubleshooting.yml /home/azureuser/lakehouse/docs/developer/diagrams/cicd-flow.mmd && python -c "import yaml; [yaml.safe_load(open(f'docs/developer/data/{n}.yml')) for n in ['etl-patterns','testing','cicd','service-urls','troubleshooting']]" && echo "All YAML valid"</automated>
  </verify>
  <done>
    5 YAML data files created with accurate, structured content derived from actual source files (etl-patterns.md, pyproject.toml, conftest.py, CI workflows, docker-compose.yml). 1 Mermaid CI/CD flow diagram created. All YAML files parse without error. ETL patterns content preserves all 8 sections. Service URLs use extract_services() dynamic data pattern (not hardcoded ports). Troubleshooting FAQ has 10 entries in Symptom-Fix-Why format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render 5 HTML pages and add content verification tests</name>
  <files>
    docs/developer/etl-patterns.html,
    docs/developer/testing.html,
    docs/developer/cicd.html,
    docs/developer/service-urls.html,
    docs/developer/troubleshooting.html,
    etl/tests/test_html_render.py
  </files>
  <action>
    1. Render all developer docs by calling render_developer_docs() (this picks up the 5 new YAML files from Task 1 plus the 4 from Plan 07-01). Can invoke via a short Python script or by extending render_html.py's __main__ block.

    2. Verify all 5 new HTML files exist in docs/developer/ and contain expected content.

    3. Add 5 content-specific tests to `etl/tests/test_html_render.py` (append after existing developer tests from Plan 07-01):
       - `test_developer_etl_patterns` (DEV-04): HTML contains "Medallion", code block elements, all 8 section headings
       - `test_developer_testing_guide` (DEV-05): HTML contains "pytest", marker names (unit, integration, slow, snowflake), formatted output snippet
       - `test_developer_cicd` (DEV-06): HTML contains "staging", "production", workflow names (ci.yml), Mermaid SVG or placeholder
       - `test_developer_service_urls` (DEV-07): HTML contains "localhost", service names from docker-compose.yml (airflow, trino, minio), port numbers
       - `test_developer_troubleshooting` (DEV-08): HTML contains "Symptom" (or "symptom"), details/summary elements for collapsible FAQ, at least 8 FAQ entries
       - All tests use @pytest.mark.unit and render from actual YAML data files (not synthetic test data) to validate real content

    4. Run full test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/test_html_render.py -x --tb=short -m unit</automated>
  </verify>
  <done>
    5 HTML pages rendered as standalone files with embedded CSS, navy/gold branding, and version-stamped footers. ETL patterns page contains all 8 sections. CI/CD page has Mermaid diagram (SVG or placeholder). Service URLs page shows dynamic service data. Troubleshooting FAQ has collapsible Symptom-Fix-Why entries. All tests pass (existing + new).
  </done>
</task>

</tasks>

<verification>
1. `cd /home/azureuser/lakehouse/etl && python -m pytest tests/test_html_render.py -x --tb=short -m unit` -- all tests pass
2. `ls docs/developer/*.html | wc -l` -- shows 9 (4 from plan 01 + 5 from plan 02)
3. `grep -c "Medallion" docs/developer/etl-patterns.html` -- confirms ETL patterns content
4. `grep "localhost" docs/developer/service-urls.html` -- confirms dynamic service URLs rendered
5. `grep -c "details" docs/developer/troubleshooting.html` -- confirms collapsible FAQ entries
</verification>

<success_criteria>
- ETL patterns page contains all 8 sections from etl-patterns.md converted to HTML
- Testing guide documents all 4 pytest markers with descriptions and includes output snippets
- CI/CD page documents all 5 workflows with a Mermaid promotion flowchart
- Service URL table renders service data from extract_services() (not hardcoded)
- Troubleshooting FAQ has 10 entries in Symptom-Fix-Why format with collapsible details
- All tests pass including both new content tests and existing regression tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-developer-documentation/07-02-SUMMARY.md`
</output>
