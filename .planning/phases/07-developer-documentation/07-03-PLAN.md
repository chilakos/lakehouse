---
phase: 07-developer-documentation
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - docs/developer/data/api-reference.yml
  - docs/developer/data/contributor.yml
  - docs/developer/data/class-hierarchy.yml
  - docs/developer/data/dev-index.yml
  - docs/developer/diagrams/class-hierarchy.mmd
  - docs/developer/api-reference.html
  - docs/developer/contributor.html
  - docs/developer/class-hierarchy.html
  - docs/developer/index.html
  - docs/render_html.py
  - etl/tests/test_html_render.py
autonomous: true
requirements: [DEV-10, DEV-11, DEV-12]

must_haves:
  truths:
    - "API reference documents all 8 packages (pipelines, config, governance, quality, semantic, iceberg_utils, lineage, inventory) with class/function signatures, import paths, and usage examples"
    - "Class hierarchy visualization shows BasePipeline inheritance tree with all 7 concrete pipelines plus supporting classes"
    - "Contributor guidelines specify branch naming, PR process, ruff style, pytest markers, and commit format with rationale for each rule"
    - "Developer docs index page links all 12 developer doc pages with audience-tagged cards (New Engineers, All Engineers, Contributors)"
  artifacts:
    - path: "docs/developer/api-reference.html"
      provides: "DEV-10 API reference for all 8 packages"
      contains: "BasePipeline"
    - path: "docs/developer/class-hierarchy.html"
      provides: "DEV-11 class hierarchy visualization"
      contains: "BasePipeline"
    - path: "docs/developer/contributor.html"
      provides: "DEV-12 contributor guidelines"
      contains: "ruff"
    - path: "docs/developer/index.html"
      provides: "Developer docs index with card links to all 12 pages"
      contains: "card-grid"
    - path: "docs/developer/diagrams/class-hierarchy.mmd"
      provides: "Mermaid classDiagram source"
      contains: "classDiagram"
    - path: "docs/render_html.py"
      provides: "extract_package_api() AST-based extraction + render_dev_index()"
      contains: "extract_package_api"
  key_links:
    - from: "docs/render_html.py"
      to: "etl/src/"
      via: "extract_package_api() parses Python source files with ast module"
      pattern: "ast.parse"
    - from: "docs/developer/index.html"
      to: "docs/developer/*.html"
      via: "Card links to all 12 developer doc pages"
      pattern: "href"
    - from: "docs/developer/data/contributor.yml"
      to: ".pre-commit-config.yaml"
      via: "Content derived from pre-commit hook configuration"
      pattern: "ruff"
---

<objective>
Create the API/module reference (DEV-10), class hierarchy visualization (DEV-11), contributor guidelines (DEV-12), and the developer docs index page linking all 12 pages with audience-tagged cards.

Purpose: Completes the developer documentation suite with the "Contributors" content batch (API reference, code style, PR process) and the index page that ties all 12 developer docs together for navigation.

Output: AST-based API extraction function, 4 YAML data files, 1 Mermaid diagram, 4 rendered HTML pages (api-reference, class-hierarchy, contributor, index), render_dev_index() function, and tests.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-developer-documentation/07-CONTEXT.md
@.planning/phases/07-developer-documentation/07-RESEARCH.md
@.planning/phases/07-developer-documentation/07-01-SUMMARY.md
@.planning/phases/07-developer-documentation/07-02-SUMMARY.md

<interfaces>
<!-- Key assets the executor needs -->

From docs/render_html.py (available after Plans 07-01/02):
```python
def render_developer_docs(...) -> list[Path]:
    """Renders all YAML data files in docs/developer/data/ to HTML."""

# Constants:
DEV_DATA_DIR = PROJECT_ROOT / "docs" / "developer" / "data"
DEV_DIAGRAM_DIR = PROJECT_ROOT / "docs" / "developer" / "diagrams"
DEV_OUTPUT_DIR = PROJECT_ROOT / "docs" / "developer"
```

From RESEARCH -- Pipeline Class Hierarchy (for DEV-11 Mermaid diagram):
```
BasePipeline (abstract) -- etl/src/pipelines/base.py
  +-- TradesBronzePipeline (bronze/trades_ingest.py)
  +-- PositionsBronzePipeline (bronze/positions_ingest.py)
  +-- MainframeBronzePipeline (bronze/mainframe_ingest.py)
  +-- TradesSilverPipeline (silver/trades_clean.py)
  +-- PositionsSilverPipeline (silver/positions_clean.py)
  +-- TradingMetricsGoldPipeline (gold/trading_metrics.py)
  +-- RiskExposureGoldPipeline (gold/risk_exposure.py)
Supporting: PipelineConfig, MedallionLayer, SchemaValidationError, QualityGateError, IncrementalConfig
```

From RESEARCH -- Codebase API Size (for DEV-10):
| Package | Files | Classes | Functions |
|---------|-------|---------|-----------|
| config | 1 | 1 | 0 |
| governance | 8 | 9 | 20 |
| iceberg_utils | 4 | 1 | 19 |
| inventory | 2 | 3 | 0 |
| lineage | 1 | 0 | 1 |
| pipelines | 9 | 14 | 4 |
| quality | 2 | 2 | 2 |
| semantic | 6 | 4 | 13 |
| Total | 33 | 34 | 59 |

From RESEARCH -- AST extraction pattern:
```python
def extract_module_api(source_path: Path) -> dict:
    tree = ast.parse(source_path.read_text())
    # Extract classes, functions, docstrings, args from AST nodes
```

From docs/templates/base_arch_index.html (for developer index page):
- Card grid pattern with audience badges
- Audience classes to add: .audience-new-engineers, .audience-all-engineers, .audience-contributors

From RESEARCH -- CI workflow data (for DEV-12 contributor guidelines):
- .pre-commit-config.yaml: 4 hook repos (ruff, trailing-whitespace, yaml/toml, detect-secrets, terraform fmt)
- pyproject.toml: ruff line-length, select rules, pytest markers
- ci.yml: lint, test, validate gates
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extract_package_api() function, API reference YAML, class hierarchy diagram, and contributor guidelines YAML</name>
  <files>
    docs/render_html.py,
    docs/developer/data/api-reference.yml,
    docs/developer/data/contributor.yml,
    docs/developer/data/class-hierarchy.yml,
    docs/developer/diagrams/class-hierarchy.mmd
  </files>
  <action>
    1. Add `extract_package_api(package_dir: Path) -> dict` function to `docs/render_html.py`:
       - Use Python `ast` module to parse source files (NOT runtime imports -- avoids PySpark dependency per RESEARCH Pitfall 2)
       - Walk all .py files in package_dir (excluding __pycache__, __init__.py files with no content)
       - For each file, extract:
         - Public classes (not starting with _): name, docstring, base classes, public methods (name, args excluding self, docstring, return annotation if present)
         - Public functions (not starting with _): name, args, docstring, return annotation if present
         - Type annotations where available (use ast.unparse for annotation nodes)
       - Return structured dict: `{"package_name": str, "modules": [{"name": str, "path": str, "classes": [...], "functions": [...]}]}`
       - Handle `ast.parse()` failures gracefully (skip unparseable files with warning)

    2. Add `extract_all_apis(etl_src_dir: Path) -> list[dict]` helper that calls extract_package_api() for each of the 8 packages:
       - pipelines, config, governance, quality, semantic, iceberg_utils, lineage, inventory
       - Returns list of package API dicts

    3. Update `render_developer_docs()` to call `extract_all_apis()` when rendering api-reference data file:
       - Pass `api_packages` to template context so api-reference.yml can reference it
       - The ETL source dir: `PROJECT_ROOT / "etl" / "src"`

    4. Create `docs/developer/data/api-reference.yml` (DEV-10) with page_type: reference:
       - title: "API and Module Reference"
       - subtitle: "All 8 ETL Packages -- Public APIs, Signatures, and Usage"
       - The template should iterate over `api_packages` context variable (populated dynamically by extract_all_apis())
       - For each package: show package name, module listing, and for each class/function: name, signature, docstring, one usage example
       - Usage examples: provide a static `examples` dict in the YAML keyed by class/function name with representative usage snippets showing full import paths (from src.pipelines.base import BasePipeline, etc.)
       - Include all 8 packages per user decision: pipelines, config, governance, quality, semantic, iceberg_utils, lineage, inventory

    5. Create `docs/developer/diagrams/class-hierarchy.mmd` (DEV-11) -- Mermaid classDiagram per user decision:
       ```
       classDiagram
           class BasePipeline {
               <<abstract>>
               +extract() DataFrame
               +transform(df) DataFrame
               +validate_schema(df) bool
               +execute() dict
               +write(df)
           }
           class PipelineConfig {
               <<frozen dataclass>>
               +name: str
               +layer: MedallionLayer
               +source_path: str
               +target_table: str
           }
           class MedallionLayer {
               <<enum>>
               BRONZE
               SILVER
               GOLD
           }
           BasePipeline <|-- TradesBronzePipeline
           BasePipeline <|-- PositionsBronzePipeline
           BasePipeline <|-- MainframeBronzePipeline
           BasePipeline <|-- TradesSilverPipeline
           BasePipeline <|-- PositionsSilverPipeline
           BasePipeline <|-- TradingMetricsGoldPipeline
           BasePipeline <|-- RiskExposureGoldPipeline
       ```
       - Verify class names match actual source code in etl/src/pipelines/

    6. Create `docs/developer/data/class-hierarchy.yml` (DEV-11) with page_type: visualization:
       - title: "Pipeline Class Hierarchy"
       - References class-hierarchy.mmd diagram (rendered to SVG at build time)
       - Narrative context sections: "Design Philosophy" (abstract base pattern, medallion layers), "Adding a New Pipeline" (extend BasePipeline, implement extract/transform), "Supporting Classes" (PipelineConfig, MedallionLayer enum, exception classes)

    7. Create `docs/developer/data/contributor.yml` (DEV-12) with page_type: reference:
       - title: "Contributor Guidelines"
       - Sections with rationale per user decision ("rules with brief rationale"):
         a) Branch Naming: `feature/TICKET-description` ("Why: CI parses branch prefix for environment routing")
         b) PR Process: create branch, make changes, run pre-commit, push, create PR, wait for CI, get review
         c) Code Style: ruff with project settings from pyproject.toml (line-length, select rules). Read actual ruff config
         d) Testing Requirements: all new code needs tests, use appropriate markers (@pytest.mark.unit, etc.), run `python -m pytest -m unit -x` before pushing
         e) Pre-commit Hooks: list all hooks from .pre-commit-config.yaml (ruff check, ruff format, trailing-whitespace, end-of-file-fixer, check-yaml, check-toml, detect-secrets, terraform fmt)
         f) Commit Format: conventional commits (feat, fix, test, refactor, docs)
         g) Naming Conventions: classes=PascalCase, functions=snake_case, constants=UPPER_SNAKE, test files=test_*.py
       - Read .pre-commit-config.yaml and etl/pyproject.toml for accurate configuration values
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && python -c "from docs.render_html import extract_package_api; from pathlib import Path; api = extract_package_api(Path('etl/src/pipelines')); print(f'Classes: {len(api[\"modules\"])} modules'); assert len(api['modules']) > 0" && echo "AST extraction works"</automated>
  </verify>
  <done>
    extract_package_api() parses all 8 ETL packages via AST without runtime imports. API reference YAML references dynamic api_packages data. Class hierarchy Mermaid diagram shows all 7 concrete pipelines inheriting from BasePipeline. Contributor guidelines include all pre-commit hooks and ruff config with rationale. All YAML files parse without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render HTML pages, create developer docs index, and add all remaining tests</name>
  <files>
    docs/developer/api-reference.html,
    docs/developer/class-hierarchy.html,
    docs/developer/contributor.html,
    docs/developer/index.html,
    docs/developer/data/dev-index.yml,
    docs/render_html.py,
    etl/tests/test_html_render.py
  </files>
  <action>
    1. Create `docs/developer/data/dev-index.yml` -- data file for the developer docs index page:
       - title: "Developer Documentation"
       - subtitle: "Guides, References, and Tools for Lakehouse Engineers"
       - pages (all 12, grouped by audience per user decision):
         New Engineers:
           - Getting Started (onboarding.html) -- "Prerequisites, local setup, Docker Compose launch"
           - Repository Structure (repo-structure.html) -- "Directory layout and key files"
           - Write Your First Pipeline (first-pipeline.html) -- "Step-by-step CSV-to-Bronze tutorial"
           - Day 1 Checklist (day1-checklist.html) -- "Printable onboarding checklist"
         All Engineers:
           - ETL Patterns Reference (etl-patterns.html) -- "Medallion, quality, DAGs, incremental, mainframe"
           - Testing Guide (testing.html) -- "Unit tests, markers, CI gates, pytest patterns"
           - CI/CD Workflow (cicd.html) -- "PR to production promotion path"
           - Service URLs (service-urls.html) -- "All platform services with ports and credentials"
           - Troubleshooting FAQ (troubleshooting.html) -- "Common issues with Symptom-Fix-Why format"
         Contributors:
           - API Reference (api-reference.html) -- "All 8 packages with signatures and examples"
           - Pipeline Class Hierarchy (class-hierarchy.html) -- "BasePipeline inheritance visualization"
           - Contributor Guidelines (contributor.html) -- "Branch naming, PR process, code style, testing"

    2. Add `render_dev_index()` function to `docs/render_html.py`:
       - Similar to render_arch_index(): loads dev-index.yml, renders through a template
       - Reuse base_arch_index.html template pattern but with developer-specific audience badges
       - OR: render through base_developer.html with page_type that handles card grid layout
       - Add audience CSS classes for developer docs: `.audience-new-engineers` (green #059669), `.audience-all-engineers` (blue #2563eb), `.audience-contributors` (purple #7c3aed)
       - Output to docs/developer/index.html

    3. Render all pages:
       - Call render_developer_docs() to render api-reference.html, class-hierarchy.html, contributor.html (plus re-render all existing pages)
       - Call render_dev_index() to render docs/developer/index.html

    4. Add tests to `etl/tests/test_html_render.py`:
       - `test_developer_api_reference` (DEV-10): HTML contains all 8 package names (pipelines, config, governance, quality, semantic, iceberg_utils, lineage, inventory), "BasePipeline" class entry, at least one function signature
       - `test_developer_class_hierarchy` (DEV-11): HTML contains Mermaid SVG (or placeholder SVG), "BasePipeline", "TradesBronzePipeline"
       - `test_developer_contributor` (DEV-12): HTML contains "ruff", "pytest", branch naming convention, pre-commit hook names
       - `test_developer_index`: HTML contains card-grid, links to all 12 pages (check for key filenames: onboarding.html, api-reference.html, troubleshooting.html), audience badge text
       - `test_extract_package_api`: Unit test for extract_package_api() function -- parse etl/src/config/ (simplest package, 1 class), verify returns dict with classes list containing "Settings"

    5. Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/test_html_render.py -x --tb=short -m unit</automated>
  </verify>
  <done>
    API reference page documents all 8 packages with class/function signatures from AST extraction. Class hierarchy page shows Mermaid SVG (or placeholder) with BasePipeline tree. Contributor guidelines page has complete PR workflow with rationale. Developer docs index links all 12 pages with audience-tagged cards. All tests pass. `ls docs/developer/*.html | wc -l` shows 13 (12 doc pages + 1 index).
  </done>
</task>

</tasks>

<verification>
1. `cd /home/azureuser/lakehouse/etl && python -m pytest tests/test_html_render.py -x --tb=short -m unit` -- all tests pass
2. `ls docs/developer/*.html | wc -l` -- shows 13 (12 content pages + 1 index)
3. `grep -c "BasePipeline" docs/developer/api-reference.html` -- confirms API content
4. `grep "pipelines\|config\|governance\|quality\|semantic\|iceberg_utils\|lineage\|inventory" docs/developer/api-reference.html | head -8` -- all 8 packages present
5. `grep "card-grid" docs/developer/index.html` -- confirms card layout
6. `grep -c "href" docs/developer/index.html` -- at least 12 links
7. `cd /home/azureuser/lakehouse/etl && python -m pytest tests/ -x --tb=short -m unit` -- full unit suite green
</verification>

<success_criteria>
- API reference documents all 8 packages (34 classes, 59 functions) with signatures extracted via AST
- All code examples show full import paths (from src.pipelines.base import BasePipeline)
- Class hierarchy Mermaid diagram shows all 7 concrete pipelines inheriting from BasePipeline
- Contributor guidelines include branch naming, ruff config, pytest markers, pre-commit hooks, commit format -- each with brief rationale
- Developer docs index page has 12 cards grouped by audience (New Engineers, All Engineers, Contributors)
- All 13 HTML files in docs/developer/ are standalone with embedded CSS
- All tests pass including full regression suite
</success_criteria>

<output>
After completion, create `.planning/phases/07-developer-documentation/07-03-SUMMARY.md`
</output>
