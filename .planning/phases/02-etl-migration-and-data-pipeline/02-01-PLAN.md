---
phase: 02-etl-migration-and-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - etl/src/pipelines/__init__.py
  - etl/src/pipelines/base.py
  - etl/src/pipelines/bronze/__init__.py
  - etl/src/pipelines/bronze/trades_ingest.py
  - etl/src/pipelines/silver/__init__.py
  - etl/src/pipelines/silver/trades_clean.py
  - etl/src/pipelines/gold/__init__.py
  - etl/src/pipelines/gold/trading_metrics.py
  - etl/src/config/settings.py
  - etl/pyproject.toml
  - etl/tests/unit/test_base_pipeline.py
  - etl/tests/unit/test_schema_validation.py
  - etl/tests/integration/test_medallion_layers.py
autonomous: true
requirements:
  - FNDTN-07
  - ETL-01
  - QUAL-01

must_haves:
  truths:
    - "Bronze/Silver/Gold namespaces exist in Nessie catalog with correct naming convention (lakehouse.bronze.*, lakehouse.silver.*, lakehouse.gold.*)"
    - "ETL base class enforces extract/transform/validate_schema/write contract -- subclasses cannot skip schema validation"
    - "Schema validation rejects DataFrames that do not match the target StructType before Iceberg writes"
    - "Bronze layer adds source_system, ingestion_ts, batch_id metadata columns to every ingested record"
    - "A trade record flows from Bronze (raw) through Silver (cleaned/deduped) to Gold (aggregated metrics) via pipeline subclasses"
  artifacts:
    - path: "etl/src/pipelines/base.py"
      provides: "BasePipeline ABC with MedallionLayer enum, PipelineConfig, execute() orchestration"
      exports: ["BasePipeline", "PipelineConfig", "MedallionLayer", "SchemaValidationError", "QualityGateError"]
      min_lines: 80
    - path: "etl/src/pipelines/bronze/trades_ingest.py"
      provides: "Concrete Bronze pipeline for trades ingestion with metadata columns"
      min_lines: 30
    - path: "etl/src/pipelines/silver/trades_clean.py"
      provides: "Concrete Silver pipeline for trades cleaning and deduplication"
      min_lines: 30
    - path: "etl/src/pipelines/gold/trading_metrics.py"
      provides: "Concrete Gold pipeline for pre-aggregated trading metrics"
      min_lines: 30
    - path: "etl/tests/unit/test_base_pipeline.py"
      provides: "Unit tests for BasePipeline contract enforcement"
      min_lines: 40
    - path: "etl/tests/unit/test_schema_validation.py"
      provides: "Unit tests for schema validation accept/reject behavior"
      min_lines: 30
  key_links:
    - from: "etl/src/pipelines/base.py"
      to: "etl/src/iceberg_utils/catalog.py"
      via: "SparkSession from get_spark_session(), write via df.writeTo()"
      pattern: "writeTo.*append|overwritePartitions"
    - from: "etl/src/pipelines/bronze/trades_ingest.py"
      to: "etl/src/pipelines/base.py"
      via: "extends BasePipeline, implements extract() and transform()"
      pattern: "class.*BasePipeline"
    - from: "etl/src/pipelines/base.py"
      to: "etl/src/config/settings.py"
      via: "Settings for Nessie/S3 configuration"
      pattern: "Settings|from.*config.*import"
---

<objective>
Build the ETL framework core: BasePipeline abstract class with medallion layer enforcement, schema validation before writes, and concrete Bronze/Silver/Gold pipeline implementations for trades.

Purpose: This is the foundation that all subsequent ETL work depends on. The base class pattern enforces consistency across the 40+ engineer team (ETL-06 standardization starts here). Medallion namespaces (FNDTN-07) and schema validation (QUAL-01) are baked into the execution contract so they cannot be skipped.

Output: Working ETL base class with three concrete pipeline implementations (trades Bronze/Silver/Gold), unit tests for contract enforcement and schema validation, integration test for end-to-end medallion flow.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-CONTEXT.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-RESEARCH.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->

From etl/src/iceberg_utils/catalog.py:
```python
def get_spark_session(
    nessie_uri: str | None = None,
    warehouse: str | None = None,
    s3_endpoint: str | None = None,
    app_name: str = "lakehouse-etl",
) -> SparkSession:

def create_namespace(spark: SparkSession, namespace: str) -> None:

def create_iceberg_table(
    spark: SparkSession, namespace: str, table_name: str,
    schema: StructType, location: str,
    partition_by: list[str] | None = None,
) -> None:

def write_data(spark: SparkSession, namespace: str, table_name: str,
    data: list[dict], schema: StructType) -> None:

def read_table(spark: SparkSession, namespace: str, table_name: str) -> DataFrame:
```

From etl/src/config/settings.py:
```python
@dataclass(frozen=True)
class Settings:
    nessie_uri: str  # default "http://localhost:19120"
    nessie_warehouse: str  # default "lakehouse"
    minio_endpoint: str  # default "http://localhost:9000"
    minio_access_key: str  # default "admin"
    minio_secret_key: str  # default "admin123456"
    environment: str  # default "dev"

    @classmethod
    def from_env(cls) -> "Settings":
```

From etl/src/synthetic/generators.py:
```python
def generate_trades(num_records: int, seed: int = 42) -> list[dict]:
def trades_schema() -> StructType:
# Fields: trade_id, trade_date, symbol, side, trade_type, quantity, price, notional, account_id, trader_id, exchange, settlement_date
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ETL base class, medallion types, and pipeline config</name>
  <files>etl/src/pipelines/__init__.py, etl/src/pipelines/base.py, etl/src/config/settings.py, etl/pyproject.toml, etl/tests/unit/test_base_pipeline.py, etl/tests/unit/test_schema_validation.py</files>
  <behavior>
    - Test: BasePipeline cannot be instantiated directly (ABC enforcement)
    - Test: A concrete subclass that implements extract() and transform() can be instantiated
    - Test: execute() calls extract -> transform -> validate_schema -> write in order
    - Test: validate_schema returns True when DataFrame schema matches target_schema
    - Test: validate_schema returns False when DataFrame has missing columns
    - Test: validate_schema returns False when DataFrame has wrong column types
    - Test: execute() raises SchemaValidationError when schema validation fails (QUAL-01)
    - Test: execute() raises QualityGateError when critical quality checks fail
    - Test: add_metadata_columns adds source_system, ingestion_ts, batch_id columns for Bronze
    - Test: MedallionLayer enum has BRONZE, SILVER, GOLD values
    - Test: PipelineConfig correctly maps target_layer to namespace (bronze -> lakehouse.bronze.{table})
  </behavior>
  <action>
    1. Add `soda-core-spark-df>=3.5.0` to pyproject.toml dependencies (needed by quality gate in base class -- actual Soda integration comes in Plan 04, but the import path must exist).

    2. Extend `etl/src/config/settings.py` with Airflow and lineage settings:
       - `openlineage_url`: env OPENLINEAGE_URL, default "http://localhost:5000"
       - `openlineage_namespace`: env OPENLINEAGE_NAMESPACE, default "lakehouse"
       - `airflow_home`: env AIRFLOW_HOME, default "/opt/airflow"

    3. Create `etl/src/pipelines/__init__.py` re-exporting BasePipeline, PipelineConfig, MedallionLayer.

    4. Create `etl/src/pipelines/base.py` with:
       - `MedallionLayer` enum: BRONZE, SILVER, GOLD
       - `SchemaValidationError(Exception)` and `QualityGateError(Exception)` custom exceptions
       - `PipelineConfig` frozen dataclass: name, source_layer (MedallionLayer | None), target_layer (MedallionLayer), target_table (str), target_schema (StructType -- use TYPE_CHECKING), quality_checks_path (str), critical_checks (list[str]), max_retries (int=3), retry_delay_seconds (int=60)
       - `BasePipeline` ABC with:
         - `__init__(self, spark, config)` storing spark and config
         - `extract() -> DataFrame` (abstractmethod)
         - `transform(df) -> DataFrame` (abstractmethod)
         - `validate_schema(df) -> bool` comparing df.schema fields against config.target_schema fields (compare field names and types, allow nullable differences)
         - `add_metadata_columns(df, source_system, batch_id) -> DataFrame` adding source_system (lit), ingestion_ts (current_timestamp), batch_id (lit) columns -- used by Bronze pipelines
         - `write(df) -> None` writing to `lakehouse.{target_layer.value}.{target_table}` via df.writeTo().append()
         - `run_quality_checks(df) -> dict` placeholder returning {"passed": True, "critical_failures": [], "warnings": []} -- real Soda integration in Plan 04
         - `execute() -> dict` orchestrating: extract -> transform -> validate_schema (raise SchemaValidationError on failure) -> run_quality_checks (raise QualityGateError on critical failures) -> write -> return {"rows_written": count, "quality": qc_results}
       - Use TYPE_CHECKING pattern for PySpark imports (consistent with catalog.py pattern)

    5. Write `etl/tests/unit/test_base_pipeline.py` -- test the base class contract:
       - Create a MockPipeline(BasePipeline) that returns a hardcoded DataFrame from extract/transform
       - Test ABC enforcement (cannot instantiate BasePipeline directly)
       - Test execute() happy path returns correct dict
       - Test execute() raises SchemaValidationError on schema mismatch
       - Use pytest.mark.unit marker
       - Mock SparkSession where needed (unit tests should not need Docker)

    6. Write `etl/tests/unit/test_schema_validation.py` -- test schema validation logic:
       - Test matching schema passes
       - Test missing column fails
       - Test wrong type fails
       - Test extra columns in DataFrame are accepted (additive is OK)
       - Use pytest.mark.unit marker
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_base_pipeline.py tests/unit/test_schema_validation.py -x --tb=short -v</automated>
  </verify>
  <done>BasePipeline ABC enforces extract/transform/validate/write contract. SchemaValidationError raised on schema mismatch. MedallionLayer enum and PipelineConfig properly configured. All unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bronze/Silver/Gold pipeline implementations and medallion integration test</name>
  <files>etl/src/pipelines/bronze/__init__.py, etl/src/pipelines/bronze/trades_ingest.py, etl/src/pipelines/silver/__init__.py, etl/src/pipelines/silver/trades_clean.py, etl/src/pipelines/gold/__init__.py, etl/src/pipelines/gold/trading_metrics.py, etl/tests/integration/test_medallion_layers.py</files>
  <behavior>
    - Test: Bronze trades pipeline adds source_system, ingestion_ts, batch_id columns
    - Test: Bronze trades pipeline preserves all original fields without transformation
    - Test: Silver trades pipeline deduplicates by trade_id (keeps latest)
    - Test: Silver trades pipeline produces entity-centric output (one row per trade_id)
    - Test: Gold trading metrics pipeline produces aggregated metrics (total notional by symbol/side)
    - Test: End-to-end flow: synthetic data -> Bronze write -> Silver clean -> Gold aggregate, all in Iceberg tables
    - Test: Namespaces lakehouse.bronze, lakehouse.silver, lakehouse.gold all exist and are queryable
  </behavior>
  <action>
    1. Create `etl/src/pipelines/bronze/__init__.py` and `etl/src/pipelines/bronze/trades_ingest.py`:
       - `TradesBronzePipeline(BasePipeline)` with:
         - `__init__` accepting spark, source_data (list[dict] or DataFrame), source_system (str), batch_id (str)
         - PipelineConfig with target_layer=BRONZE, target_table="trades", target_schema extending trades_schema() with metadata columns
         - `extract()` creating DataFrame from source_data using trades_schema()
         - `transform(df)` calling self.add_metadata_columns(df, source_system, batch_id) -- Bronze is raw-as-is per locked decision

    2. Create `etl/src/pipelines/silver/__init__.py` and `etl/src/pipelines/silver/trades_clean.py`:
       - `TradesSilverPipeline(BasePipeline)` with:
         - `__init__` accepting spark and source table name (reads from Bronze)
         - PipelineConfig with source_layer=BRONZE, target_layer=SILVER, target_table="trades"
         - `extract()` reading from lakehouse.bronze.trades
         - `transform(df)` deduplicating by trade_id (window function, keep most recent ingestion_ts), dropping metadata columns (source_system, ingestion_ts, batch_id) from Bronze, applying business rule: ensure price > 0 and quantity > 0

    3. Create `etl/src/pipelines/gold/__init__.py` and `etl/src/pipelines/gold/trading_metrics.py`:
       - `TradingMetricsGoldPipeline(BasePipeline)` with:
         - `__init__` accepting spark
         - PipelineConfig with source_layer=SILVER, target_layer=GOLD, target_table="trading_metrics"
         - `extract()` reading from lakehouse.silver.trades
         - `transform(df)` computing per-symbol/side aggregates: total_notional (sum), trade_count (count), avg_price (avg), min_price, max_price. Pre-aggregated metrics for BI per locked Gold decision.

    4. Write `etl/tests/integration/test_medallion_layers.py`:
       - Uses @pytest.mark.integration marker
       - Fixture creates bronze/silver/gold namespaces via catalog.create_namespace()
       - Test creates Iceberg tables in each namespace
       - Test runs trades through Bronze -> Silver -> Gold pipelines
       - Asserts: Bronze has metadata columns, Silver is deduped, Gold has aggregates
       - Asserts: All three namespaces follow lakehouse.{layer}.{table} convention
       - Uses synthetic generators for test data (generate_trades with seed)
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_base_pipeline.py tests/unit/test_schema_validation.py tests/integration/test_medallion_layers.py -x --tb=short -v</automated>
  </verify>
  <done>Three concrete pipeline implementations (Bronze/Silver/Gold) for trades. Bronze adds metadata columns. Silver deduplicates. Gold aggregates. Integration test proves end-to-end medallion flow with Iceberg tables in correct namespaces. FNDTN-07, ETL-01, QUAL-01 requirements satisfied.</done>
</task>

</tasks>

<verification>
1. Unit tests pass: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_base_pipeline.py tests/unit/test_schema_validation.py -x --tb=short`
2. Integration tests pass: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/integration/test_medallion_layers.py -x --tb=short`
3. All pipeline classes importable: `cd /home/azureuser/lakehouse/etl && python -c "from src.pipelines.base import BasePipeline, MedallionLayer, PipelineConfig; from src.pipelines.bronze.trades_ingest import TradesBronzePipeline; from src.pipelines.silver.trades_clean import TradesSilverPipeline; from src.pipelines.gold.trading_metrics import TradingMetricsGoldPipeline; print('All imports OK')"`
</verification>

<success_criteria>
- BasePipeline ABC prevents direct instantiation and enforces extract/transform/validate_schema/write contract
- SchemaValidationError raised when DataFrame schema does not match target (QUAL-01)
- Bronze pipeline adds source_system, ingestion_ts, batch_id metadata columns (locked decision)
- Silver pipeline deduplicates and produces entity-centric output (locked decision)
- Gold pipeline produces pre-aggregated metrics (locked decision)
- Namespaces follow lakehouse.bronze.*, lakehouse.silver.*, lakehouse.gold.* convention (FNDTN-07, locked decision)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-etl-migration-and-data-pipeline/02-01-SUMMARY.md`
</output>
