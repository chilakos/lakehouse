---
phase: 02-etl-migration-and-data-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker-compose.test.yml
  - etl/dags/__init__.py
  - etl/dags/example_dag.py
  - etl/src/lineage/__init__.py
  - etl/src/lineage/config.py
  - etl/src/iceberg_utils/catalog.py
  - etl/tests/unit/test_dag_integrity.py
  - etl/tests/integration/test_lineage_capture.py
  - infra/docker/airflow/Dockerfile
  - infra/docker/airflow/airflow.cfg
  - infra/docker/airflow/requirements.txt
autonomous: true
requirements:
  - ETL-04
  - GOVN-01

must_haves:
  truths:
    - "Airflow 3.1.x is running locally via Docker Compose with CeleryExecutor, Redis broker, and Postgres backend"
    - "Marquez backend is running and accepting OpenLineage events via HTTP on port 5000"
    - "Airflow DAGs load without import errors and appear in the Airflow UI"
    - "Spark jobs emit OpenLineage events that appear in Marquez with correct namespace and table references"
    - "Airflow provider auto-injects parent job info into Spark OpenLineage events for task-level lineage"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Extended Docker Compose with Airflow (webserver, scheduler, worker, redis) + Marquez services"
      contains: "airflow-webserver"
    - path: "etl/src/lineage/config.py"
      provides: "OpenLineage configuration helper for Spark sessions"
      exports: ["get_openlineage_spark_config", "OPENLINEAGE_NAMESPACE"]
    - path: "etl/dags/example_dag.py"
      provides: "Example Airflow DAG using Airflow 3.x patterns with SparkSubmitOperator"
      min_lines: 30
    - path: "etl/tests/unit/test_dag_integrity.py"
      provides: "DAG import integrity test ensuring all DAGs load without errors"
      min_lines: 20
    - path: "infra/docker/airflow/Dockerfile"
      provides: "Custom Airflow image with OpenLineage provider and Spark provider"
      min_lines: 10
  key_links:
    - from: "etl/src/lineage/config.py"
      to: "etl/src/iceberg_utils/catalog.py"
      via: "OpenLineage Spark configs added to SparkSession builder"
      pattern: "openlineage|OpenLineageSparkListener"
    - from: "etl/dags/example_dag.py"
      to: "etl/src/pipelines"
      via: "SparkSubmitOperator submitting pipeline scripts"
      pattern: "SparkSubmitOperator"
    - from: "docker-compose.yml"
      to: "infra/docker/airflow/Dockerfile"
      via: "Docker Compose builds custom Airflow image"
      pattern: "build.*airflow"
---

<objective>
Deploy Airflow 3.1.x and Marquez into the Docker Compose development environment, configure OpenLineage for dual lineage capture (Airflow plugin + Spark agent), and create DAG infrastructure.

Purpose: Airflow orchestration (ETL-04) and OpenLineage lineage capture (GOVN-01) are infrastructure concerns independent of the ETL framework logic. Standing these up in parallel with Plan 01 means Wave 2 plans can immediately wire pipelines into orchestrated, lineage-tracked DAGs.

Output: Extended Docker Compose with Airflow + Marquez services, OpenLineage configuration for Spark sessions, example DAG, DAG integrity tests, lineage capture integration test.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-CONTEXT.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-RESEARCH.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->

From etl/src/iceberg_utils/catalog.py:
```python
def get_spark_session(
    nessie_uri: str | None = None,
    warehouse: str | None = None,
    s3_endpoint: str | None = None,
    app_name: str = "lakehouse-etl",
) -> SparkSession:
    # Current spark.jars.packages includes only Iceberg runtime
    # Must be extended with OpenLineage spark agent
```

From etl/src/config/settings.py:
```python
@dataclass(frozen=True)
class Settings:
    nessie_uri: str
    nessie_warehouse: str
    minio_endpoint: str
    environment: str
    @classmethod
    def from_env(cls) -> "Settings":
```

From docker-compose.yml (existing services):
```yaml
services:
  postgres:     # Port 5432, DB: nessie
  minio:        # Port 9000/9001
  minio-init:   # Bucket creation
  nessie:       # Port 19120
  trino:        # Port 8080
volumes:
  pgdata:
  miniodata:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Airflow + Marquez Docker Compose and OpenLineage configuration</name>
  <files>docker-compose.yml, docker-compose.test.yml, infra/docker/airflow/Dockerfile, infra/docker/airflow/airflow.cfg, infra/docker/airflow/requirements.txt, etl/src/lineage/__init__.py, etl/src/lineage/config.py, etl/src/iceberg_utils/catalog.py</files>
  <action>
    1. Create `infra/docker/airflow/Dockerfile`:
       - Base image: `apache/airflow:3.1.8-python3.11`
       - Install providers: `apache-airflow-providers-openlineage>=2.10.0`, `apache-airflow-providers-apache-spark>=4.0.0`
       - Install `soda-core-spark-df>=3.5.0` for quality checks in DAGs
       - Copy requirements.txt and pip install

    2. Create `infra/docker/airflow/requirements.txt` with the pip dependencies.

    3. Create `infra/docker/airflow/airflow.cfg` or use environment variables in Docker Compose for:
       - executor = CeleryExecutor
       - broker_url = redis://redis:6379/0
       - result_backend = db+postgresql://airflow:airflow@airflow-db:5432/airflow
       - OpenLineage transport: HTTP to http://marquez:5000, endpoint api/v1/lineage
       - OpenLineage namespace: "lakehouse"
       - spark_inject_parent_job_info = true
       - spark_inject_transport_info = true

    4. Extend `docker-compose.yml` with new services (add to existing file, do NOT remove existing services):
       - `airflow-db`: postgres:15, DB airflow/airflow, port 5433 (avoid conflict with existing postgres:5432)
       - `redis`: redis:7, port 6379
       - `airflow-webserver`: custom Dockerfile, port 8081 (avoid conflict with Trino:8080), depends_on airflow-db + redis
       - `airflow-scheduler`: same image, depends_on airflow-db + redis
       - `airflow-worker`: same image, depends_on airflow-db + redis
       - `airflow-init`: one-shot init service (airflow db init + create admin user)
       - `marquez`: marquezproject/marquez:0.50.0, port 5000 (API) + 3000 (UI)
       - `marquez-db`: postgres:15, DB marquez/marquez, port 5434
       - Mount `etl/dags/` as /opt/airflow/dags in Airflow containers
       - Mount `etl/src/` as /opt/airflow/etl_src for SparkSubmitOperator access
       - Set AIRFLOW__OPENLINEAGE__TRANSPORT and related env vars
       - Add volumes: airflow-db-data, marquez-db-data

    5. Extend `docker-compose.test.yml` similarly (add Airflow + Marquez services for integration tests).

    6. Create `etl/src/lineage/__init__.py` (empty).

    7. Create `etl/src/lineage/config.py`:
       - `OPENLINEAGE_NAMESPACE = "lakehouse"` constant
       - `get_openlineage_spark_config() -> dict` returning Spark config keys for OpenLineage:
         - spark.jars.packages addition: `io.openlineage:openlineage-spark_2.12:1.25.0`
         - spark.extraListeners: `io.openlineage.spark.agent.OpenLineageSparkListener`
         - spark.openlineage.transport.type: "http"
         - spark.openlineage.transport.url: from Settings or env var (default http://marquez:5000)
         - spark.openlineage.transport.endpoint: "api/v1/lineage"
         - spark.openlineage.namespace: "lakehouse"
       - Returns a dict of {config_key: value} that can be applied to SparkSession.builder

    8. Update `etl/src/iceberg_utils/catalog.py` get_spark_session():
       - Add optional parameter `enable_lineage: bool = False`
       - When enable_lineage=True, add OpenLineage spark configs from lineage.config.get_openlineage_spark_config()
       - Add openlineage-spark JAR to spark.jars.packages alongside Iceberg runtime
       - Default False to avoid breaking existing tests that do not have Marquez running
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && docker compose config --quiet 2>&1 && echo "Docker Compose config valid" && cd etl && python -c "from src.lineage.config import get_openlineage_spark_config, OPENLINEAGE_NAMESPACE; cfg = get_openlineage_spark_config(); assert 'spark.extraListeners' in cfg; print('Lineage config OK')"</automated>
  </verify>
  <done>Docker Compose extended with Airflow 3.1.x (webserver, scheduler, worker, redis) and Marquez (API + UI). OpenLineage configuration module provides Spark config dict. catalog.py supports enable_lineage parameter. Docker Compose validates without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Example DAG, DAG integrity tests, and lineage capture test</name>
  <files>etl/dags/__init__.py, etl/dags/example_dag.py, etl/tests/unit/test_dag_integrity.py, etl/tests/integration/test_lineage_capture.py</files>
  <behavior>
    - Test: All DAG files in etl/dags/ load without import errors
    - Test: Example DAG has correct dag_id, schedule, and default_args (retries=3, exponential backoff)
    - Test: Example DAG tasks follow correct dependency order (ingest -> quality_check -> transform)
    - Test: OpenLineage Spark config produces valid listener configuration
    - Test: Spark session with lineage enabled emits events (integration, requires Marquez)
  </behavior>
  <action>
    1. Create `etl/dags/__init__.py` (empty).

    2. Create `etl/dags/example_dag.py`:
       - Use Airflow 3.x patterns (airflow.sdk imports where applicable, TaskFlow API)
       - DAG id: "example_bronze_silver_trades"
       - Schedule: daily at 06:00 UTC
       - default_args per locked decision: retries=3, retry_delay=timedelta(minutes=2), retry_exponential_backoff=True, max_retry_delay=timedelta(minutes=30), on_failure_callback (placeholder function)
       - Tags: ["bronze", "silver", "trades", "example"]
       - Task 1: SparkSubmitOperator for Bronze ingestion
       - Task 2: SparkSubmitOperator for quality check
       - Task 3: SparkSubmitOperator for Silver transformation
       - Dependency: task1 >> task2 >> task3
       - Use Airflow Variables for connection URIs (var.value.nessie_iceberg_uri, var.value.marquez_url)
       - Spark conf includes OpenLineage config

    3. Write `etl/tests/unit/test_dag_integrity.py`:
       - Pytest fixture that imports all .py files from etl/dags/
       - Test that each file defines at least one DAG object
       - Test that DAG ids are unique across all files
       - Test that default_args include retries >= 3 (per locked decision)
       - Use @pytest.mark.unit marker
       - NOTE: This test must handle the case where Airflow is not installed by skipping gracefully (pytest.importorskip("airflow"))

    4. Write `etl/tests/integration/test_lineage_capture.py`:
       - Uses @pytest.mark.integration marker
       - Fixture creates SparkSession with enable_lineage=True
       - Test writes an Iceberg table and verifies OpenLineage event was emitted
       - Verify by querying Marquez API: GET http://marquez:5000/api/v1/namespaces/lakehouse/jobs
       - Skip gracefully if Marquez is not running (TCP probe or HTTP check)
       - This is a smoke test -- detailed lineage graph validation is a manual checkpoint
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_dag_integrity.py -x --tb=short -v</automated>
  </verify>
  <done>Example DAG loads without errors and follows Airflow 3.x patterns with correct retry/backoff config. DAG integrity test validates all DAGs. Lineage capture integration test verifies OpenLineage event emission to Marquez. ETL-04 and GOVN-01 infrastructure requirements satisfied.</done>
</task>

</tasks>

<verification>
1. Docker Compose validates: `cd /home/azureuser/lakehouse && docker compose config --quiet`
2. DAG integrity tests pass: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_dag_integrity.py -x --tb=short`
3. Lineage config importable: `cd /home/azureuser/lakehouse/etl && python -c "from src.lineage.config import get_openlineage_spark_config; print('OK')"`
4. catalog.py enable_lineage parameter works: `cd /home/azureuser/lakehouse/etl && python -c "import inspect; from src.iceberg_utils.catalog import get_spark_session; sig = inspect.signature(get_spark_session); assert 'enable_lineage' in sig.parameters; print('OK')"`
</verification>

<success_criteria>
- Airflow 3.1.x services defined in Docker Compose with CeleryExecutor, Redis, dedicated Postgres
- Marquez API (port 5000) and UI (port 3000) defined in Docker Compose
- OpenLineage configured: Airflow provider with spark_inject_parent_job_info=true, Spark agent via extraListeners
- Example DAG uses Airflow 3.x patterns with retries=3, exponential backoff (locked decision)
- DAG integrity test catches import errors
- catalog.py supports enable_lineage=True for Spark sessions that emit lineage
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-etl-migration-and-data-pipeline/02-02-SUMMARY.md`
</output>
