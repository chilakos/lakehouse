---
phase: 02-etl-migration-and-data-pipeline
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - etl/src/inventory/__init__.py
  - etl/src/inventory/catalog.py
  - etl/src/inventory/models.py
  - etl/dags/bronze_trades_dag.py
  - etl/dags/bronze_positions_dag.py
  - etl/dags/gold_trading_metrics_dag.py
  - etl/dags/maintenance_dag.py
  - etl/dags/quality_report_dag.py
  - infra/docker/grafana/dashboards/pipeline_observability.json
  - infra/docker/grafana/provisioning/dashboards.yml
  - infra/docker/grafana/provisioning/datasources.yml
  - docs/etl-patterns.md
  - etl/tests/unit/test_job_inventory.py
  - etl/tests/unit/test_etl_patterns.py
  - etl/tests/unit/test_dashboard_config.py
autonomous: false
requirements:
  - ETL-06
  - ETL-07
  - PLAT-02

must_haves:
  truths:
    - "DataStage job inventory model can classify jobs as simple/medium/complex with source systems, dependencies, and estimated effort"
    - "Standardized ETL patterns are documented with examples that 40+ engineers can follow"
    - "Production Airflow DAGs follow the hybrid pattern: source-specific Bronze-to-Silver DAGs, separate Gold DAGs"
    - "Grafana dashboard shows pipeline SLA status, failure rates, run durations, data freshness, and quality scores"
    - "All DAGs emit OpenLineage events for end-to-end lineage tracking"
  artifacts:
    - path: "etl/src/inventory/models.py"
      provides: "DataStage job inventory data model with complexity classification"
      exports: ["DataStageJob", "JobComplexity"]
      min_lines: 30
    - path: "etl/src/inventory/catalog.py"
      provides: "Inventory management utilities (load, save, filter, classify)"
      min_lines: 40
    - path: "etl/dags/bronze_trades_dag.py"
      provides: "Production trades Bronze-to-Silver DAG"
      min_lines: 30
    - path: "etl/dags/gold_trading_metrics_dag.py"
      provides: "Production Gold aggregation DAG"
      min_lines: 25
    - path: "infra/docker/grafana/dashboards/pipeline_observability.json"
      provides: "Grafana dashboard for pipeline observability (ops + data metrics)"
      min_lines: 50
    - path: "docs/etl-patterns.md"
      provides: "Standardized ETL patterns documentation for team onboarding"
      min_lines: 100
  key_links:
    - from: "etl/dags/bronze_trades_dag.py"
      to: "etl/src/pipelines/bronze/trades_ingest.py"
      via: "SparkSubmitOperator submitting pipeline script"
      pattern: "SparkSubmitOperator.*trades"
    - from: "etl/dags/gold_trading_metrics_dag.py"
      to: "etl/src/pipelines/gold/trading_metrics.py"
      via: "SparkSubmitOperator submitting Gold pipeline"
      pattern: "SparkSubmitOperator.*trading_metrics"
    - from: "infra/docker/grafana/dashboards/pipeline_observability.json"
      to: "docker-compose.yml"
      via: "Grafana service with provisioned dashboard"
      pattern: "grafana"
---

<objective>
Build production Airflow DAGs, DataStage job inventory, Grafana observability dashboard, and standardized ETL patterns documentation. This is the capstone plan that wires all prior work into production-ready artifacts.

Purpose: ETL-06 (standardized patterns) enables the 40+ engineer team to onboard. ETL-07 (job inventory) enables planning the full v2 DataStage migration. PLAT-02 (observability dashboard) gives operations visibility into pipeline health. Production DAGs demonstrate the complete pattern from orchestration through quality gates to lineage.

Output: 5 production DAGs, job inventory module, Grafana dashboard, ETL patterns documentation.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-CONTEXT.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-RESEARCH.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-01-SUMMARY.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-02-SUMMARY.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-03-SUMMARY.md
@.planning/phases/02-etl-migration-and-data-pipeline/02-04-SUMMARY.md

<interfaces>
<!-- From Plans 01-04 (all must be completed) -->
From etl/src/pipelines/base.py:
```python
class BasePipeline(ABC):
    # Full pipeline execution with schema validation + quality gates
    def execute(self) -> dict: ...

class MedallionLayer(Enum):
    BRONZE = "bronze"; SILVER = "silver"; GOLD = "gold"
```

From etl/src/pipelines/bronze/trades_ingest.py:
```python
class TradesBronzePipeline(BasePipeline): ...
```

From etl/src/pipelines/silver/trades_clean.py:
```python
class TradesSilverPipeline(BasePipeline): ...
```

From etl/src/pipelines/gold/trading_metrics.py:
```python
class TradingMetricsGoldPipeline(BasePipeline): ...
```

From etl/src/quality/scanner.py:
```python
def run_soda_checks(spark_session, df, checks_yaml_path, critical_check_names, data_source_name="spark_df") -> dict:
```

From etl/src/lineage/config.py:
```python
def get_openlineage_spark_config() -> dict:
OPENLINEAGE_NAMESPACE = "lakehouse"
```

From etl/src/iceberg_utils/catalog.py:
```python
def get_spark_session(..., enable_lineage: bool = False) -> SparkSession:
```

From etl/src/iceberg_utils/maintenance.py:
```python
def full_maintenance(spark, namespace, table_name, ...) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DataStage job inventory module and production DAGs</name>
  <files>etl/src/inventory/__init__.py, etl/src/inventory/catalog.py, etl/src/inventory/models.py, etl/dags/bronze_trades_dag.py, etl/dags/bronze_positions_dag.py, etl/dags/gold_trading_metrics_dag.py, etl/dags/maintenance_dag.py, etl/dags/quality_report_dag.py, etl/tests/unit/test_job_inventory.py</files>
  <behavior>
    - Test: DataStageJob model serializes to dict and deserializes from dict
    - Test: JobComplexity enum has SIMPLE, MEDIUM, COMPLEX values
    - Test: Job inventory can be loaded from and saved to JSON file
    - Test: Job inventory can filter by complexity, source_system, migration_status
    - Test: Job inventory can compute migration statistics (count by complexity, by status)
  </behavior>
  <action>
    1. Create `etl/src/inventory/__init__.py` (empty).

    2. Create `etl/src/inventory/models.py`:
       - `JobComplexity` enum: SIMPLE, MEDIUM, COMPLEX (per research code example)
       - `DataStageJob` dataclass with all fields from research: job_name, job_id, complexity (JobComplexity), source_systems (list[str]), target_tables (list[str]), dependencies (list[str]), estimated_effort_hours (float), has_mainframe_source (bool), transformation_description (str), schedule (str), avg_runtime_minutes (float), row_volume_estimate (int), migration_status (str), notes (str = "")
       - `to_dict() -> dict` and `@classmethod from_dict(cls, data) -> DataStageJob` methods
       - Per locked decision: Full structured catalog with complexity classification, source systems, dependencies, estimated effort per job

    3. Create `etl/src/inventory/catalog.py`:
       - `JobInventory` class:
         - `__init__(self, jobs: list[DataStageJob] = None)`
         - `add_job(job: DataStageJob)` / `remove_job(job_id: str)`
         - `filter_by_complexity(complexity: JobComplexity) -> list[DataStageJob]`
         - `filter_by_source_system(system: str) -> list[DataStageJob]`
         - `filter_by_status(status: str) -> list[DataStageJob]`
         - `get_migration_stats() -> dict` returning counts by complexity, by status, total estimated hours
         - `save_to_json(path: str)` / `@classmethod load_from_json(cls, path: str) -> JobInventory`
         - `classify_complexity(job: DataStageJob) -> JobComplexity`: applies rules -- SIMPLE: single source, no mainframe; MEDIUM: multi-source joins, lookups; COMPLEX: mainframe, COBOL, multi-step

    4. Create production Airflow DAGs (all following Airflow 3.x patterns):

       `etl/dags/bronze_trades_dag.py`:
       - DAG id: "bronze_silver_trades"
       - Schedule: "0 6 * * *" (daily 06:00 UTC)
       - Tags: ["bronze", "silver", "trades"]
       - default_args: retries=3, retry_delay=2min, exponential backoff, max_retry_delay=30min, on_failure_callback
       - Task 1: SparkSubmitOperator "ingest_trades_bronze" running trades_ingest.py
       - Task 2: SparkSubmitOperator "quality_check_bronze_trades" running quality scanner with bronze_trades.yml
       - Task 3: SparkSubmitOperator "transform_trades_silver" running trades_clean.py
       - Task 4: SparkSubmitOperator "quality_check_silver_trades" running quality scanner with silver_trades.yml
       - Dependency: task1 >> task2 >> task3 >> task4
       - Spark conf includes OpenLineage config from lineage.config

       `etl/dags/bronze_positions_dag.py`:
       - DAG id: "bronze_silver_positions"
       - Same pattern as trades but for positions pipeline

       `etl/dags/gold_trading_metrics_dag.py`:
       - DAG id: "gold_trading_metrics"
       - Schedule: "0 8 * * *" (daily 08:00 UTC, after Bronze/Silver complete)
       - Tags: ["gold", "metrics"]
       - Uses ExternalTaskSensor to wait for Bronze/Silver DAGs to complete
       - Task: SparkSubmitOperator running trading_metrics.py

       `etl/dags/maintenance_dag.py`:
       - DAG id: "iceberg_maintenance"
       - Schedule: "0 2 * * 0" (weekly Sunday 02:00 UTC)
       - Tags: ["maintenance", "iceberg"]
       - Tasks: compaction, snapshot expiration, orphan cleanup for bronze/silver/gold tables
       - Uses maintenance.py functions via SparkSubmitOperator

       `etl/dags/quality_report_dag.py`:
       - DAG id: "quality_monitoring_report"
       - Schedule: "0 9 * * *" (daily 09:00 UTC, after all pipelines)
       - Tags: ["quality", "monitoring"]
       - Runs quality checks across all tables and outputs structured results for Grafana

    5. Write `etl/tests/unit/test_job_inventory.py`:
       - @pytest.mark.unit marker
       - Test DataStageJob serialization/deserialization
       - Test JobInventory CRUD operations
       - Test filter_by_complexity, filter_by_source_system, filter_by_status
       - Test get_migration_stats
       - Test classify_complexity rules
       - Test save_to_json/load_from_json roundtrip
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_job_inventory.py tests/unit/test_dag_integrity.py -x --tb=short -v</automated>
  </verify>
  <done>DataStage job inventory module supports full CRUD, classification, filtering, and statistics. Five production DAGs follow hybrid pattern (source-specific Bronze/Silver, cross-source Gold). All DAGs load without errors. ETL-07 and partial ETL-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Grafana observability dashboard and ETL patterns documentation</name>
  <files>infra/docker/grafana/dashboards/pipeline_observability.json, infra/docker/grafana/provisioning/dashboards.yml, infra/docker/grafana/provisioning/datasources.yml, docker-compose.yml, docs/etl-patterns.md, etl/tests/unit/test_etl_patterns.py, etl/tests/unit/test_dashboard_config.py</files>
  <action>
    1. Add Grafana + Prometheus services to `docker-compose.yml`:
       - `grafana`: grafana/grafana:latest, port 3001 (avoid conflict with Marquez UI on 3000)
       - Mount provisioning directory for auto-configured datasources and dashboards
       - Mount dashboards directory
       - `prometheus`: prom/prometheus:latest, port 9090
       - Mount prometheus.yml config scraping Airflow StatsD exporter
       - Add `statsd-exporter`: prom/statsd-exporter:latest, port 9125 (UDP) + 9102 (metrics)
       - Configure Airflow to send StatsD metrics to statsd-exporter

    2. Create `infra/docker/grafana/provisioning/datasources.yml`:
       - Prometheus datasource pointing to http://prometheus:9090

    3. Create `infra/docker/grafana/provisioning/dashboards.yml`:
       - Dashboard provider pointing to /var/lib/grafana/dashboards

    4. Create `infra/docker/grafana/dashboards/pipeline_observability.json`:
       - Grafana dashboard JSON with panels for (per locked decision -- combined ops + data view):
       - Operational metrics: DAG run success/failure rates (Airflow StatsD), DAG run durations, SLA compliance (% on-time), active/queued/running tasks
       - Data metrics: Data freshness (last successful run timestamp per DAG), quality scores (from quality report DAG), row counts per table
       - Layout: Top row summary (success rate, SLA compliance, freshness), middle row operational panels, bottom row data quality panels
       - Use Prometheus queries targeting Airflow StatsD metrics (dag.*, task.*)

    5. Create `docs/etl-patterns.md` (per ETL-06 -- standardized patterns for 40+ engineer team):
       - Section 1: Architecture Overview -- Medallion layers, namespace convention, data flow
       - Section 2: Creating a New Pipeline -- Step-by-step guide extending BasePipeline
       - Section 3: Quality Checks -- How to write SodaCL YAML, critical vs advisory checks
       - Section 4: DAG Patterns -- Hybrid DAG design, retry/failure handling
       - Section 5: Incremental Loading -- Watermark-based, CDC, MERGE INTO
       - Section 6: Mainframe Sources -- Cobrix usage, COBOL copybook handling
       - Section 7: Testing -- Unit test patterns, integration test patterns, reconciliation
       - Section 8: Job Inventory -- How to catalog DataStage jobs, complexity classification
       - Include code examples from actual project implementations
       - This is the team onboarding document per locked decision "opinionated enough to enforce consistency"

    6. Write `etl/tests/unit/test_etl_patterns.py`:
       - @pytest.mark.unit marker
       - Test that docs/etl-patterns.md exists and has all required sections
       - Test that all code examples in the doc are importable (extract import statements and try importing)
       - This is a meta-test ensuring documentation stays in sync with code

    7. Write `etl/tests/unit/test_dashboard_config.py`:
       - @pytest.mark.unit marker
       - Test that pipeline_observability.json is valid JSON
       - Test that dashboard has required panel types (graph, stat, table)
       - Test that datasource references match provisioned datasources
       - Test that Prometheus queries reference expected Airflow metric names
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_etl_patterns.py tests/unit/test_dashboard_config.py -x --tb=short -v</automated>
  </verify>
  <done>Grafana dashboard provides combined ops + data observability view. ETL patterns documented for team onboarding. Dashboard config validates as correct JSON with expected panels. Documentation tests confirm all sections present and code examples importable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end Phase 2 system</name>
  <files>N/A</files>
  <action>
    Human verifies the complete Phase 2 ETL pipeline system:
    - ETL framework with BasePipeline, Bronze/Silver/Gold pipelines for trades and positions
    - Airflow 3.1.x with 5 production DAGs (hybrid pattern: source-specific + cross-source)
    - Soda Core quality gates between layers (critical blocks, advisory warns)
    - OpenLineage lineage capture (Airflow plugin + Spark agent to Marquez)
    - Source-to-lakehouse reconciliation framework
    - Incremental/delta loading with watermark and MERGE INTO
    - Mainframe COBOL parsing via Cobrix
    - DataStage job inventory with complexity classification
    - Grafana observability dashboard (ops + data metrics)
    - ETL patterns documentation for 40+ engineer team

    Verification steps:
    1. Start the full stack: cd /home/azureuser/lakehouse and docker compose up -d
    2. Wait for services: Airflow UI at http://localhost:8081, Marquez UI at http://localhost:3000, Grafana at http://localhost:3001
    3. Verify Airflow: Open http://localhost:8081, confirm all 5 DAGs appear (bronze_silver_trades, bronze_silver_positions, gold_trading_metrics, iceberg_maintenance, quality_monitoring_report)
    4. Trigger a DAG: Manually trigger bronze_silver_trades and watch it complete
    5. Verify Marquez lineage: Open http://localhost:3000, search for lakehouse namespace, confirm lineage graph shows Bronze to Silver flow
    6. Verify Grafana: Open http://localhost:3001, confirm Pipeline Observability dashboard shows panels
    7. Verify docs: Open docs/etl-patterns.md, confirm it covers all 8 sections
  </action>
  <verify>Human visual verification of Airflow UI, Marquez lineage graph, and Grafana dashboard</verify>
  <done>User types "approved" confirming end-to-end flow works, or describes issues for resolution</done>
</task>

</tasks>

<verification>
1. Job inventory tests pass: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_job_inventory.py -x --tb=short`
2. ETL patterns tests pass: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_etl_patterns.py -x --tb=short`
3. Dashboard config tests pass: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_dashboard_config.py -x --tb=short`
4. All DAGs load: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_dag_integrity.py -x --tb=short`
5. Docker Compose validates with all services: `cd /home/azureuser/lakehouse && docker compose config --quiet`
6. Full unit test suite: `cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/ -x --tb=short`
</verification>

<success_criteria>
- DataStage job inventory classifies jobs as simple/medium/complex with full metadata (ETL-07)
- ETL patterns documentation covers all 8 sections with code examples (ETL-06)
- 5 production DAGs follow hybrid pattern and load without errors (ETL-04 production)
- Grafana dashboard shows combined ops + data metrics (PLAT-02, locked decision)
- Human verification confirms end-to-end flow: DAG trigger -> pipeline execution -> lineage in Marquez -> metrics in Grafana
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-etl-migration-and-data-pipeline/02-05-SUMMARY.md`
</output>
