---
phase: 01-foundation-and-feasibility-validation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - infra/terraform/modules/s3/main.tf
  - infra/terraform/modules/s3/variables.tf
  - infra/terraform/modules/s3/outputs.tf
  - infra/terraform/modules/minio/main.tf
  - infra/terraform/modules/minio/variables.tf
  - infra/terraform/modules/minio/outputs.tf
  - infra/terraform/modules/networking/main.tf
  - infra/terraform/modules/networking/variables.tf
  - infra/terraform/modules/networking/outputs.tf
  - infra/terraform/modules/nessie/main.tf
  - infra/terraform/modules/nessie/variables.tf
  - infra/terraform/modules/nessie/outputs.tf
  - infra/terraform/modules/nessie/values.yaml.tpl
  - infra/terraform/modules/trino/main.tf
  - infra/terraform/modules/trino/variables.tf
  - infra/terraform/modules/trino/outputs.tf
  - infra/terraform/modules/trino/values.yaml.tpl
  - infra/terraform/main.tf
  - ci/.github/workflows/ci.yml
  - ci/.github/workflows/deploy-dev.yml
  - ci/.github/workflows/deploy-staging.yml
  - ci/.github/workflows/deploy-prod.yml
  - ci/.github/workflows/infra.yml
  - .github/workflows/ci.yml
  - .github/workflows/deploy-dev.yml
  - .github/workflows/deploy-staging.yml
  - .github/workflows/deploy-prod.yml
  - .github/workflows/infra.yml
autonomous: true
requirements:
  - CICD-02
  - CICD-03
  - CICD-04
  - SEC-05
  - SEC-06

must_haves:
  truths:
    - "Terraform modules define the complete infrastructure for Nessie, Trino, S3, MinIO, and networking"
    - "Terraform validate passes for all modules and the root configuration"
    - "GitHub Actions CI workflow runs lint, test, and terraform validate on every PR"
    - "Environment promotion workflows exist for dev, staging, and prod deployments"
    - "S3 bucket configuration includes SSE-KMS encryption at rest"
    - "Nessie and Trino Terraform modules include TLS configuration for encryption in transit"
    - "Each environment (dev/staging/prod) has its own isolated Nessie catalog and Trino cluster"
    - "GitHub Actions discovers workflows at .github/workflows/ (symlinked or copied from ci/.github/workflows/)"
  artifacts:
    - path: "infra/terraform/modules/nessie/main.tf"
      provides: "Nessie Helm deployment with PostgreSQL backend"
      contains: "helm_release"
      min_lines: 30
    - path: "infra/terraform/modules/trino/main.tf"
      provides: "Trino Helm deployment with Iceberg/Nessie catalog"
      contains: "helm_release"
      min_lines: 30
    - path: "infra/terraform/modules/s3/main.tf"
      provides: "S3 buckets with SSE-KMS encryption"
      contains: "server_side_encryption_configuration"
      min_lines: 20
    - path: "ci/.github/workflows/deploy-dev.yml"
      provides: "Automated deployment to dev on merge to dev branch"
      contains: "terraform apply"
    - path: "ci/.github/workflows/deploy-prod.yml"
      provides: "Automated deployment to prod on merge to main"
      contains: "terraform apply"
    - path: ".github/workflows/ci.yml"
      provides: "Symlinked or copied workflow file discoverable by GitHub Actions"
      contains: "on:"
  key_links:
    - from: "infra/terraform/main.tf"
      to: "infra/terraform/modules/nessie/main.tf"
      via: "Root module references nessie module"
      pattern: "module.*nessie"
    - from: "ci/.github/workflows/infra.yml"
      to: "infra/terraform/main.tf"
      via: "CI workflow runs terraform plan/apply on infra changes"
      pattern: "terraform.*plan|terraform.*apply"
    - from: "infra/terraform/modules/s3/main.tf"
      to: "aws_kms_key"
      via: "S3 encryption uses KMS key for SSE-KMS"
      pattern: "aws_kms_key|sse_algorithm.*aws:kms"
    - from: ".github/workflows/"
      to: "ci/.github/workflows/"
      via: "Symlink or copy ensures GitHub Actions discovers workflows at repo root"
      pattern: "ci/.github/workflows"
---

<objective>
Create Terraform IaC modules for all lakehouse infrastructure components (Nessie, Trino, S3, MinIO, networking) and GitHub Actions CI/CD workflows for automated testing, deployment, and environment promotion. Includes encryption at rest (S3 SSE-KMS) and TLS configuration for encryption in transit.

Purpose: Infrastructure as Code and CI/CD pipelines are prerequisites for production-grade deployment. S3 encryption and TLS satisfy SEC-05 and SEC-06 security requirements. The environment promotion workflow (dev -> staging -> prod) with full isolation per environment is a locked decision.
Output: Complete Terraform module library, GitHub Actions workflows for CI and deployment, encryption configurations.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-feasibility-validation/01-CONTEXT.md
@.planning/phases/01-foundation-and-feasibility-validation/01-RESEARCH.md
@.planning/phases/01-foundation-and-feasibility-validation/01-01-SUMMARY.md

Key interfaces from Plan 01 (executor should use directly):
- infra/terraform/main.tf: Root module already references ./modules/nessie, ./modules/trino, etc. (stubs)
- infra/terraform/variables.tf: Already declares environment, region, nessie_replicas, trino_workers, etc.
- infra/terraform/environments/{dev,staging,prod}/terraform.tfvars: Environment-specific values already defined
- infra/terraform/backend.tf: S3 remote state already configured
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Terraform modules for S3, networking, and MinIO</name>
  <files>
    infra/terraform/modules/s3/main.tf
    infra/terraform/modules/s3/variables.tf
    infra/terraform/modules/s3/outputs.tf
    infra/terraform/modules/minio/main.tf
    infra/terraform/modules/minio/variables.tf
    infra/terraform/modules/minio/outputs.tf
    infra/terraform/modules/networking/main.tf
    infra/terraform/modules/networking/variables.tf
    infra/terraform/modules/networking/outputs.tf
  </files>
  <action>
    Create three simpler Terraform modules: S3 (with encryption), MinIO (configuration only), and networking (security groups). These have no Helm charts, no secrets config, and no TLS complexity.

    1. `modules/s3/` -- AWS S3 bucket configuration with encryption:
       - `main.tf`:
         - `aws_kms_key` resource for S3 encryption (description, enable_key_rotation=true, deletion_window_in_days=30)
         - `aws_kms_alias` for the key
         - `aws_s3_bucket` for lakehouse data (bucket name from variable with environment prefix)
         - `aws_s3_bucket_server_side_encryption_configuration` with SSE-KMS using the KMS key (sse_algorithm = "aws:kms")
         - `aws_s3_bucket_versioning` enabled
         - `aws_s3_bucket_public_access_block` (block all public access)
         - `aws_s3_bucket_lifecycle_configuration` for incomplete multipart upload cleanup
         - IAM policy document for Nessie and Trino service accounts to access the bucket
       - `variables.tf`: environment, bucket_name_prefix, kms_key_deletion_window, tags
       - `outputs.tf`: bucket_name, bucket_arn, kms_key_arn, kms_key_id, iam_policy_arn

    2. `modules/minio/` -- MinIO configuration (not deployment -- team operates MinIO):
       - `main.tf`: This module does NOT deploy MinIO (team already operates it). Instead it:
         - Creates a `null_resource` with provisioner that runs mc commands to create buckets (lakehouse-data, lakehouse-onprem) and set encryption policies
         - Stores MinIO endpoint and credentials as outputs (from variables)
         - Creates a Kubernetes secret with MinIO credentials for Nessie/Trino to consume
       - `variables.tf`: minio_endpoint, minio_access_key (sensitive), minio_secret_key (sensitive), bucket_names (list), namespace
       - `outputs.tf`: minio_endpoint, bucket_names, credentials_secret_name

    3. `modules/networking/` -- VPC and security groups (stub for Phase 1):
       - `main.tf`: Create security groups allowing:
         - Nessie API (19120) from Trino and ETL workloads
         - Trino coordinator (8443) from authorized CIDR blocks
         - PostgreSQL (5432) from Nessie only
         - MinIO (9000) from Nessie and Trino
         - All services: TLS (443) outbound for S3 access
       - `variables.tf`: vpc_id, allowed_cidrs, environment, tags
       - `outputs.tf`: nessie_sg_id, trino_sg_id, postgres_sg_id

    IMPORTANT per locked decisions:
    - SSE-KMS for S3 for SEC-05
    - Separate infrastructure per environment (full isolation)
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/infra/terraform && for mod in s3 minio networking; do echo "--- $mod ---" && ls modules/$mod/*.tf; done 2>&1</automated>
  </verify>
  <done>
    Three Terraform modules exist (s3, minio, networking) with main.tf, variables.tf, and outputs.tf each. S3 module includes SSE-KMS encryption configuration (SEC-05) with KMS key rotation enabled. MinIO module creates bucket configuration and Kubernetes secrets. Networking module defines security groups with least-privilege port access.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Terraform modules for Nessie and Trino, wire root module</name>
  <files>
    infra/terraform/modules/nessie/main.tf
    infra/terraform/modules/nessie/variables.tf
    infra/terraform/modules/nessie/outputs.tf
    infra/terraform/modules/nessie/values.yaml.tpl
    infra/terraform/modules/trino/main.tf
    infra/terraform/modules/trino/variables.tf
    infra/terraform/modules/trino/outputs.tf
    infra/terraform/modules/trino/values.yaml.tpl
    infra/terraform/main.tf
  </files>
  <action>
    Create the two complex Terraform modules (Nessie and Trino) that involve Helm charts, TLS/LDAP/KMS secrets configuration, and values templates. Then wire all five modules together in the root main.tf.

    1. `modules/nessie/` -- Nessie catalog deployment via Helm:
       - `main.tf`: Use `helm_release` resource to deploy Nessie from Bitnami chart. Configure:
         - versionStoreType = JDBC_POSTGRESQL
         - PostgreSQL enabled with configurable password (sensitive variable)
         - replicaCount from variable (default 2)
         - Catalog configuration: default warehouse, S3/MinIO endpoints, regions
         - TLS: Configure HTTPS on Nessie API endpoint (Quarkus TLS config) using cert-manager generated certs (reference a secret name)
         - Use `templatefile()` for values.yaml.tpl to inject environment-specific settings
       - `variables.tf`: environment, namespace, nessie_replicas, nessie_db_password (sensitive), s3_bucket, s3_region, minio_endpoint, minio_bucket, minio_access_key (sensitive), minio_secret_key (sensitive), tls_secret_name
       - `outputs.tf`: nessie_endpoint (including http/https prefix), nessie_internal_endpoint
       - `values.yaml.tpl`: Helm values template with all Nessie configuration (per Research Pattern 1)

    2. `modules/trino/` -- Trino cluster deployment via Helm:
       - `main.tf`: Use `helm_release` for Trino chart. Configure:
         - Coordinator with HTTPS enabled (http-server.https.enabled=true, port 8443)
         - Worker count from variable (default 2)
         - Iceberg catalog properties via REST catalog type pointing to Nessie (per Research: connector.name=iceberg, iceberg.catalog.type=rest, iceberg.rest-catalog.uri pointing to nessie_endpoint)
         - LDAP authentication configuration (password-authenticator.properties) -- values templated from variables
         - File-based access control (access-control.properties pointing to rules.json) -- rules file content from variable
         - TLS keystore configuration for HTTPS
         - JVM config with memory settings based on instance size
       - `variables.tf`: environment, namespace, trino_workers, coordinator_memory, worker_memory, nessie_endpoint, ldap_url, ldap_user_bind_pattern, ldap_user_base_dn, tls_keystore_path, tls_keystore_password (sensitive), access_control_rules (JSON string)
       - `outputs.tf`: trino_endpoint, trino_coordinator_service
       - `values.yaml.tpl`: Helm values with catalog config, security config, resource limits

    3. Update `infra/terraform/main.tf` -- Wire all five modules together:
       - Remove .gitkeep references, add proper module blocks for all five modules
       - Pass outputs from one module as inputs to another (s3 bucket -> nessie, nessie endpoint -> trino, networking SGs -> nessie/trino)
       - Use terraform workspace for environment isolation

    IMPORTANT per locked decisions:
    - Separate Trino cluster and Nessie catalog per environment (full isolation)
    - Use REST catalog type for Trino-Nessie connection (not Nessie-specific type)
    - PostgreSQL as Nessie metadata backing store
    - TLS on all endpoints for SEC-06
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/infra/terraform && terraform init -backend=false 2>&1 | tail -5 && terraform validate 2>&1</automated>
  </verify>
  <done>
    Nessie and Trino Terraform modules exist with main.tf, variables.tf, outputs.tf, and values.yaml.tpl each. Root main.tf properly references and wires all five modules (s3, minio, networking, nessie, trino) together. terraform validate passes. Nessie module includes TLS configuration (SEC-06). Trino module includes TLS, LDAP auth, and file-based access control configuration (SEC-06). Each environment gets isolated infrastructure through workspace-based configuration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub Actions CI/CD workflows for environment promotion</name>
  <files>
    ci/.github/workflows/ci.yml
    ci/.github/workflows/deploy-dev.yml
    ci/.github/workflows/deploy-staging.yml
    ci/.github/workflows/deploy-prod.yml
    ci/.github/workflows/infra.yml
    .github/workflows/ci.yml
    .github/workflows/deploy-dev.yml
    .github/workflows/deploy-staging.yml
    .github/workflows/deploy-prod.yml
    .github/workflows/infra.yml
  </files>
  <action>
    Create five GitHub Actions workflows implementing the locked decision: branch-based environment promotion (feature branches -> PR to dev -> merge to staging -> merge to main/prod). Then ensure GitHub can discover them at the repo root.

    1. `ci/.github/workflows/ci.yml` -- PR validation (update existing stub from Plan 01):
       Trigger: pull_request to dev, staging, main branches
       Jobs:
       - **python-lint**: Setup Python 3.11, install ruff, run `ruff check etl/` and `ruff format --check etl/`
       - **python-test**: Setup Python 3.11, install dependencies from etl/pyproject.toml, run `cd etl && pytest tests/unit/ -x --tb=short`
       - **terraform-validate**: Setup Terraform (hashicorp/setup-terraform@v3), run `cd infra/terraform && terraform init -backend=false && terraform validate`
       - **terraform-fmt**: Run `terraform fmt -check -recursive infra/terraform/`
       All jobs run in parallel. Use `actions/checkout@v4`.

    2. `ci/.github/workflows/deploy-dev.yml` -- Deploy to dev on merge to dev branch:
       Trigger: push to dev branch
       Jobs:
       - **deploy**:
         - permissions: id-token: write, contents: read (OIDC for AWS)
         - Setup AWS credentials via OIDC (aws-actions/configure-aws-credentials@v4, role-to-assume from secrets)
         - Setup Terraform
         - `cd infra/terraform && terraform init && terraform workspace select dev && terraform plan -var-file=environments/dev/terraform.tfvars -out=tfplan`
         - `terraform apply tfplan`
         - Run integration tests against dev environment (optional, gated by test success)

    3. `ci/.github/workflows/deploy-staging.yml` -- Deploy to staging on merge to staging branch:
       Trigger: push to staging branch
       Same structure as deploy-dev but:
       - workspace select staging
       - var-file=environments/staging/terraform.tfvars
       - Requires manual approval via GitHub environment protection rules (add `environment: staging` to job)

    4. `ci/.github/workflows/deploy-prod.yml` -- Deploy to prod on merge to main:
       Trigger: push to main branch
       Same structure as deploy-staging but:
       - workspace select prod
       - var-file=environments/prod/terraform.tfvars
       - `environment: production` with required reviewers
       - Additional step: run smoke tests after deploy (health check on Nessie and Trino endpoints)

    5. `ci/.github/workflows/infra.yml` -- Terraform plan on infra changes (PR only):
       Trigger: pull_request paths: ['infra/**']
       Jobs:
       - **plan**:
         - Setup AWS credentials via OIDC
         - terraform init, terraform plan for each environment (dev, staging, prod)
         - Post plan output as PR comment using `actions/github-script@v7`
         - Do NOT run terraform apply (plan only for review)

    6. **GitHub workflow discovery** -- GitHub Actions requires workflows at `.github/workflows/` at repo root. After creating all workflow files in `ci/.github/workflows/`, create a symlink: `.github/workflows -> ../../ci/.github/workflows`. If git does not follow symlinked directories properly (test with `git ls-files .github/workflows/`), instead copy each workflow file to `.github/workflows/` and add a header comment `# Source of truth: ci/.github/workflows/<filename>`. Also create `ci/README.md` explaining the authoring convention.

    IMPORTANT:
    - Use OIDC for AWS authentication (no long-lived credentials) -- per Research state-of-the-art
    - Reference secrets via ${{ secrets.AWS_ROLE_ARN }} (do not hardcode ARNs)
    - All workflows must have proper `permissions` blocks (least privilege)
    - Use `actions/checkout@v4` and `hashicorp/setup-terraform@v3` (pinned major versions)
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse && ls -la ci/.github/workflows/*.yml && for f in ci/.github/workflows/*.yml; do python -c "import yaml; yaml.safe_load(open('$f'))" && echo "$f: valid YAML"; done && echo "--- GitHub root discovery ---" && ls -la .github/workflows/ 2>&1 | head -10</automated>
  </verify>
  <done>
    Five GitHub Actions workflow files exist in ci/.github/workflows/: ci.yml (PR checks), deploy-dev.yml, deploy-staging.yml, deploy-prod.yml (environment deployments), infra.yml (Terraform plan on PR). All are valid YAML. CI workflow runs lint, test, and terraform validate. Deployment workflows use OIDC for AWS auth, workspace-based environment selection, and environment protection rules. GitHub can discover workflows at .github/workflows/ (verified via `ls .github/workflows/` showing all 5 workflow files either as symlinks or copies).
  </done>
</task>

</tasks>

<verification>
1. `cd infra/terraform && terraform init -backend=false && terraform validate` -- Terraform validates
2. `cd infra/terraform && terraform fmt -check -recursive` -- Terraform formatting correct
3. `ls ci/.github/workflows/*.yml | wc -l` -- 5 workflow files exist
4. `ls .github/workflows/*.yml 2>/dev/null | wc -l` -- 5 workflow files discoverable at repo root
5. `grep -l "aws:kms" infra/terraform/modules/s3/main.tf` -- S3 encryption configured (SEC-05)
6. `grep -l "https" infra/terraform/modules/nessie/main.tf infra/terraform/modules/trino/main.tf` -- TLS configured (SEC-06)
7. `grep -l "id-token.*write" ci/.github/workflows/deploy-*.yml` -- OIDC configured for AWS auth
</verification>

<success_criteria>
- Terraform validate passes for all modules and root configuration
- Five Terraform modules exist (nessie, trino, s3, minio, networking) with proper variable/output declarations
- S3 module configures SSE-KMS encryption at rest (SEC-05)
- Nessie and Trino modules include TLS/HTTPS configuration (SEC-06)
- Five GitHub Actions workflows cover: PR validation, dev deploy, staging deploy, prod deploy, infra plan
- GitHub discovers workflows at .github/workflows/ (symlinked or copied from ci/.github/workflows/)
- Deployment workflows use OIDC (no long-lived credentials)
- Environment promotion follows locked decision: feature -> dev -> staging -> main (prod)
- Each environment gets isolated infrastructure (separate Nessie + Trino per env)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-feasibility-validation/01-03-SUMMARY.md`
</output>
