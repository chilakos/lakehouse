---
phase: 01-foundation-and-feasibility-validation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - etl/src/synthetic/generators.py
  - etl/src/synthetic/__init__.py
  - etl/src/iceberg_utils/catalog.py
  - etl/src/iceberg_utils/maintenance.py
  - etl/src/iceberg_utils/__init__.py
  - etl/tests/integration/test_iceberg_s3.py
  - etl/tests/integration/test_iceberg_minio.py
  - etl/tests/integration/test_nessie_dual_storage.py
  - etl/tests/integration/test_schema_evolution.py
  - etl/tests/integration/test_partition_evolution.py
  - etl/tests/integration/test_table_maintenance.py
  - etl/tests/unit/test_generators.py
autonomous: true
requirements:
  - FNDTN-01
  - FNDTN-02
  - FNDTN-03
  - FNDTN-04
  - FNDTN-05
  - FNDTN-06

must_haves:
  truths:
    - "An Iceberg table created via PySpark is stored on MinIO (local S3-compatible) and queryable through Nessie catalog"
    - "The same Nessie catalog serves tables on both S3 and MinIO storage backends without separate catalog instances"
    - "Schema evolution (add column, widen type) applied to an Iceberg table is reflected in metadata without data rewrites"
    - "Partition evolution changes partitioning strategy without rewriting existing data"
    - "Automated table maintenance (compaction, snapshot expiration, orphan file cleanup) runs successfully on Iceberg tables"
    - "Synthetic financial data (trades, positions, risk metrics) provides realistic test data for feasibility validation"
  artifacts:
    - path: "etl/src/synthetic/generators.py"
      provides: "Deterministic synthetic financial data generators"
      contains: "generate_trades"
      min_lines: 80
    - path: "etl/src/iceberg_utils/catalog.py"
      provides: "Nessie catalog interaction utilities (create namespace, create table, list tables)"
      contains: "create_namespace"
      min_lines: 50
    - path: "etl/src/iceberg_utils/maintenance.py"
      provides: "Iceberg table maintenance procedures (compaction, snapshot expiration, orphan cleanup)"
      contains: "compact_table"
      min_lines: 40
    - path: "etl/tests/integration/test_iceberg_s3.py"
      provides: "Integration tests for Iceberg CRUD on S3/MinIO"
      contains: "test_create_table"
    - path: "etl/tests/integration/test_schema_evolution.py"
      provides: "Integration tests for schema evolution across engines"
      contains: "test_add_column"
  key_links:
    - from: "etl/src/synthetic/generators.py"
      to: "etl/src/iceberg_utils/catalog.py"
      via: "Generators produce data that catalog utilities write to Iceberg tables"
      pattern: "generate_trades|create_table"
    - from: "etl/src/iceberg_utils/catalog.py"
      to: "docker-compose.yml"
      via: "Catalog utilities connect to Nessie REST endpoint from Docker Compose"
      pattern: "nessie.*19120|rest-catalog"
    - from: "etl/src/iceberg_utils/maintenance.py"
      to: "etl/src/iceberg_utils/catalog.py"
      via: "Maintenance procedures operate on tables managed by catalog utilities"
      pattern: "rewrite_data_files|expire_snapshots"
---

<objective>
Create synthetic financial data generators, Iceberg table creation and maintenance utilities, and integration tests proving that Iceberg tables work on both S3-compatible storage (MinIO locally, AWS S3 in cloud) through a shared Nessie catalog with schema evolution, partition evolution, and automated maintenance.

Purpose: This is the core data layer that all multi-engine query validation (Plan 03/04) depends on. Without working Iceberg tables on both storage backends, the multi-engine feasibility proof cannot proceed.
Output: Synthetic data generators, Iceberg catalog utilities, table maintenance scripts, and integration tests for FNDTN-01 through FNDTN-06.
</objective>

<execution_context>
@/home/azureuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/azureuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-feasibility-validation/01-CONTEXT.md
@.planning/phases/01-foundation-and-feasibility-validation/01-RESEARCH.md
@.planning/phases/01-foundation-and-feasibility-validation/01-01-SUMMARY.md

Key interfaces from Plan 01 (executor should use directly):
- docker-compose.yml: Nessie at localhost:19120, MinIO at localhost:9000, Trino at localhost:8080, PostgreSQL at localhost:5432
- etl/tests/conftest.py: spark_session fixture (Iceberg REST catalog configured), minio_client fixture, nessie_url fixture, trino_connection fixture
- etl/src/config/settings.py: Settings class with NESSIE_URI, MINIO_ENDPOINT, S3_ENDPOINT, etc.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synthetic data generators and Iceberg catalog utilities</name>
  <files>
    etl/src/synthetic/generators.py
    etl/src/synthetic/__init__.py
    etl/src/iceberg_utils/catalog.py
    etl/src/iceberg_utils/maintenance.py
    etl/src/iceberg_utils/__init__.py
    etl/tests/unit/test_generators.py
    etl/tests/integration/test_iceberg_s3.py
    etl/tests/integration/test_iceberg_minio.py
    etl/tests/integration/test_nessie_dual_storage.py
  </files>
  <behavior>
    Unit tests (generators):
    - generate_trades(100) returns list of 100 dicts with keys: trade_id, trade_date, symbol, side, trade_type, quantity, price, notional, account_id, trader_id, exchange, settlement_date
    - generate_positions(50) returns list of 50 dicts with keys: position_id, account_id, symbol, quantity, market_value, as_of_date, sector, currency
    - generate_risk_metrics(30) returns list of 30 dicts with keys: metric_id, account_id, var_95, var_99, expected_shortfall, stress_pnl, calc_date, model_version
    - All generators are deterministic with seed parameter (same seed = same output)
    - Numeric fields use Decimal type for financial precision

    Integration tests (Iceberg on MinIO via Nessie -- runs against Docker Compose):
    - test_create_namespace: Create namespace "test_ns" via Nessie, verify it exists
    - test_create_iceberg_table_minio: Create trades table on MinIO (s3://lakehouse-data/...), insert 100 rows, read back and verify count
    - test_create_iceberg_table_onprem: Create positions table on MinIO on-prem bucket (s3://lakehouse-onprem/...), insert 50 rows, verify
    - test_nessie_dual_storage: Create one table on lakehouse-data bucket, another on lakehouse-onprem bucket, verify both accessible from same Nessie catalog
    - test_read_back_data_matches: Insert known synthetic data, read back, verify values match exactly (including Decimal precision)
  </behavior>
  <action>
    1. Create `etl/src/synthetic/generators.py`:
       - `generate_trades(num_records: int, seed: int = 42) -> list[dict]` -- Uses Faker with fixed seed. Financial symbols (15 common stocks), sides (BUY/SELL), trade types (MARKET/LIMIT/STOP/STOP_LIMIT). All prices/quantities use Decimal for financial precision. Include fields per Research code example.
       - `generate_positions(num_records: int, seed: int = 42) -> list[dict]` -- Portfolio positions with account_id, symbol, quantity, market_value, as_of_date, sector, currency.
       - `generate_risk_metrics(num_records: int, seed: int = 42) -> list[dict]` -- Risk metrics: VaR 95/99, expected shortfall, stress P&L, calc_date, model_version.
       - `trades_schema() -> StructType` -- Returns PySpark schema for trades table.
       - `positions_schema() -> StructType` -- Returns PySpark schema for positions table.
       - `risk_metrics_schema() -> StructType` -- Returns PySpark schema for risk metrics table.

    2. Create `etl/src/iceberg_utils/catalog.py`:
       - `get_spark_session(nessie_uri: str, warehouse: str, s3_endpoint: str | None = None) -> SparkSession` -- Configures SparkSession with Iceberg REST catalog pointed at Nessie. If s3_endpoint is provided, configure S3 to point at MinIO (path-style-access, endpoint override).
       - `create_namespace(spark: SparkSession, namespace: str) -> None` -- Creates namespace if not exists using spark.sql.
       - `create_iceberg_table(spark: SparkSession, namespace: str, table_name: str, schema: StructType, location: str, partition_by: list[str] | None = None) -> None` -- Creates Iceberg table with USING iceberg LOCATION clause. Optional partitioning.
       - `write_data(spark: SparkSession, namespace: str, table_name: str, data: list[dict], schema: StructType) -> None` -- Converts data to DataFrame and writes to Iceberg table using append mode.
       - `read_table(spark: SparkSession, namespace: str, table_name: str) -> DataFrame` -- Reads Iceberg table and returns DataFrame.

    3. Create `etl/src/iceberg_utils/maintenance.py`:
       - `compact_table(spark: SparkSession, namespace: str, table_name: str, target_file_size_bytes: int = 268435456) -> dict` -- Calls rewrite_data_files procedure per Research code example. Returns result dict.
       - `expire_snapshots(spark: SparkSession, namespace: str, table_name: str, older_than_days: int = 7, retain_last: int = 10) -> dict` -- Calls expire_snapshots procedure.
       - `remove_orphan_files(spark: SparkSession, namespace: str, table_name: str, older_than_days: int = 3) -> dict` -- Calls remove_orphan_files procedure.
       - `rewrite_manifests(spark: SparkSession, namespace: str, table_name: str) -> dict` -- Calls rewrite_manifests procedure.
       - `full_maintenance(spark: SparkSession, namespace: str, table_name: str) -> dict` -- Runs all four maintenance operations in sequence. Returns combined results.

    4. Write unit tests first (RED), then implement generators (GREEN). Integration tests require Docker Compose services running.

    IMPORTANT: Use the REST catalog type for Spark (not Nessie-specific type) per Research anti-pattern guidance. Configure S3 with path-style-access for MinIO. Use "lakehouse" as the catalog name in Spark.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/unit/test_generators.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    Unit tests for generators pass: all three generators produce correct record counts, correct field names, deterministic output with same seed, and Decimal types for financial fields. Catalog utilities and maintenance functions are implemented with correct Spark/Nessie configuration. Integration tests exist for Iceberg CRUD on both storage backends through Nessie (will pass when Docker services are running).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema evolution, partition evolution, and table maintenance integration tests</name>
  <files>
    etl/tests/integration/test_schema_evolution.py
    etl/tests/integration/test_partition_evolution.py
    etl/tests/integration/test_table_maintenance.py
  </files>
  <behavior>
    Integration tests (require Docker Compose services):
    - test_add_column: Create trades table, add settlement_currency column, verify column appears in schema, verify existing data has null for new column
    - test_widen_type: Create table with INT quantity column, widen to BIGINT, verify existing data is still readable
    - test_schema_evolution_no_data_rewrite: Verify that after schema evolution, file count in $files metadata table has not increased (metadata-only change)
    - test_partition_by_date: Create table partitioned by trade_date (day), insert data, verify partition pruning works (query for single day scans fewer files)
    - test_partition_evolution: Change partitioning from day to month, verify old data still readable, new data written with new partition scheme
    - test_compact_small_files: Write 10 small batches (10 rows each), compact, verify file count decreased and data is still correct
    - test_expire_snapshots: Write data, write more data (creating snapshots), expire old snapshots, verify current data intact
    - test_remove_orphan_files: Run orphan file cleanup, verify no errors and valid data still readable
    - test_full_maintenance_cycle: Run full_maintenance() and verify table is still queryable with correct data
  </behavior>
  <action>
    Create the three integration test files that validate FNDTN-04, FNDTN-05, and FNDTN-06.

    1. `etl/tests/integration/test_schema_evolution.py`:
       - Uses spark_session fixture from conftest.py
       - Creates a fresh namespace and trades table for each test (use clean_nessie fixture)
       - Tests add column via `ALTER TABLE lakehouse.{ns}.trades ADD COLUMN settlement_currency STRING`
       - Tests widen type via `ALTER TABLE lakehouse.{ns}.trades ALTER COLUMN quantity SET DATA TYPE BIGINT` (or equivalent Spark SQL)
       - Verifies metadata-only by checking `SELECT * FROM lakehouse.{ns}.trades.files` count before and after evolution
       - Each test writes initial data using generators, performs evolution, then reads back to verify

    2. `etl/tests/integration/test_partition_evolution.py`:
       - Creates trades table partitioned by days(trade_date)
       - Inserts synthetic data spanning multiple days
       - Verifies partition pruning: queries for a single day should show fewer files scanned (check query plan or file count)
       - Tests partition evolution: change from days(trade_date) to months(trade_date) using Spark SQL ALTER TABLE
       - Verifies old data still readable after partition evolution
       - Verifies new writes use the new partition scheme

    3. `etl/tests/integration/test_table_maintenance.py`:
       - Uses maintenance.py functions
       - test_compact: Writes 10 small batches to create many small files, runs compact_table(), verifies file count decreased
       - test_expire: Creates multiple snapshots by writing data in batches, runs expire_snapshots(), verifies data integrity
       - test_orphan_cleanup: Runs remove_orphan_files(), verifies no valid data was removed
       - test_full_cycle: Runs full_maintenance(), verifies table is still fully queryable

    All integration tests should be marked with `@pytest.mark.integration` and skip gracefully when Docker services are not running.

    IMPORTANT: Use the catalog utilities from Task 1 (etl/src/iceberg_utils/catalog.py) for table creation and data insertion. Do not duplicate that logic in tests.
  </action>
  <verify>
    <automated>cd /home/azureuser/lakehouse/etl && python -m pytest tests/integration/test_schema_evolution.py tests/integration/test_partition_evolution.py tests/integration/test_table_maintenance.py --collect-only 2>&1 | tail -20</automated>
  </verify>
  <done>
    All integration test files exist and are collectible by pytest. Tests cover: schema evolution (add column, widen type, metadata-only verification), partition evolution (day-to-month, old data readable), and table maintenance (compaction, snapshot expiration, orphan cleanup, full cycle). Tests use shared fixtures and catalog utilities from Task 1. Tests will pass when Docker services are running.
  </done>
</task>

</tasks>

<verification>
1. `cd etl && python -m pytest tests/unit/ -x --tb=short` -- Unit tests pass (generators produce correct output)
2. `cd etl && python -m pytest tests/integration/ --collect-only` -- All integration tests are discoverable
3. `python -c "from src.synthetic.generators import generate_trades; print(len(generate_trades(100)))"` -- Generators work
4. `python -c "from src.iceberg_utils.catalog import get_spark_session; print('OK')"` -- Catalog utilities importable
5. `python -c "from src.iceberg_utils.maintenance import full_maintenance; print('OK')"` -- Maintenance utilities importable
</verification>

<success_criteria>
- Synthetic data generators produce deterministic, financially-accurate test data for trades, positions, and risk metrics
- Iceberg catalog utilities create tables on both S3 and MinIO through Nessie REST catalog
- Schema evolution tests validate add-column and widen-type as metadata-only operations
- Partition evolution tests validate day-to-month transition without data rewrite
- Table maintenance tests validate compaction, snapshot expiration, and orphan cleanup
- All unit tests pass; integration tests are collectible and skip gracefully without Docker
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-feasibility-validation/01-02-SUMMARY.md`
</output>
